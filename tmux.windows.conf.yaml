session_name: Dev
windows:
  - name: "ContextHub"
    cwd: "~/projects/ContextHub"
    panes:
      - cmd: "opencode -c"
      - cmd: "wl tui"
        split:
          dir: vertical
          size_pct: 50
      - cmd: "clear"
        split:
          dir: horizontal
          size_pct: 30
