## Standup Report (22nd September 2026)

### Yesterday I completed work on...

  - Scheduled prompts: add time-of-day dispatch (time window) to scheduled-prompts tier — As an operator, I want to add an optional `time` field (e.g. `06:05`) to a scheduled-prompt entry so the system dispatches it in the first available idle window **after** that time, so the standup run
  - [test-failure] CLI documentation validator (integrated) > validator script exits zero and prints OK — failing test — Test name: CLI documentation validator (integrated) > validator script exits zero and prints OK
  - install-herdr.sh should reload herdr config after inserting keybinding — When install-herdr.sh appends the [[keys.command]] prefix+l -> worklog-selection-list.open-worklist block to ~/.config/herdr/config.toml, the running herdr server keeps its startup keybindings in memo
  - Unify audit-freshness at-or-near logic across icon display and downtime dispatcher — Ensure the herdr audit-freshness predicate enforces the canonical "at-or-near"
  - Align wl init with SorraAgents install: delegate agent-guidance/workflow setup instead of duplicating — As a developer running wl init on a machine with the SorraAgents install, I want wl init to delegate agent-guidance/workflow setup to the SorraAgents install so the global guidance is the single sourc
  - Herdr pane title should reflect how it was spawned — As a user, I trigger `/skill:implement <id>` from the worklist, and the
  - Add --fields option to wl list and wl search for field-selective output — As a release-gate script, I want wl list --stage in_review --fields id,title,status,stage,needsProducerReview --json so I stop downloading 4.9 MB of descriptions I discard.
  - Fix stuck stage/status on audit-completed work items — Two work items have completed audits with 'Ready to close: Yes' verdicts but never received the verdict-driven lifecycle transition (status completed / stage in_review). The stage/status axes are stuc
  - packages/herdr npm run build exits 2: TS6059 rootDir violations from test-file imports — `npm run build` in `packages/herdr` exits 2 with 84 `TS6059` errors (`File ... is not under 'rootDir' '/home/.../packages/herdr/src'`).
  - Herdr hydrator: rescue in_progress work items with no active pane — As a producer, I want items whose agent pane has died to be demoted automatically so they re-enter the selection pool instead of rotting in `in_progress`.
  - Sync: regression guard — refuse import of default-value regressions from newer remote state — When `wl sync` merges remote state that is newer than local, it can silently import regressions: fields the local store had set to non-default values (e.g. `effort: 'Small'`, `status: completed`) get
  - test skill: node_suite_commands() ignores detected project root (vacuous node suite for non-framework repos) — Discovered-from: WL-0MSGI7UIH008USVB. skills/test/scripts/run_tests.py run_suite() (line ~318) and run_summary() (line ~526) call node_suite_commands() with NO project_root, so commands are built from
  - Downtime dispatch: recover items stranded by marker-write-failed or spawn-failed (no permanent->in_progress stranding) — As an operator, I do not want a transient failure in the downtime dispatch pipeline to strand a work item in `in_progress` with no agent working it and no automatic recovery.
  - Allow bounded concurrent downtime dispatches (use both cheap-mode slots) — Proxy usage analysis of the 04:00-06:00 cheap-mode window (2026-08-22 report, `~/proxy-usage-reports/report.md`) shows the local Qwen3 model busy only ~25-38% of the window. Root cause is structural,
  - Dispatched audits: use both cheap slots during Phase 2 deep analysis (mode-aware PARALLELISM) — The downtime worker pins `AUDIT_PHASE2_PARALLELISM=1` for dispatched audits (WL-0MSORQ1RG005DGUS) so an audit's Phase 2 child deep-analysis runs strictly sequentially (parent first, then one child at
  - Warn in wl export/import --help that export dumps the whole DB and import REPLACEs it — After a destructive `wl import` incident in the SorraAgents project (a work item was moved between projects via export/import and the source DB was replaced), the operator requested (2026-08-27, Table
  - Pane-title: ID survives truncation, drop 'triggered' — Amend the pane-title builders (WL-0MSJ4E8UA005KG9Y deliverable, landed in commit 66e4b8c8) so the work-item ID always survives the 60-char truncation and the prefixes become 'Downtime: ' / 'Manual: '
  - Herdr downtime dispatcher: allow blocked-status dispatch and remove the implement risk cap — As an operator, I want the Herdr downtime dispatcher to select work items that are
  - Re-sort no longer invalidates previously fresh audits via updatedAt churn — `batchUpdateSortIndices` in `packages/shared/src/persistent-store.ts:747-769` stamps `updatedAt = now` alongside `sortIndex` in a single SQL UPDATE:
  - Bot-created children must not silently demote completed/in_review parents — `demoteParentOnChildAdded` (`database.ts:1566-1585`, via `create.ts:252-286`, `update.ts:489`) rewinds any `completed`/`in_review` parent to `open`/`plan_complete` on child attach. The triage/test bot
  - Demotion must leave an audit-trail comment + queue surfaces must ignore mechanical churn — Two observability gaps make revivals confusing:
  - Completed/audited work items resurface as open (re-sort timestamp churn + parent demotion) — Multiple work items that were audited and completed (e.g. WL-0MTJ7IEI80055V2V with "Ready to close: Yes" 2026-09-03, WL-0MTK1ILM2009QYB2, WL-0MTRQT482001SNXC) reappear in the open/planning queues, loo
  - Client retry does not honor Retry-After (503 startup_ramp from llm-proxy) — The llm-proxy's **post-restart startup ramp** gate returns HTTP `503` with a

### Open critical items

  - RCA: Downtime dispatcher re-dispatches CRITICAL implement items (duplicate panes on one worktree/branch) — As a herdr operator, I want the downtime dispatcher to open at most one pane per work item per skill while a prior dispatch is still in-flight, so concurrent agents never share a worktree/branch or fi

### Additional focus items for today

  - No additional focus items scheduled.

### Regressions

  - No regressions — no items slipped back from completed.

### Blockers

  - No immediate blockers.