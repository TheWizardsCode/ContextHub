## Standup Report (21st September 2026)

### Yesterday I completed work on...

  - install-herdr.sh should reload herdr config after inserting keybinding — When install-herdr.sh appends the [[keys.command]] prefix+l -> worklog-selection-list.open-worklist block to ~/.config/herdr/config.toml, the running herdr server keeps its startup keybindings in memo
  - test skill: node_suite_commands() ignores detected project root (vacuous node suite for non-framework repos) — `node_suite_commands()` in `skills/test/scripts/run_tests.py` is called without `project_root` in `run_suite()` and `run_summary()`, causing node test commands to be built relative to the framework RE
  - Downtime dispatch: recover items stranded by marker-write-failed or spawn-failed (no permanent->in_progress stranding) — As an operator, I do not want a transient failure in the downtime dispatch pipeline to strand a work item in `in_progress` with no agent working it and no automatic recovery.
  - [downtime] Release stale dispatch success-markers when the item has not advanced — The downtime dispatch pipeline writes a durable *dispatched marker* to `.worklog/downtime-dispatches.log` **before** spawning the agent pane, to prevent duplicate dispatch. Marker readers (`dispatched
  - Herdr extension selection list: icons sometimes render as error (?) — Icons in the Herdr extension selection list are not being rendered correctly. They sometimes show as the "error" icon (small white circle with a ?). The user has not identified a pattern as to when th
  - Herdr mode-switch worker: modeSwitchEnabled defaults to false; cheap switch needs downtime-dispatcher idle trigger — The activity-gated proxy mode-switching feature (WL-0MSN3FWV5008KQE9) ships but never runs: `modeSwitchEnabled` defaults to `false` in `packages/herdr/src/settings.ts` (line 107) while the README docu
  - wl update --delete-reason on deleted items silently discards deleteReason — `wl update <id> --delete-reason "..."` on a deleted work item reports success but silently discards the `deleteReason` — it is never persisted to the SQLite database. The same bug affects `deletedBy`
  - [downtime] Critical items blocked by a filter are never escalated (critical override is dead code) — The dispatcher once had a *critical-first* tier: `getNextCriticalCandidate()` enumerated open critical items across **all** stages and dispatched the highest-priority one with its stage-appropriate sk
  - [test-failure] worklist StageFilter lists all stage options — failing test — Test name: worklist StageFilter lists all stage options
  - [downtime] Dispatch window can starve: Herdr head cap can exclude the only dispatchable item — `getHerdrListHead` calls `fetchNextItems(30)` → `selectWorkItems(merged, 30)` (`packages/herdr/src/fetcher.ts`, `packages/herdr/src/smart-selection.ts`). Mandatory items (all `priority=critical` + all
  - Dispatcher consumes Herdr selection list head (supersedes wl next unification) — As a producer, I want the downtime dispatcher to pick the first item shown in the Herdr list, so automated work follows the priority I already see.

### Open critical items

  - Completed/audited work items resurface as open (re-sort timestamp churn + parent demotion) — Multiple work items that were audited and completed (e.g. WL-0MTJ7IEI80055V2V with "Ready to close: Yes" 2026-09-03, WL-0MTK1ILM2009QYB2, WL-0MTRQT482001SNXC) reappear in the open/planning queues, loo

### Additional focus items for today

  - No additional focus items scheduled.

### Regressions

  - Scheduled prompts: add time-of-day dispatch (time window) to scheduled-prompts tier — As an operator, I want to add an optional `time` field (e.g. `06:05`) to a scheduled-prompt entry so the system dispatches it in the first available idle window **after** that time, so the standup run

### Blockers

  - No immediate blockers.