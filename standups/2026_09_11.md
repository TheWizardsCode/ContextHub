## Standup Report (11th September 2026)

### Yesterday I completed work on...

  - Add tests for typing-gated scheduler ticks — Test-first task for WL-0MTV67MZU003H7SH — Text inputs miss keypresses when extension is syncing.
  - Add isInputActive() guard to scheduler refresh and sync ticks — Implementation task for WL-0MTV67MZU003H7SH — Text inputs miss keypresses when extension is syncing.

### Open critical items

  - Text inputs miss keypresses when extension is syncing — pause sync while user is typing — As a user filling the command-parameter form (`FormState` — unknown `<identifier>` prompts), I want every keystroke to register so I can submit the command without retyping.

### Additional focus items for today

  - Dispatcher consumes Herdr selection list head (supersedes wl next unification) — As a producer, I want the downtime dispatcher to pick the first item shown in the Herdr list, so automated work follows the priority I already see.
  - Remove coordination entry expiry; validate eligibility at dispatch time — Downtime coordination entries expire via `pruneStaleEntries(coordinationDir, leaseTtlMs)` in `packages/herdr/src/downtime-worker.ts:dispatchFromCoordination` (`leaseTtlMs = DEFAULT_LEASE_TTL_SECONDS*1
  - Prevent top line info bar from scrolling off screen — As a Herdr user with a narrow pane and many items, I want the top `Work Items` info bar to stay visible at row 0 regardless of pane width or list content, so I don't lose the item count and system sta
  - Unify audit-freshness at-or-near logic across icon display and downtime dispatcher — As a producer who ran `/skill:audit`, I see the fresh audit icon (✅/❌/❓) immediately after the run, not the stale glyph.

### Regressions

  - No regressions — no items slipped back from completed.

### Blockers

  - No immediate blockers.