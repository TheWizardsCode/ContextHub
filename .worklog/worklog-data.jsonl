{"__worklog_sync__":{"kind":"delta","version":1}}
{"data":{"assignee":"","createdAt":"2026-08-07T02:56:12.564Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Implement version read + dimmed right-aligned header display\n\n## Summary\nRead the plugin version at runtime from `packages/herdr/package.json` and render it as a dimmed, right-aligned `v<version>` suffix in the work item selection list header, without disturbing the left-aligned title.\n\n## Acceptance Criteria\n- A runtime helper reads the plugin version from `packages/herdr/package.json` (JSON parse via `readFileSync`, following the `shortcut-config.ts` pattern); when the file is missing or unparseable it falls back gracefully (e.g. `'?'` or omit) without throwing.\n- The list-mode header renders `v<version>` right-aligned to `cols` (visible-width padding computed with ANSI codes stripped before padding), dimmed via `ANSI.dim`, matching the `[auto-refresh on]` treatment; the left-aligned `Work Items — N item(s)` title is unchanged.\n- The version is read at runtime from the manifest, not hardcoded in the renderer (stays in sync with the manifest version).\n- On narrow panes where the suffix would overlap the title, the suffix is dropped or truncated (guard on `cols`) — no layout breakage, no overflow beyond the pane width.\n- Full project test suite passes with the new changes, including the header version tests (F1).\n\n## Minimal Implementation\n- Add a small version-read helper in `packages/herdr/src/worklist.ts` (or a sibling module) using the `readFileSync`/`dirname` pattern from `shortcut-config.ts`, with a safe fallback.\n- In the list-mode header renderer (near line ~1585): compute the header's visible width, pad to `cols`, and append the dimmed `v<version>` suffix right-aligned; reuse `truncateLine`/visible-length logic; guard on `cols` for narrow panes.\n- Keep the renderer testable via the existing `readFile`-style injection seam where practical.\n\n## Dependencies\n- Blocked by: header version rendering tests (F1) — test-first ordering.\n\n## Deliverables\n- Updated `packages/herdr/src/worklist.ts`; version helper; no change to `packages/herdr/package.json` (version source only).","effort":"","id":"WL-0MSICS36B0072K9B","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSGKEBFU004T3N3","priority":"medium","risk":"","sortIndex":10300,"stage":"in_progress","status":"blocked","tags":[],"title":"Implement version read + dimmed right-aligned header display","updatedAt":"2026-08-30T00:41:27.694Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-07T02:56:16.029Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Document version display\n\n## Summary\nUpdate the Herdr plugin documentation to describe the dimmed, right-aligned plugin version shown in the work item selection list header.\n\n## Acceptance Criteria\n- `packages/herdr/README.md` documents that the work item selection list header shows the running plugin version (dimmed, right-aligned).\n- Documentation is consistent with the implemented behaviour (F2), including the fallback when the version is unavailable.\n- CHANGELOG.md is NOT modified (managed automatically by the ship skill release pipeline).\n\n## Minimal Implementation\n- Add a bullet under the Features section (and/or a short note in the relevant UI section) of `packages/herdr/README.md` describing the version display.\n\n## Dependencies\n- Blocked by: implementation (F2) — document the implemented behaviour, not the intended one.\n\n## Deliverables\n- Updated `packages/herdr/README.md`.","effort":"","id":"WL-0MSICS5UL0098443","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSGKEBFU004T3N3","priority":"medium","risk":"","sortIndex":10400,"stage":"intake_complete","status":"blocked","tags":[],"title":"Document version display","updatedAt":"2026-08-30T00:41:27.694Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-09T14:24:07.551Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Feature Request: --fields field-selection for wl list / wl search\n\n## Problem statement\n\nwl list and wl search return full work-item records (or compact snippets for search), with no way to request only the fields a caller needs. Full-record dumps are the root cause of token waste documented in SA-0MSLVQMKF000ESPZ (SorraAgents): bare wl list --json = 5.3 MB / 1,861 records; wl list --status completed = 4.9 MB; scripts (ship gates, triage, code-review) download tens-to-hundreds of KB of fields they never read (e.g. full descriptions) when they only need id, title, status, stage, priority.\n\n## User stories\n\n- As a release-gate script, I want wl list --stage in_review --fields id,title,status,stage,needsProducerReview --json so I stop downloading 4.9 MB of descriptions I discard.\n- As an agent running /skill:audit, I want a compact project-status query (fields id,title,status,stage,priority) so the worklog overview stays in the low-KB range.\n- As a triage helper, I want wl list --priority critical --tags test-failure --fields id,title,description --json so my match heuristics get exactly the fields they need.\n\n## Expected behaviour\n\n- New option on wl list and wl search: --fields comma-separated (e.g. id,title,status,stage,priority).\n- Output objects contain ONLY the requested fields (plus id always, for usability); unknown field names are rejected with a clear error listing valid fields.\n- wl list and wl search share the same field vocabulary: id, title, description, status, stage, priority, issueType, assignee, tags, createdAt, updatedAt, parentId, needsProducerReview, sortIndex.\n- --json output shape unchanged (same keys; omitted fields absent). Human output uses the same projection for concise display.\n- -f as shorthand is acceptable; must not clash with existing flags (-F/--format already taken).\n\n## Acceptance criteria\n\n1. wl list --fields id,title --json returns objects with only id/title; verified for filters and bare queries.\n2. wl search term --fields id,title,status --json returns compact objects with only those fields.\n3. Invalid field names produce a non-zero exit and a clear error listing valid fields.\n4. Output size reduction verified: wl list --stage in_review --fields id,title --json is smaller than the full-record equivalent (target: >=90% reduction for the completed-status case cited above).\n5. Existing output without --fields is unchanged (backward compatible); full test suite passes.\n\n## Plan\n\n### Child Work Items (dependency-ordered)\n\n1. **WL-0MT5L3BGA001MKOP** - Create pickFields() projection helper module\n2. **WL-0MT5L4MGL000Z43Y** - Write unit tests for pickFields projection helper (depends on 1)\n3. **WL-0MT5L55AQ002WTGQ** - Add --fields option to wl list (depends on 1, 2)\n4. **WL-0MT5L6G88006N4WD** - Add --fields option to wl search (depends on 1, 2)\n5. **WL-0MT5L6TLQ004NDLK** - Integration test: end-to-end --fields flag verification (depends on 3, 4)\n\n### Implementation Approach\n\n1. Create packages/shared/src/fields.ts with pickFields(item, fields?) helper\n2. Add fields?: string to ListOptions and SearchOptions in cli-types.ts\n3. Apply pickFields projection in list.ts before JSON output\n4. Apply pickFields projection in search.ts before JSON output\n5. Write unit tests and integration tests\n\n### Review Stages Summary\n\n1. Requirements & AC alignment: All 5 ACs map to user stories and expected behaviour. No gaps or contradictions.\n2. Completeness: Description has all required sections. Key files are valid.\n3. Sequencing & dependencies: Test items before implementation items. Dependencies properly set.\n4. Scope sizing: Small effort, Low risk - appropriate for scope.\n5. Acceptance & testability: All ACs are testable with clear pass/fail criteria.\n6. Polish & handoff: Plan is copy-pasteable and actionable.\n\n## Plan Changelog\n\n- 2026-08-23T09:09Z - Plan created with 5 child work items. Pre-check: Small effort, Low risk. All review stages passed.\n\n## Appendix\n\n- Q: Should --fields support nested field access (e.g. auditResult.readyToClose)? Answer (plan agent): No. The work item scope is flat field selection only. Nested access can be added later if needed. Final: not in scope.\n- Q: Should -f shorthand be supported? Answer (plan agent): Yes, but -f is not taken in either list or search. Final: -f can be added as shorthand.","effort":"Small","id":"WL-0MSLW8GHQ0092PJK","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"high","risk":"Low","sortIndex":100,"stage":"in_review","status":"in-progress","tags":["search","cli"],"title":"Add --fields option to wl list and wl search for field-selective output","updatedAt":"2026-08-30T00:41:27.694Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-11T18:02:30.101Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Intake Brief: Sync — Regression Guard\n\n## Problem Statement\n\nWhen `wl sync` merges remote state that is newer than local, it can silently import regressions: fields the local store had set to non-default values (e.g. `effort: 'Small'`, `status: completed`) get overwritten with defaults by a stale/divergent second store, as observed Aug 6–10, 2026 on the llm worklog ref.\n\n## Users\n\n- **Primary**: Operators of multi-store worklog setups — anyone running more than one `wl sync` instance against the same remote.\n- **Secondary**: Producers/reviewers whose audit results (status/stage transitions) are silently reverted by stale syncs.\n\n## Acceptance Criteria\n\n1. During merge, for each incoming remote item with a newer `updatedAt` than local, detect regressions where a field moves from a non-default value to its default on the fields that participate in the merge: `status`/`stage` (non-default → default, e.g. `completed/in_review` → `open/idea`), `priority` (non-default → default), and `assignee` (set → unset).\n\n   **`effort`/`risk` (producer resolution, 2026-08-23): stay PERMANENTLY EXCLUDED from the merge field lists** — ping-pong prevention by construction. A remote `effort: ''` / `risk: ''` never enters the merge; local non-default values are always kept. The guard additionally REPORTS any attempted effort/risk regression (remote non-empty → default) as a protected conflict (local kept, nothing imported) for observability, but no override path is needed for these two fields since they cannot be imported through the merge.\n2. On detecting a regression, `wl sync` refuses to import that item's regressed fields: reports the conflict (item id, field, old local value, new remote default value) and keeps the local value. An explicit override flag (`--accept-regressions` or equivalent) allows the operator to accept the regression.\n3. Genuine concurrent edits (both stores changed different non-default fields, or both changed the same field to different non-default values) still merge and re-timestamp exactly as today — this guard must NOT change legitimate conflict resolution.\n4. The regression guard applies only to the `import()`/merge path used by `wl sync` — it does NOT affect explicit user `wl update` commands, which remain authoritative.\n5. Unit tests cover: (a) newer remote with `effort: ''` against local `effort: 'Small'` → local kept (exclusion by construction), conflict REPORTED as protected, remote value NOT imported; (b) newer remote with `status: open` against local `completed` → refused, local kept, conflict reported; (c) both sides non-default differing → normal merge + re-timestamp (no regression path — guard must NOT alter legitimate conflict resolution); (d) override flag accepts the regression for merge-participating fields only; (e) `--dry-run` reports the same refusals.\n6. Manual verification: sync against the known commit `5fc880a` (effort/risk cleared) → regressions refused and reported; sync against `54b1375` (restored values) → no regression flags.\n\n## Constraints\n\n- Must not change behavior for legitimate concurrent edits — the guard is regression-only, not conflict-resolution.\n- Must be deterministic: dry-run and normal sync report the same refusals.\n- Does NOT prevent a second store from pushing — it prevents the primary store from absorbing regressive state.\n- Complements the identity gate (WL-0MSOYWWS4009HTCB) — the identity gate catches the writer; this guard catches the damage even if the writer's identity is unknown.\n\n## Existing State\n\nThe merge functions (`mergeSameTimestampItems`, `mergeDifferentTimestampItems` in `src/sync.ts`) already implement a non-default-preference heuristic: when one side has a non-default value and the other has a default, the non-default is preserved. The `isDefaultValue` helper in `src/sync/merge-utils.ts` classifies empty strings, `undefined`, and empty arrays as defaults.\n\nThe merge field arrays include `status`, `stage`, `priority`, and `assignee` — these fields **already** keep the local non-default value when the remote sends a default (via the `!localIsDefault && remoteIsDefault` branch). They do **not** produce an explicit regression report — the local value is silently kept.\n\nThe merge field arrays do **not** include `effort` or `risk` — these fields are never merged at all; the local value is always preserved regardless of the remote.\n\n**Contradiction — RESOLVED (producer decision, 2026-08-23)**: The 13-field merge list currently excludes `effort`/`risk` (code-confirmed), yet the original ACs listed them as regression targets. Producer directive: *\"The goal is to prevent ping-pong — do what you need to.\"* Resolution: **Reading A** — `effort`/`risk` remain excluded from the merge lists; adding them to the merge would re-introduce the ping-pong (the DB re-timestamp amplifier in `hasWorkItemChanged` already tracks these fields, so a single unguarded import becomes the newest record and propagates). The guard therefore targets the merge-participating fields (`status`/`stage`/`priority`/`assignee`), and `effort`/`risk` regressions are reported as protected (local kept). Effort/risk merge-participation is a non-goal.\n\n## Desired Change\n\nAdd a regression detection layer on top of the merge path that:\n- Identifies fields regressing from non-default → default in newer remote state, for the merge-participating fields: `status`/`stage`/`priority`/`assignee`\n- Reports conflicts with details (item id, field, old value, new value)\n- Refuses to import regressed fields (keeping local) unless overridden (`--accept-regressions`, merge-participating fields only)\n- Preserves all existing merge behavior for non-regression cases\n- Reports (without importing) attempted `effort`/`risk` regressions as protected — these fields remain excluded from the merge lists (non-goal: adding them back)\n\n## Risks & Assumptions\n\n| Risk | Impact | Mitigation |\n|------|--------|------------|\n| Regression detection logic breaks existing merge behavior | High | Unit tests must verify non-regression cases still merge correctly (AC3) |\n| Scope creep: \"regression\" definition might be too broad (e.g., should a valid transition like `in_progress → open` count as regression?) | Medium | Strict definition: only non-default → default transitions count |\n| Override flag (`--accept-regressions`) could be misused, allowing silent data loss | Medium | Require explicit flag; report what was accepted in output |\n| **DECIDED**: effort/risk must NOT be added to the merge list | — | Producer resolution 2026-08-23: adding them re-introduces ping-pong (DB re-timestamp amplifier `hasWorkItemChanged` tracks these fields; one unguarded import becomes the newest record) |\n| `isDefaultValue` may not correctly identify defaults for all fields (e.g., custom stages) | Low | Only built-in defaults (`open`, `idea`, `medium`, empty strings) are guarded; custom stages fall through to normal merge |\n| **Scope-creep risk**: The guard could be extended to \"merge strategies\" or general conflict-policy work — those are separate concerns. | Medium | Record as future work items; do not implement here |\n\n**Assumptions**:\n- The `isDefaultValue` function correctly classifies all default values for the fields in question\n- Regression detection can be cleanly layered on top of existing merge logic without restructuring\n- The operator will accept a new CLI flag for override\n\n## Key Files (predicted)\n\n- `src/sync.ts` — `mergeSameTimestampItems`, `mergeDifferentTimestampItems`: add regression detection alongside the existing merge logic (field lists UNCHANGED for `effort`/`risk` — exclusion is the ping-pong prevention)\n- `src/sync/merge-utils.ts` — `isDefaultValue`: may need no changes (already classifies empties as defaults)\n- `src/commands/sync.ts` — CLI flag for `--accept-regressions`, conflict reporting in output\n- `tests/sync-regression-guard.test.ts` — new test file for regression detection scenarios\n\n## Related Work\n\n| ID | Title | Relationship |\n|----|-------|-------------|\n| WL-0MSOYWWS4009HTCB | Sync: identity gate — refuse merge of foreign/empty-author commits | Related: same root cause incident (Aug 2026 llm worklog pollution); identity gate catches the writer, regression guard catches the damage |\n| WL-0MSORD6HC005QVZX | Sync: do not change updatedAt on whitespace-only changes | Completed; orthogonal concern |\n| WL-0MKVZ510K1XHJ7B3 | Sync & data integrity | Completed; foundational sync merge logic |\n| WL-0MSUEOAQO0081TJH | Wire identity gate into wl sync + config flag + sync-point persistence | Completed; identity gate now wired into sync |\n\n## Appendix: Clarifying Questions\n\n- **Q (auto-resolved by code inspection)**: The description says \"the 13-field merge list currently excludes effort/risk\" — the code confirms `effort` and `risk` are NOT in the merge field arrays. Does this mean: (a) the ACs about effort/risk are stale and should be removed, or (b) the description is outdated and effort/risk regressions need explicit detection and reporting? → **Answer (producer, 2026-08-23): \"The goal is to prevent ping-pong — do what you need to.\" Interpreted as Reading A: effort/risk stay excluded from the merge lists (prevention by construction); the guard targets merge-participating fields (status/stage/priority/assignee) and reports protected effort/risk conflict attempts without importing them. Adding effort/risk to the merge is a non-goal.** Source: interactive reply, this intake session (2026-08-23).\n- **Q**: What is the exact override flag name? The ACs mention `--accept-regressions` or equivalent. → **Flagged for producer confirmation.**\n## Related work (automated report)\n\n### Related work items\n- **WL-0MS8XLS0Z00759QI** – Pi TUI tools.ts: smart selection (helper copy + fetch merge + apply) (completed)\n- **WL-0MSVRMAWM007QNR5** – Downtime worker: parseLlamaStatus rejects numeric slot_id — every poll fail-closes to busy, zero dispatches (completed)\n- **WL-0MSHWHFAK005WKDB** – [test-failure] defaultSettings has default browseItemCount of 10 — failing test (completed)\n\n### Repository file matches\n- `scripts/audit_runner.py` — matched: ac3, accept, acceptance, accepted, accepts (+231 more)\n- `packages/herdr/README.md` — matched: ac3, accept, acceptance, accepted, accepts (+204 more)\n- `CLI.md` — matched: accept, accepts, add, adding, affect (+191 more)","effort":"Small","id":"WL-0MSOYWZTH003JFVD","issueType":"bug","needsProducerReview":false,"parentId":null,"priority":"high","risk":"Medium","sortIndex":200,"stage":"in_review","status":"in-progress","tags":["sync","data-integrity","multi-store"],"title":"Sync: regression guard — refuse import of default-value regressions from newer remote state","updatedAt":"2026-08-30T00:41:27.694Z"},"type":"workitem"}
{"data":{"assignee":"pi","createdAt":"2026-08-13T16:37:21.522Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Problem\n\nDuplicate work items are created by e2e tests running against the real worklog DB:\n- `tests/e2e/headless-tui.test.ts`: 'Test item for show' (265) + 'Test item for show JSON' (266) — 531 items created 08-01..08-07, no cleanup teardown. Still present in 3 stale worktrees.\n- `tests/e2e/agent-flow.test.ts`: 'Test E2E item from agent pipeline' (43) — created 05-21, all deleted.\n\nSync-clobber bug reverts soft-deletes (known in WL-0MSPZP7FE009YXPG), and `wl doctor prune` has never been run (778 soft-deleted items accumulate).\n\n## Acceptance Criteria\n\n- [ ] **AC1**: `tests/e2e/headless-tui.test.ts` has teardown that deletes created items (or the test is removed if no longer needed)\n- [ ] **AC2**: `tests/e2e/agent-flow.test.ts` has teardown that deletes created items\n- [ ] **AC3**: Stale worktrees (align-icons, grid-rebalance, wire-read-commands) are pruned or their copies of these tests are deleted\n- [ ] **AC4**: All 284 open 'Test item for show'/'…JSON' items are deleted and verified gone after sync\n- [ ] **AC5**: `wl doctor prune --days 0` removes all remaining soft-deleted items (778 total)\n- [ ] **AC6**: Post-fix `wl sync` shows no sync conflicts for these items (remote state matches)\n\n## Implementation Notes\n\n1. Add teardown to headless-tui test: capture created item IDs, call `wl delete <id>` in `afterEach`/`afterAll`\n2. Same for agent-flow test\n3. Check if stale worktrees are needed; if not, `wl doctor prune-worktree <dir>` or manual cleanup\n4. Re-delete open items, sync, verify remote\n5. Run `wl doctor prune --days 0` on both local and remote branches","effort":"","id":"WL-0MSRQR7CH002C4UH","issueType":"","needsProducerReview":false,"parentId":"WL-0MSHZ2JV0002D6EM","priority":"medium","risk":"","sortIndex":10500,"stage":"plan_complete","status":"blocked","tags":[],"title":"Fix: add test-teardown guard, clean up stale worktrees, and run prune","updatedAt":"2026-08-30T00:41:27.694Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T09:09:00.684Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Implement auto-downgrade of critical priority on terminal transitions\n\n## Goal\nWhen a work item with `priority=critical` transitions to a terminal state (status=completed or stage=in_review), automatically downgrade its priority to `high`.\n\n## Implementation Details\n\n### 1. Downgrade Logic\n\nAdd a helper function to detect and perform the auto-downgrade:\n- Location: `packages/shared/src/database.ts` alongside the existing `cascadePriorityDowngrade`\n- Function: `downgradeIfTerminal(item)` or inline check\n- Logic: if item's new state has `status=completed` or `stage=in_review` AND `priority=critical`, set `priority=high`\n\n### 2. Call Sites\n\nInvoke from terminal-transition paths:\n- `src/commands/update.ts` — after status/stage update, before response\n- `src/commands/close.ts` — after close logic, before response\n\nPrefer explicit call sites to avoid interfering with internal DB `update()` cascade calls.\n\n### 3. Reporting\n\nReport the downgrade in both output formats, mirroring the existing `downgradedChildren` pattern:\n- JSON output: add `downgradedItem` field (or similar) with `{id, priority}` \n- Human-readable: add a summary line like `Downgraded priority from critical to high`\n\n### 4. Constraints\n\n- Only affect `critical` → `high` downgrade\n- No change to other priorities (high/medium/low remain untouched)\n- Only trigger on terminal transitions: `--status completed`, `--stage in_review`, or `wl close`\n- Non-terminal transitions (e.g., `idea` → `plan_complete`) must NOT change priority\n\n### Key Files\n- `src/commands/update.ts` — terminal transition handling\n- `src/commands/close.ts` — close path\n- `packages/shared/src/database.ts` — helper function location\n- `src/validators/priority.ts` — priority constants","effort":"","id":"WL-0MT2Q9FUZ002MZU2","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSJM4EIV001A0V9","priority":"high","risk":"","sortIndex":10100,"stage":"intake_complete","status":"blocked","tags":[],"title":"Implement auto-downgrade of critical priority on terminal transitions","updatedAt":"2026-08-30T00:41:27.694Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-29T22:50:41.552Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Downtime dispatch stalls ~60 of every 62 minutes despite free slots — empty coordination file must not trigger the 1-hour no-candidate cooldown\n\n## Problem statement\n\nThe downtime worker in coordination mode (`dispatchFromCoordination` + `tick` in `packages/herdr/src/downtime-worker.ts`) dispatches at most ~1 item per ~62 minutes regardless of how many llama-proxy slots are free: all 3 slots sit idle for ~60 of every 62 minutes with no polls, no file activity, and no dispatch. The coordination check-in offers exactly ONE entry (the instance's most-important item); after a successful dispatch the leader removes that entry, leaving `downtime-coordination.json` with `entries: []`. ~60 s later the leader evaluates idle → `dispatchFromCoordination([], ...)` returns `{dispatched:false, reason:'no-candidate'}` → `tick()` treats this as a *genuine empty backlog* and enters the 60-minute no-candidate cooldown — even though the worklog still has dispatchable candidates. The cooldown short-circuit sits BEFORE the check-in in `tick()`, so the 30-minute check-in (the only re-offer mechanism) is blocked for the whole pause. Observed cadence (Tableau-Card-Engine, `CG`): 19:28:03Z → 20:30:19Z → 21:32:44Z → 22:35:28Z.\n\n## Users & user stories\n\n- Operators running herdr downtime dispatch against a worklog with available work (ContextHub, Tableau-Card-Engine).\n  - \"As an operator, I want free LLM slots to be used for dispatch whenever the worklog has dispatchable work, so downtime capacity isn't wasted for an hour at a time.\"\n  - \"As an operator, I want the coordination check-in to keep running during a no-candidate pause, so new work is picked up promptly rather than on a 60-minute cooldown timer.\"\n  - \"As an operator, I want the no-candidate pause to mean *genuinely nothing to do*, so I'm not left guessing whether dispatch is stuck or really idle.\"\n\n## Acceptance Criteria\n\n- [ ] AC1 (**empty file ≠ empty backlog**): A dispatch attempt against an empty coordination file must NOT enter the no-candidate cooldown while `computeMostImportantItem` (or an equivalent candidate query) still finds dispatchable work in the worklog; the cooldown is reserved for a genuinely empty backlog.\n- [ ] AC2 (**check-in not suppressed by the pause**): The 30-min coordination check-in must not be suppressed by the no-candidate cooldown (or the cooldown must not exceed the check-in cadence), so a re-offer can land during the pause and dispatch resumes promptly.\n- [ ] AC3 (**headless regression coverage**): Unit/headless tests prove that with N free slots, ≥2 dispatchable candidates, and a single-entry coordination file, the worker dispatches at least once per `min(noCandidateCooldownMs, 2 × checkInIntervalMs)` — not once per `noCandidateCooldownMs`. The test fails on current code and passes after the fix.\n- [ ] AC4 (**validation & docs**): All related documentation is updated to reflect the corrected cooldown semantics — code comments (the header contract at the top of `downtime-worker.ts`, ~line 59) and `docs/dev/downtime-dispatcher.md`.\n- [ ] AC5 (**full suite**): The full project test suite must pass with the new changes.\n\n## Constraints\n\n- The no-candidate cooldown must remain in place for a genuinely empty backlog (its original purpose, WL-0MSI7DQL10016QYX); `DOWNTIME_NO_CANDIDATE_COOLDOWN_FLOOR_MS` (60 s) floor stays.\n- Existing \"pause completely\" semantics (no proxy polling, no idle tracking, no dispatch during the pause) are preserved for the genuine-empty case; only the *entry condition* and the *check-in/cooldown ordering* change.\n- The fix lives in coordination mode (leader + shared coordination file); the legacy (non-coordination) tier chain is unaffected.\n- Implemented BEFORE the sibling lease fix (WL-0MTEZ5EK5002RXXD, Blocked-by this item) so that item builds against the final cooldown contract — both touch the same `tick()` region, serialized to avoid merge churn.\n\n## Existing state\n\n- `tick()` gates on `cooldownUntil` FIRST (lines ~2478–2490 of `downtime-worker.ts`): while paused, it returns before the leader block and the 30-min check-in.\n- `dispatchFromCoordination([], ...)` with an empty file returns `{dispatched:false, reason:'no-candidate'}` (line ~1558, `entries.length === 0`), which the caller maps to the no-candidate cooldown.\n- The check-in offers exactly one entry (`computeMostImportantItem` → `upsertEntry`), which the leader removes after a successful dispatch → transiently empty file every cycle.\n- Verified live: `dispatchFromCoordination([], opts)` → `no-candidate`; `evaluateIdle` → true (3/3 slots free); `computeMostImportantItem` finds CG-0MTC31LN3000UHDY while the file is empty and the worker paused.\n\n## Desired change\n\n- Enter the no-candidate cooldown only when the worklog itself is genuinely empty of dispatchable candidates (probe candidates before pausing), not merely when the coordination file is transiently empty.\n- Ensure a re-offer (check-in) can land during any pause — either by running the check-in before/independent of the cooldown gate, or by capping the pause at the check-in cadence.\n\n## Key Files\n\n- `packages/herdr/src/downtime-worker.ts` — `tick()` cooldown gate ordering (lines ~2478–2490, before leader block + check-in ~2492+); cooldown entry sites (~line 2689); `dispatchFromCoordination` (~1505); `computeMostImportantItem` (~1420); `runCoordinationCheckIn`; constants `DEFAULT_DOWNTIME_NO_CANDIDATE_COOLDOWN_MS` (60 min) and `DEFAULT_COORDINATION_CHECK_IN_MS` (30 min); header contract doc (~line 59).\n- `packages/herdr/src/coordination.ts` — `readCoordinationFile` / entry lifecycle used by `dispatchFromCoordination`.\n- `packages/herdr/src/downtime-worker.test.ts` — existing no-candidate/cooldown coverage (e.g. ~line 378, 433, 515); extend for AC3.\n- `packages/herdr/src/coordination-dispatch.test.ts` — coordination dispatch unit coverage.\n- `packages/herdr/src/integration-leader-dispatch.test.ts` — leader dispatch integration coverage (two-instance scenarios).\n- `packages/herdr/src/downtime-worker.fixtures.ts` — fixtures for headless tests (N slots, multiple candidates, single-entry file).\n- `docs/dev/downtime-dispatcher.md` — document corrected cooldown semantics (AC4).\n\n## Related work\n\n- WL-0MTEZ5EK5002RXXD — Leader lease expires during no-candidate cooldown (zombie leader double-dispatch risk). **Depends on this item** (`wl dep add WL-0MTEZ5EK5002RXXD WL-0MTEZ4XZJ006Y9U7`): same `tick()` cooldown region; implement this item first so the lease fix builds against the final cooldown contract.\n- WL-0MSI7DQL10016QYX — original no-candidate cooldown (\"60-min pause when no work found\"); the cooldown's legitimate purpose, preserved here.\n- WL-0MST3OJ8S0001ROL — Refactor Downtime Dispatcher: leader election with shared coordination file (parent of the coordination architecture).\n- WL-0MSXH9UT6008151F — shared coordination file module; WL-0MSXHA1B0005G3E5 — leader-only dispatch logic.\n- WL-0MSKUG2WW0058A7W — Evaluate downtime-worker config and silent-failure paths (context).\n\n## Risks & assumptions\n\n- **Semantics tension**: reordering the check-in relative to the cooldown gate must not cause dispatch activity during a genuine-empty pause (check-in writes the coordination file — file writes are coordination, not dispatch; acceptable, but the check-in must not spawn panes).\n- **Cooldown floor preserved**: the fix must not let the cooldown be bypassed entirely; `DOWNTIME_NO_CANDIDATE_COOLDOWN_FLOOR_MS` still applies to genuine-empty pauses.\n- **Probe cost**: probing `computeMostImportantItem` before entering the cooldown adds a `wl` CLI call per idle evaluation; bounded by `DOWNTIME_WL_TIMEOUT_MS` (10 s) and failure must fail closed to the existing three-strike rule, never a silent pause.\n- **Scope-creep guard**: tuning the check-in cadence itself, hardening the global single-dispatch invariant, or reworking the legacy tier chain are out of scope — record as follow-up work items, not this fix.\n- **Assumption**: sibling RXXD remains blocked-by this item; if unblocking is required sooner, RXXD still stands alone (its draft notes this).\n\n## Appendix: clarifying questions & answers\n\n- Q: \"Should this intake run an interview round?\" — Answer (operator): N/A — no interview needed. The seed description is fully specified: problem statement, code-verified root-cause chain (probes + file watches), live evidence, and three measurable ACs. Source: work item description (WL-0MTEZ4XZJ006Y9U7).\n- Q: \"Which direction should the dependency between this item and WL-0MTEZ5EK5002RXXD (lease expiry during cooldown) point?\" — Answer (operator): \"evaluate the easiest to manage and setup the dependency accordingly.\" Research: both bugs share the same root-cause region — the cooldown gate in `tick()` (lines ~2478–2490) short-circuits before the leader block (~2492) and the check-in. This item is the structural fix (redefines the cooldown contract); RXXD is a nested residual fix (lease refresh during the genuine-empty pause, which can still exceed the 5-min lease TTL even after this fix). Implementing this item first lets RXXD's tests build against the final cooldown contract; the reverse forces RXXD's tests to assert the current 60-min-cooldown semantics that this fix immediately changes. Serializing also avoids parallel-merge conflicts on the same `tick()` lines. Decision: **RXXD blocked-by this item** — recorded via `wl dep add WL-0MTEZ5EK5002RXXD WL-0MTEZ4XZJ006Y9U7` and Blocked-by/Blocks comments on both items. Consistent with the existing RXXD intake draft (`.worklog/tmp/intake-draft-leader-lease-cooldown-WL-0MTEZ5EK5002RXXD.md`).\n## Related work (automated report)\n\n### Related work items\n- **WL-0MT3PHW4I002SNOV** – Ensure only single audit active during downtime dispatch (completed)\n- **WL-0MLRFF0771A8NAVW** – TUI: prevent mouse click-through from dialogs to underlying widgets (completed)\n- **WL-0MT1TMAZJ002LJET** – ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required (open)\n\n### Repository file matches\n- `packages/herdr/README.md` — matched: 0mskug2ww0058a7w, 0mst3oj8s0001rol, ac2, ac3, ac4 (+281 more)\n- `CLI.md` — matched: add, adds, also, answer, applies (+190 more)\n- `CHANGELOG.md` — matched: 03z, 0mst3oj8s0001rol, ac1, ac3, activity (+185 more)\n","effort":"Extra Small","id":"WL-0MTEZ4XZJ006Y9U7","issueType":"bug","needsProducerReview":true,"parentId":null,"priority":"critical","risk":"Low","sortIndex":100,"stage":"in_review","status":"completed","tags":[],"title":"Downtime dispatch stalls ~60 of every 62 minutes despite free slots (empty coordination file → 1h no-candidate cooldown)","updatedAt":"2026-08-30T00:41:28.035Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-29T22:51:03.030Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Fix zombie leader: lease must survive the no-candidate cooldown and leadership must self-heal (no double dispatch)\n\n## Problem statement\n\nA downtime leader's 5-minute lease (`downtime-leader-lease.json`, TTL 300 s, `packages/herdr/src/leader-election.ts`) silently dies during the **1-hour no-candidate cooldown** (see WL-0MTEZ4XZJ006Y9U7) and can never be renewed by that instance again. The worker nevertheless keeps checking in and dispatching via its cached `leaderState` — a **zombie leader with an expired lease** — so any second herdr instance pointing at the same coordination dir can win a fresh election and dispatch **concurrently** with the zombie, double-dispatching coordination entries.\n\n## Users & user stories\n\n- Operators running multiple herdr instances against one worklog (e.g. ContextHub + Tableau-Card-Engine).\n  - \"As an operator, I want at most one herdr instance to dispatch at any moment, even after a no-candidate pause, so coordination entries are never processed twice.\"\n  - \"As an operator, I want a paused-but-alive leader to retain its leadership claim, so dispatch resumes without a leadership fight.\"\n\n## Acceptance Criteria\n\n- [ ] AC1 (**lease refresh during cooldown**): While the worker is paused in the no-candidate cooldown, an alive leader's lease must not expire. Implement via (a) continued periodic lease refresh during the pause, and/or (b) immediate lease renewal on cooldown exit before any leadership/dispatch decision. After the fix, an alive leader never holds an expired lease, and `downtime-leader-lease.json` mtime/content advance through the cooldown.\n- [ ] AC2 (**self-heal + re-derived leader state**): `refreshLease()` must renew an owned-but-expired lease instead of no-oping, and `leaderState` must be re-derived each tick (or at least on cooldown exit) rather than read from a stale cache, so an expired lease triggers a new election instead of zombie dispatch.\n- [ ] AC3 (**no double dispatch — regression test**): While non-leader with a valid lease held by another live instance, the worker stays silent (existing behavior preserved). Add a test — unit and/or in `integration-leader-dispatch.test.ts` — proving two instances pointing at one coordination dir can **never both dispatch simultaneously**, including the zombie scenario: leader enters cooldown, lease expires mid-cooldown, a second instance wins election, and the zombie must not dispatch (re-election/takeover or silent non-leader), while exactly one instance dispatches at most.\n- [ ] AC4 (**validation & docs**): The new regression test fails on the current code (reproduces the bug) and passes after the fix; the full project test suite passes with the new changes. All related documentation is updated — code comments, `docs/dev/downtime-dispatcher.md`, and any relevant wiki/docs-site entries (per global AGENTS.md).\n\n## Risks & assumptions\n\n- **Semantics tension**: refreshing the lease during the cooldown must not violate the \"pause completely\" contract (no proxy polling, no no idle tracking, no dispatch during the pause). Lease maintenance is coordination-only, not dispatch activity — if this proves unacceptable, the cooldown-exit renewal (`(b)`) alone satisfies AC1.\n- **Takeover race**: another instance may win the lease during our pause; on cooldown exit the re-derived `leaderState` must yield to that lease (stay silent) rather than fight it — AC3 covers this.\n- **Scope-creep guard**: related opportunities (e.g. tuning `MIN_VALID_LEASE_TTL_MS`, hardening the global single-dispatch invariant beyond the cooldown case) are recorded as follow-up work items, not scope of this fix.\n- **Assumption**: the sibling cooldown fix (WL-0MTEZ4XZJ006Y9U7) lands first and stabilizes the cooldown block this fix builds on (Blocked-by relationship). If unblocking is required sooner, this fix still stands alone.\n\n## Constraints\n\n- Existing \"pause completely\" semantics of the cooldown (no proxy polling, no idle tracking, no dispatch during the pause) are preserved where possible; lease maintenance during the pause must not dispatch anything.\n- Non-leader instances with a valid lease held by another live instance must remain silent (AC3).\n- Fail-safe behavior preserved: a missing/unreadable lease file ⇒ treated as \"not leader\".\n- CAS claim mitigation for the same item stays in place, but must not be the only guard (different items can be double-claimed by zombie + new leader, exceeding slot budgets).\n\n## Existing state\n\n- `tick()` in `packages/herdr/src/downtime-worker.ts` short-circuits on `cooldownUntil` (lines ~2485–2493) **before** the leader block, so `refreshLease()` is never called for the full `noCandidateCooldownMs` (60 min default) — far longer than the lease TTL (5 min).\n- `leaderState` is cached once at worker creation (line ~2413) and only re-derived in the takeover branch (line ~2526), which is unreachable while `leaderState` is true.\n- `refreshLease()` in `leader-election.ts` (line ~299) guards with `if (!this.isLeader()) return`; `isLeader()` requires `isLeaseValid(lease)` (`remaining > MIN_VALID_LEASE_TTL_MS`, 10 s). An expired lease ⇒ `isLeader()` false ⇒ `refreshLease()` silently no-ops ⇒ the lease can never be renewed by this instance.\n- Competing instance path (`detectStaleLeader` → `cleanupStaleElection` → `attemptElection`) elects the second instance while the zombie keeps dispatching.\n- Observed evidence: Tableau-Card-Engine lease frozen since `2026-08-27T18:01:09Z` while dispatches continued >48 h later; ContextHub lease (`2a3f2a88…`) will hit the same state at its first cooldown.\n- Existing integration tests (`integration-leader-dispatch.test.ts`) cover two-instance election, lease expiry + re-election of a *dead* leader, and re-queue — but **not** the zombie-in-cooldown case (live leader whose lease expired during pause).\n\n## Desired change\n\nFix direction (operator-confirmed): **belt-and-braces** — both keep the lease alive through the cooldown (or renew on exit) **and** make leadership self-healing/re-derived so an expired lease can never leave this instance dispatching as a zombie:\n1. Ensure an alive leader's lease cannot expire during the no-candidate cooldown (refresh during pause or renew on exit — before any new dispatch decision).\n2. Make `refreshLease()` self-heal an owned-but-expired lease, and re-derive `leaderState` on cooldown exit (and/or each tick) so an expired lease routes into the takeover/re-election path instead of zombie dispatch.\n3. Prove single-dispatch invariant with a regression test covering the zombie case.\n\n## Key Files:\n\n- `packages/herdr/src/downtime-worker.ts` — cooldown gate + leader/refresh block in `tick()` (~lines 2413, 2474–2540); add lease maintenance during/on-exit of cooldown + re-derive leadership on cooldown exit.\n- `packages/herdr/src/leader-election.ts` — `refreshLease()` (~line 299) self-heal for owned-but-expired lease; `isLeader()`/`isLeaseValid` (~lines 245, 290) contract.\n- `packages/herdr/src/integration-leader-dispatch.test.ts` — add zombie dual-instance regression test (leader cooldown → lease expiry → second instance election → assert single dispatch).\n- `packages/herdr/src/downtime-worker.test.ts` — unit tests for cooldown lease maintenance, cooldown-exit leadership re-derivation, and self-healing refresh.\n- `docs/dev/downtime-dispatcher.md` (+ `docs/ARCHITECTURE.md` if it documents the cooldown/lease interaction) — update documentation to reflect the new lease-maintenance-during-cooldown and self-healing behavior.\n\n## Related work\n\n- WL-0MTEZ4XZJ006Y9U7 — **Blocked-by**: \"Downtime dispatch stalls ~60 of every 62 minutes despite free slots\" — the 1-hour no-candidate cooldown that triggers this bug; its fix changes the cooldown semantics this fix builds on.\n- WL-0MSXH3UF5000HJUS — completed: leader election module with 5-minute lease management (module whose contract is violated).\n- WL-0MST3OJ8S0001ROL — completed: refactor introducing `leaderState` caching + coordination check-in.\n- WL-0MSXHAKDT005H6VQ — completed: integration tests & verification (source of `integration-leader-dispatch.test.ts`).\n- WL-0MT76H3Z900908TV — completed: leader-election / shared coordination file architecture docs.\n\n## Appendix: Clarifying questions & answers\n\n- Q: \"AC1 and AC2 each offer alternatives. Which fix do you prefer: (a) keep the lease alive through cooldown, (b) re-derive `leaderState` on cooldown exit, (c) both?\" — Answer (operator): **c — both (belt-and-braces)**. Source: interactive reply.\n- Q: \"Is the double-dispatch risk *observed* or *latent*? (Affects whether AC3 asserts on a live scenario or a simulated one.)\" — Answer (operator): \"your call — err on the side of caution\". Decision: treat as **latent but critical** risk; AC3 proves the invariant with a simulated zombie test (fake timers, two in-memory instances), so the guarantee holds whether or not it has fired in production. Research: `integration-leader-dispatch.test.ts` shows only dead-leader re-election coverage exists (lines 135, ~187), no live-leader zombie case.\n- Q: \"Should this fix be standalone or sequenced with the sibling cooldown item (WL-0MTEZ4XZJ006Y9U7)?\" — Answer (operator): \"your call — mark one blocked on the other, whichever is most easily managed\". Decision: mark this item **Blocked-by WL-0MTEZ4XZJ006Y9U7** — both fixes touch the same `tick()` cooldown region; landing the cooldown fix first stabilizes that shared code so this lease fix + zombie test land with minimal rework. Registration: `wl comment add WL-0MTEZ5EK5002RXXD --comment \"Blocked-by:WL-0MTEZ4XZJ006Y9U7\"`.","effort":"Small","id":"WL-0MTEZ5EK5002RXXD","issueType":"bug","needsProducerReview":false,"parentId":null,"priority":"critical","risk":"High","sortIndex":300,"stage":"intake_complete","status":"open","tags":[],"title":"Leader lease expires during no-candidate cooldown and can never refresh — zombie leader double-dispatch risk","updatedAt":"2026-08-30T00:41:27.694Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-29T23:30:51.698Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Single machine-wide downtime leader across all worklogs (replace per-worklog elections)\n\n## Problem statement\n\nLeadership today is **per-worklog** (`coordinationDir: join(targetCwd, '.worklog')` in `index.ts`; per-entry `directory: coordinator.cwd` in `runCoordinationCheckIn`). Every open project tab therefore runs its own election, its own lease/lock, and its own coordination file. The parent refactor WL-0MST3OJ8S0001ROL and its docs page (`docs/dev/downtime-dispatcher.md` §Multi-worklog support) describe a leader that \"compares offers across directories\" — but no instance ever sees another worklog's offers, so that behaviour does not exist in practice. Consequences:\n\n1. **N leaders, not 1** — TCE, ContextHub, and SorraAgents each have an independent leader (`22a6e7c7…`, `2a3f2a88…`, `97eb89d8…` observed), each polling the same shared llama-proxy.\n2. **No cross-project slot budget** — all leaders see the same \"3 free slots\" in an idle window and can each dispatch within seconds of each other, oversubscribing the shared LLM (2-slot audit dispatches from two projects at once).\n3. **Per-project ~1 item/hour cadence** — each project's leader runs the same single-entry→empty-file→no-candidate→1h-cooldown cycle (WL-0MTEZ4XZJ006Y9U7), so total throughput across projects is low and uneven.\n4. **Zombie leaders compound this** — expired leases never refresh (WL-0MTEZ5EK5002RXXD); a machine-wide design with one lease per machine removes most of the multi-leader divergence surface.\n\n## Goal\n\nA single machine-wide downtime coordinator shared by every herdr instance on the host, regardless of which worklog its tab is open on: one coordination dir, one leader election, one lease, one dispatch queue containing per-worklog offers, and one slot budget against the shared proxy. This is the \"multi-worklog leader\" the architecture doc already implies.\n\n## Proposed design\n\n- **Machine-scoped coordination dir** — e.g. `~/.herdr/downtime/` (or `HERDR_COORDINATION_DIR` env override), containing `coordination.json`, `leader-lease.json`, `leader.lock`, `dispatches.log`. All instances read/write the same files regardless of project.\n- **Per-worklog offers** — each instance contributes `{instanceId, workItemId, worklogRoot, assignedAt, lastUpdated}` (add `worklogRoot`; today's `directory` field is already the worklog root). Stale-entry pruning and 30-min check-in cadence carry over per instance.\n- **One election, one lease** — instances elect a single machine leader (file lock + lease, reuse `leader-election.ts`); the lease must survive no-candidate cooldowns (depends on WL-0MTEZ5EK5002RXXD fix).\n- **Cross-directory dispatch** — the leader orders offers by the existing tier priority (audit → implement → plan → intake) *across* worklog roots, and spawns panes into each entry's `worklogRoot` (viaroot `wl` invocation as today).\n- **Machine slot budget** — the leader enforces total concurrently-attributed panes/slots against the shared proxy status, so two projects cannot oversubscribe together. Coordinate with WL-0MT50LKAK001EF5Q (bounded concurrent dispatches within a project).\n- **Migration/fallback** — while legacy per-worklog files still exist, prefer the machine dir; document how to retire the old `.worklog/downtime-*` files. Must not double-dispatch during the transition (a TCE instance with an old TCE file must not ALSO join the machine file with a different instanceId).\n\n## Acceptance Criteria\n\n- [ ] AC1: One coordination dir shared by all instances on the host; a second instance opened on a *different* worklog joins the same file (contains entries with different `worklogRoot`s).\n- [ ] AC2: Exactly one leader machine-wide: with N instances across P worklogs, only one holds a valid lease and no two instances dispatch in the same tick window (tested for the zombie case from WL-0MTEZ5EK5002RXXD).\n- [ ] AC3: Leader dispatches across directories by tier priority — e.g. a ContextHub audit offer is picked before a TCE intake offer, and the pane spawns in the ContextHub root.\n- [ ] AC4: Global slot budget: with 3 proxy slots and 2 projects offering work, total concurrently dispatched/requesting work never exceeds the budget (no cross-project oversubscription).\n- [ ] AC5: Migration is safe — existing per-worklog coordination files stop being written once the machine dir is authoritative; no double-dispatch during transition; docs (`docs/dev/downtime-dispatcher.md`) updated to match actual behaviour.\n- [ ] AC6: Full herdr test suite + `npm run build` green; headless tests prove the AC1–AC4 invariants.\n\n## Notes\n\n- Builds on / must land after the two critical fixes: WL-0MTEZ4XZJ006Y9U7 (cooldown stall) and WL-0MTEZ5EK5002RXXD (zombie lease).\n- Supersedes the un-implemented \"multi-worklog\" paragraph of `docs/dev/downtime-dispatcher.md`; that doc must be corrected rather than describing aspirational behaviour.\n- v1 remains single-machine; multi-machine (flock/NFS) stays out of scope.\n","effort":"","id":"WL-0MTF0KLO10043YAN","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"high","risk":"","sortIndex":10200,"stage":"idea","status":"blocked","tags":["herdr","downtime","leader-election"],"title":"Single machine-wide downtime leader across all worklogs (replace per-worklog elections)","updatedAt":"2026-08-30T00:41:27.694Z"},"type":"workitem"}
{"data":{"author":"implement","comment":"Implementation complete.\n- Commit: 77c97412\n- Branch: wl-WL-0MTEZ4XZJ006Y9U7-downtime-dispatch-stalls-60-of-every-62\n- Worktree: /home/rgardler/projects/ContextHub/.worklog/worktrees/wl-WL-0MTEZ4XZJ006Y9U7-downtime-dispatch-stalls-60-of-every-62","createdAt":"2026-08-30T00:41:28.034Z","id":"WL-C0MTF33EG2003X53W","references":[],"workItemId":"WL-0MTEZ4XZJ006Y9U7"},"type":"comment"}
