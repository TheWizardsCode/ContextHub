{"__worklog_sync__":{"kind":"delta","version":1}}
{"data":{"assignee":"Map","createdAt":"2026-08-01T16:19:44.289Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Intake Brief — Incremental sync: only export/push changed work items instead of full JSONL (WL-0MSAKUBKW006FN8Q)\n\n## Problem statement\n\nEvery `wl sync` fully re-exports the entire SQLite database to JSONL (**11.6 MB, 2133 work items, 7494 comments**) and re-pushes the whole file to `refs/worklog/data` via a temp worktree — even when only a handful of items changed. This full-rewrite-per-sync makes syncs take 5–19 s (measured) and is the root cause of the pane timeout issue in WL-0MSAKM838006RZNR. `docs/ARCHITECTURE.md` already lists \"Incremental sync: Only sync changed items\" as a planned enhancement.\n\n## Users\n\n- **Anyone running `wl sync`** (agents, operators, TUI panes) on large worklog datasets — syncs become faster and less disruptive, and the Herdr 10 s timeout problem (WL-0MSAKM838006RZNR) stops recurring without needing constant timeout tuning.\n- **Teams sharing a canonical worklog ref** — fewer full-file rewrites means less churn on the shared ref and faster pulls/pushes.\n\nUser story: *As an operator, when only a few work items changed since my last sync, I want `wl sync` to export and push only those changes, so a typical sync takes seconds instead of 10+ seconds and no longer trips client timeouts.*\n\n## Acceptance Criteria\n\n- [ ] After an initial full sync, a sync that changes a small set of items (e.g., ≤ 5 items/comments) performs the local export and git push without rewriting the full 11.6 MB JSONL file (measured: local export bytes written and `git add` payload are proportional to the changed records, not the dataset size).\n- [ ] Typical incremental sync completes in ≤ 3 s on the current dataset (2133 items / 7494 comments) for small change sets (baseline today: full sync ~5.8 s local, 5–19 s with network variance).\n- [ ] Correctness is preserved: after an incremental push and a subsequent full pull, local and remote data converge (same items/comments/edges/audits as today, verified by the existing sync test suite + a convergence test that interleaves incremental and full syncs).\n- [ ] Deletions propagate: soft-deleted items (`status: 'deleted'`) are included in deltas so remote copies converge.\n- [ ] Full-snapshot fallback works: first sync, sync after an interrupted/corrupt delta chain, and any sync where the delta cannot be applied cleanly fall back to the current full export/merge path with no data loss.\n- [ ] Backward compatibility: the shared `refs/worklog/data` ref remains readable by existing clients (older `wl` versions, Herdr, Pi TUI extension); any format change is additive and documented, or gated behind a sync-state marker with automatic fallback.\n- [ ] Unit/integration tests added for the dirty-tracking, delta export/merge, deletion propagation, and fallback paths; full project test suite passes.\n- [ ] All related documentation is updated to reflect the changes, including code comments, README, `docs/ARCHITECTURE.md`, and any relevant wiki or docs site entries.\n\n> **Note:** CHANGELOG.md is **excluded**; it is managed automatically by the ship skill's release pipeline.\n\n## Constraints\n\n- The shared ref and existing readers must not break: any format change requires backward compatibility or a documented migration with automatic full-snapshot fallback.\n- Do not regress the concurrency protections: the file lock, single-flight guard, and `--if-idle` skip semantics (WL-0MSAB7ZUC004SK7E) must continue to work with the incremental path.\n- Keep the merge semantics: field-level conflict resolution by ID + timestamp (`mergeWorkItems`, `mergeComments`, `mergeDependencyEdges`, `mergeAuditResults` in `src/sync.ts`) is the correctness backbone — deltas must feed through the same resolution logic.\n- Note: git already delta-compresses the network payload of a push, so the main wins are local (full export write, temp-worktree file copy, `git add` hashing/compressing of 11.6 MB, pull-side `git show` + parse). The design should target those costs.\n- This is a design-heavy optimization: the implementing agent should pick an approach during planning (options below); the ACs are behavior-level.\n\n## Existing state\n\n- `packages/shared/src/database.ts` — `exportForSync()` exports **all** work items, comments, dependency edges, and audit results via `jsonlSvc.exportToJsonlAsync()` to the JSONL path; `deleteLocalJsonl()` removes it after a successful push (ephemeral pattern).\n- `src/jsonl.ts` — `buildJsonlContent()` emits a flat, sorted set of records (`type: workitem | comment | audit_result`) plus dependencies derived from edges. `importFromJsonlContent()` parses the same format. No version/header metadata; a delta file of the same record types is a valid subset of the format.\n- `src/commands/sync.ts` / `src/sync.ts` — pull path: `git fetch` + streamed `git show` of the remote JSONL (`execGitCaptureStdout`), parse, then by-ID merge (`mergeWorkItems`/`mergeComments`/`mergeDependencyEdges`/`mergeAuditResults`); push path: `gitPushDataFileToBranch` builds a temp worktree, copies the full JSONL, commits, pushes `HEAD:refs/worklog/data`.\n- Deletions are soft (`status: 'deleted'`), so records persist in the JSONL — no hard-delete tombstone format is needed for deltas.\n- `docs/ARCHITECTURE.md` §\"Future Considerations\" lists: (1) Incremental sync: only sync changed items, (2) Background sync, (3) Compression, (4) Delta encoding.\n- No per-item dirty tracking exists today; there is a `last-sync-time` file written after successful syncs (`src/commands/sync.ts`).\n\n## Desired change (approach options — decide in planning)\n\n1. **Dirty tracking + delta export (preferred direction):** add per-record change tracking (e.g., an `updatedAt` threshold from the previous successful export, or a dirty set in SQLite). On sync, export only records changed since the last export to the JSONL and push that delta; readers (this codebase only — Herdr and the Pi TUI extension call the `wl` CLI, not the JSONL directly) apply deltas over the last full snapshot. Periodic full snapshots (e.g., every N syncs or when the delta log exceeds a size threshold) bound replay cost; automatic full-snapshot fallback on first sync / mismatch / corruption.\n2. **Batched full rewrites:** keep the full-file format but only export+push when accumulated changes cross a threshold (fewer, bigger syncs). Simplest, backward-compatible, but does not reduce per-sync cost when a push happens — lower value than option 1; acceptable if option 1 proves too invasive.\n3. **Sharded JSONL:** split records across shard files on the ref so only changed shards are rewritten. Compatible but adds ref/file management complexity.\n\nRecommended: option 1 with a full-snapshot fallback, keeping the by-ID merge as the correctness backbone.\n\n## Key Files (predicted)\n\n- `packages/shared/src/database.ts` — dirty-tracking state, `exportForSync()` changed to delta-aware export (full vs. delta).\n- `src/jsonl.ts` — optional record header/version metadata, delta assembly (unchanged record types), full-snapshot fallback support.\n- `src/commands/sync.ts` — sync orchestration: decide full vs. incremental, apply deltas on the pull side, schedule periodic full snapshots.\n- `src/sync.ts` — merge entry points to accept delta records and detect when a full snapshot is required (missing base, chain mismatch).\n- `tests/` — new tests for dirty tracking, delta export/merge, deletion propagation, convergence, fallback.\n- `docs/ARCHITECTURE.md` — update the \"Incremental sync\" future-consideration entry to reflect the implemented design.\n\n## Related work\n\n- **wl sync timeouts: 10s Herdr auto-sync safety timeout kills legitimate slow syncs (WL-0MSAKM838006RZNR, open, intake_complete)** — the item this was discovered from (`discovered-from:WL-0MSAKM838006RZNR`); its ACs include making syncs fast enough not to trip client timeouts.\n- **wl sync lock storm: 60+ stuck sync processes (WL-0MSAB7ZUC004SK7E, completed)** — concurrency guards (`--if-idle`, single-flight, file lock) that must keep working.\n- **Add exponential back-off to file lock retry (WL-0MM0BT1FA0X23LTN, completed)** — lock contention behavior relevant to sync timing.\n- **Remove autoExport: SQLite as single source of truth with ephemeral JSONL (WL-0MN53C1BZ17WJRBR, completed)** — established the ephemeral JSONL pattern this item extends.\n- **docs/ARCHITECTURE.md** — \"Future Considerations\" § lists incremental sync as planned.\n\n## Risks & assumptions\n\n- **Assumption:** Herdr and the Pi TUI extension read worklog data via the `wl` CLI (verified: they invoke `wl` subprocesses), so only the worklog codebase itself reads the JSONL — a delta format change is tractable if older `wl` versions on other machines are considered.\n- **Risk:** incremental formats can silently diverge (missed updates, orphaned records, replay gaps). Mitigation: by-ID merge remains the resolution backbone, deltas feed through it, full-snapshot fallback on any uncertainty, and a convergence test that interleaves incremental and full syncs.\n- **Risk:** dirty tracking can miss changes if writes bypass the tracking layer (direct DB writes, imports, migrations). Mitigation: track at the export boundary using `updatedAt` thresholds rather than write interception, and treat \"no known baseline\" as full export.\n- **Risk:** periodic full snapshots add occasional slow syncs; threshold tuning (count/time/size) needed. Keep the threshold conservative.\n- **Scope-creep guard:** background async sync, JSONL compression, and delta encoding of the git transport are separate items listed in `docs/ARCHITECTURE.md`; record as linked work items rather than folding into this one.\n\n## Appendix: Clarifying questions & answers\n\n- Q: \"Should we create the incremental sync follow-up work item?\" — Answer (operator): \"yes create that\". Source: interactive reply following the WL-0MSAKM838006RZNR intake.\n- OPEN QUESTION (for planning): choose between approach options 1 (delta export), 2 (batched full rewrites), and 3 (sharded JSONL) — brief recommends option 1 with full-snapshot fallback; ACs are behavior-level and approach-agnostic.\n- OPEN QUESTION (for planning): acceptable full-snapshot cadence (every N syncs / time-based / delta-size threshold) — to be determined during planning; ACs only require that typical incremental syncs are small and fast.","effort":"Medium","id":"WL-0MSAKUBKW006FN8Q","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"low","risk":"Low","sortIndex":3700,"stage":"in_review","status":"in-progress","tags":[],"title":"Incremental sync: only export/push changed work items instead of full JSONL","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-05T14:05:59.933Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Hybrid Pi Dispatch: Pane When in a Herdr Session, Headless Fallback Otherwise\n\n## Problem statement\n\nThe evaluation of Pi subprocess mechanisms (discovered-from:WL-0MSG4VSP10020NL7) found that /skill:audit is dispatched two ways: user-initiated audits open a visible herdr pi pane via send-to-pi.sh, while automated contexts (heartbeat, cron, CI, ssh) must run headless via `pi -p --mode json`. Currently the choice of mechanism is hard-wired per caller and there is no shared way to get a visible pane when a herdr session exists and fall back to a headless subprocess when it does not. The podcast editor (open_source_llm/experimental/herdr-podcast-editor) already implements exactly this hybrid pattern via a HERDR_ENV check; it is a proven precedent to generalise.\n\n## Users\n\n- **Producers / operators** — want agent work to be visible whenever a herdr session is present, even when the invocation originates from a script or automated context.\n- **Skills authors / plugin authors** — want a single reusable dispatch primitive instead of maintaining two mechanisms.\n\nExample user stories:\n\n- \"As a producer, when an audit is triggered from a context that has a herdr session available, I want it to open in a visible pi pane so I can watch it.\"\n- \"As a producer, when no herdr session is available, I want the same invocation to fall back to headless execution so the work still happens.\"\n- \"As a plugin author, I want to dispatch a skill to pi once and have it choose pane-vs-headless automatically.\"\n\n## Expected behaviour\n\n- A shared dispatch helper determines at runtime whether a herdr session/pane environment is available (e.g. HERDR_ENV or herdr CLI/socket presence, mirroring the podcast editor).\n- When available: dispatch via send-to-pi.sh (visible pane).\n- When unavailable: fall back to `pi -p --mode json` headless execution with the same command.\n- Existing callers keep their behaviour; new callers (e.g. heartbeat audits, user-initiated audits outside the worklist) opt in.\n\n## Suggested implementation approach\n\n- Add the helper to the shared herdr scripts (packages/herdr/shared/) and/or a small skill-level utility so both herdr code and skills can use it.\n- Model the fallback on the podcast editor's send-to-pi.sh wrapper + direct-subprocess branch.\n- Wire the heartbeat audit dispatch to use the hybrid helper (pane when herdr present, headless otherwise), which also delivers visibility (see WL-0MSG5THFC004WJEI).\n\n## Acceptance criteria\n\n1. A shared dispatch helper exists that selects pane vs headless at runtime based on herdr availability.\n2. With a herdr environment present, dispatch opens a visible pi pane running the given command.\n3. Without a herdr environment, dispatch runs the command headless via `pi -p --mode json` and returns programmatic output.\n4. At least one existing caller (e.g. heartbeat audit dispatch) is converted to use the helper without regressing its current behaviour.\n5. Tests cover both branches (herdr present / absent).\n6. All related documentation is updated to reflect the changes, including code comments, README, and any relevant docs entries.\n7. Full project test suite must pass with the new changes.","effort":"","id":"WL-0MSG5TQM5003ZL8L","issueType":"task","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":3800,"stage":"idea","status":"open","tags":[],"title":"Hybrid pi dispatch: pane when in a herdr session, headless fallback otherwise","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-05T15:34:01.580Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Herdr selection list: no indication of items below the fold\n\n## Problem statement\n\nWhen the herdr worklist selection list (`createListRenderer` in `packages/herdr/src/worklist.ts`) has more work items than fit in the visible list area, the renderer silently trims the visible window to fit the pane height. There is no visual cue (scroll marker, count, ellipsis) that additional items exist below the fold, so users cannot tell the list is truncated and may miss items.\n\n## Users\n\nAnyone using the herdr worklist pane (`worklog-selection-list` plug) to browse/select work items, in particular with more items than fit on screen (the default list of 20+ items on smaller panes, or with the metadata panel reserving 20–40% of the height).\n\nUser story: *\"As a herdr user, when the worklist pane shows more items than fit, I want to see a `▼ more` marker so I know the list continues below the fold and I can scroll (j/k) to see the rest.\"*\n\nUser story: *\"As a herdr user who has scrolled down, I want to see an `▲ more` marker at the top of the list so I know there are items above the current viewport.\"*\n\n## Acceptance Criteria\n\n- When the flattened item list exceeds the visible list area at scroll offset 0, the rendered list output contains a dim-styled `▼ more` marker at the bottom of the items region (after the last visible item, before the footer/fill rows).\n- When the list is scrolled down (scrollOffset > 0) with items above the viewport, the rendered output contains a dim-styled `▲ more` marker at the top of the items region (before the first visible item/group separator).\n- When all items fit in the visible area (no overflow, scrollOffset = 0), no indicators are rendered.\n- The `rows - 1` line-count invariant is preserved with indicators active: rendered output plus the notification line still fits the pane height (regression guard for WL-0MSAAON63003N6LO).\n- Indicators are non-selectable display-only rows: they do not consume navigation state, selection index, or hierarchy scroll offset (`getVisibleItems`, `_adjustScroll` semantics unchanged).\n- New unit tests in `packages/herdr/src/worklist.test.ts` cover: indicator shown when items exist below the fold; `▲ more` shown when scrolled; no indicators when the list fits; line-count invariant with indicators active.\n- Full project test suite must pass with the new changes.\n- All related documentation is updated to reflect the changes, including code comments, README, and any relevant wiki or docs site entries.\n\n> **Note:** CHANGELOG.md is **excluded** from this list. It is managed automatically by the ship skill's release pipeline. Implementing agents should not manually update CHANGELOG.md.\n\n## Constraints\n\n- Must preserve the `rows - 1` line-count invariant established in WL-0MSAAON63003N6LO (notification line always fits; header never scrolls off the top).\n- Indicator wording is generic (\"more\"), not exact counts — group separator rows make exact \"N more\" counts unreliable.\n- Scope is list mode only: the detail view and the metadata panel (WL-0MSAYNVBY006LM9X) are unchanged.\n- Match existing styling conventions: dim-styled (`ANSI.dim`), consistent with other non-interactive chrome (footer hints, filter bar).\n- Indicator rows must be included in the render budget (the `visible` trimming loop and the fill-blank count) so output never exceeds the pane.\n\n## Existing state\n\n- `createListRenderer` sizes the visible window with `flatItems.slice(scrollOffset, scrollOffset + listHeight)` and trims it with `visible.slice(0, -1)` when items + group separators exceed `budgetForItemsAndSeps`. Nothing signals that trimming happened.\n- The pane header shows `(top N of M)` only when the *fetch* was truncated (`totalCount > items.length`), not when the *display* is truncated by the viewport — so a full fetch with a small pane shows no cue at all.\n- Scrolling works (j/k via `scrollOffset`, `_adjustScroll` keeps the selection visible), but at rest positions there is no indication of hidden items in either direction.\n- The Pi TUI browse list (`packages/tui/extensions/Worklog/lib/browse.ts`) is a separate surface and already shows `(top N of M)` in its title; pi's built-in `SelectList` component renders a `(N/M)` scroll-info line when overflow exists — the established pattern this change mirrors.\n\n## Desired change\n\n- In `createListRenderer` (list mode), after the visible window is trimmed to fit:\n  - Emit a dim `▼ more` line at the bottom of the items region when `scrollOffset + visible.length < flatItems.length` (items below the fold).\n  - Emit a dim `▲ more` line at the top of the items region when `scrollOffset > 0` (items above the viewport).\n  - Account for indicator rows in the budget: include them in the trimming calculation and in the fill-blank `used` count so the `rows - 1` invariant holds.\n  - Edge case: if reserving the bottom indicator row trims the window so that `scrollOffset + visible.length == flatItems.length`, the bottom indicator is omitted (the list then fully fits); this must terminate in a single pass (no loop).\n\n## Key Files\n\n- `packages/herdr/src/worklist.ts` — `createListRenderer`: compute indicator presence from `scrollOffset`/the trimmed visible window, emit `▼ more`/`▲ more` rows, and include them in the budget (`budgetForItemsAndSeps` trimming loop + fill-blank `used` count).\n- `packages/herdr/src/worklist.test.ts` — add unit tests: indicator shown when items below fold; `▲ more` shown when scrolled; no indicators when list fits; line-count invariant with indicators active; indicators are non-selectable display rows.\n\n## Related work\n\n- [Herdr plugin: selection list header and top lines cut off top of pane (WL-0MSAAON63003N6LO)] — completed; established the `rows - 1` line-count invariant and the visible-window trimming logic this change extends.\n- [Herdr plugin: reserve bottom 40% of selection list for item metadata and last command (WL-0MSAYNVBY006LM9X)] — completed; defines `listArea`/`panelHeight`, the fold boundary of the list region.\n- [Create a work item selection list in Herdr (WL-0MS1W8Y1L0059AKP)] — origin of the selection list.\n- pi-tui `SelectList` component (`node_modules/@earendil-works/pi-tui/dist/components/select-list.js`) — reference pattern: `(N/M)` scroll-info line when the list overflows `maxVisible`.\n\n## Risks & assumptions\n\n- Assumption: generic \"more\" wording is sufficient (user: \"more\" is fine); exact counts are intentionally avoided because group separators make them unreliable.\n- Risk: indicator rows shrink the visible item window by up to 2 rows when overflow exists. Acceptable — the alternative (no indicator) is the bug being fixed.\n- Risk: budget accounting bugs could regress the WL-0MSAAON63003N6LO line-count invariant. Mitigation: the existing invariant tests plus new tests with indicators active cover both top and bottom indicators.\n- Edge case: bottom indicator row reservation could trim the window to fully fit, making the indicator unnecessary — handle without looping.\n- Scope-creep guard: the Pi TUI browse list also lacks a fold indicator, but it already shows `(top N of M)`; improving that surface is out of scope and could be tracked as a separate work item.\n\n## Appendix: Clarifying questions & answers\n\n- Q: \"Indicator style — which do you prefer? (a) a footer line in the items region like `▼ N more` (dim), shown when items exist below the fold; (b) a position indicator like `(N/M)`; (c) both: `▼ N more` footer plus a `▲ N more` header when scrolled down\" — Answer (user): \"(c) Both\". Source: interactive reply.\n- Q: \"Scope — should this cover only the list-mode pane (herdr worklist), or also the detail-view scroll indicator (which already exists via the metadata panel scroll)?\" — Answer (user): \"confirm\" (list-mode only). Source: interactive reply.\n- Q: \"Count precision — exact remaining count (e.g. `▼ 5 more`) vs. a generic marker (e.g. `▼ more`)? Exact counts can be wrong when group separators consume rows.\" — Answer (user): \"`more` is fine\". Source: interactive reply.\n\n## Related work (automated report)\n\n### Related work items\n- **WL-0MP15YJEQ000FVWS** – Documentation: colour mapping & fallbacks (completed)\n- **WL-0MLRFF0771A8NAVW** – TUI: prevent mouse click-through from dialogs to underlying widgets (completed)\n- **WL-0MLOS7X1C1P82B5J** – Append no-questions instruction to OpenCode prompt (completed)\n\n### Repository file matches\n- `.worklog/tmp/intake-draft-herdr-below-fold-indicator-WL-0MSG8YXYJ008PWJJ.md` — matched: additional, area, cannot, count, createlistrenderer, exist, fit, fold, height, herdr, indication, indicator, items, list, marker, packages, pane, renderer, scroll, selection, silently, src, tell, there, trims, truncated, users, visible, visual, when, window, work, worklist\n- `.worklog/worktrees/wl-WL-0MSDZ64J8005Q2JJ-grid-rebalance-python-tests-mocked-socke/packages/herdr/README.md` — matched: additional, area, cannot, count, exist, height, herdr, indicator, items, list, marker, packages, pane, renderer, scroll, selection, silently, src, tell, there, visible, when, work, worklist\n- `packages/herdr/README.md` — matched: additional, area, cannot, count, exist, height, herdr, indicator, items, list, marker, packages, pane, renderer, scroll, selection, silently, src, tell, there, visible, when, work, worklist","effort":"Extra Small","id":"WL-0MSG8YXYJ008PWJJ","issueType":"bug","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"Medium","sortIndex":1800,"stage":"in_review","status":"in-progress","tags":[],"title":"Herdr selection list: no indication of items below the fold","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-05T19:36:01.143Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Herdr worklist: mouse and touch support in the selection list (WL-0MSGHM5BQ0096BNJ)\n\n## Problem statement\n\nThe Herdr work item selection list (`packages/herdr` TUI) is keyboard-only. Users of Herdr — a mouse-first, terminal workspace manager — cannot click or tap a work item to select it, cannot use the mouse wheel or touch gestures to scroll the list, and the detail/filter views are unreachable without keys. This makes the selection list feel foreign inside Herdr's pointer-friendly environment and unusable on touch-first devices (tablets, touch laptops).\n\n## Users\n\n- **Herdr users** who expect a pointer-driven experience: click/tap to pick an item, wheel/touch-scroll to browse.\n- **Touch-device users** (tablets, touch laptops, convertible terminals) who have no usable keyboard navigation path — touch taps and drags must work.\n- **Human reviewers/auditors** who switch between many work items quickly and want to point at a row instead of counting keystrokes.\n\n### User stories\n\n- As a Herdr user, I want to click or tap a row in the work item list so the selection moves to that item.\n- As a Herdr user, I want to double-click or double-tap an item so it opens in the detail view (same as pressing Enter).\n- As a Herdr user, I want to scroll the mouse wheel (or a two-finger touch scroll) in the list so the selection moves up/down like the arrow keys.\n- As a Herdr user, I want to scroll the wheel in the detail view so the description scrolls without using keys.\n- As a Herdr user, I want to tap a stage option in the filter prompt so the filter is applied without typing digits.\n\n## Acceptance Criteria\n\n1. **Mouse tracking is enabled/disabled with the TUI lifecycle**: entering raw mode emits the mouse-tracking enable sequences (`\\x1b[?1000h\\x1b[?1002h\\x1b[?1006h` — button-event tracking + SGR extended coordinates); cleanup/exit emits the matching disable sequences. Verified by test (sequence strings are emitted on setup/cleanup) and manual smoke-run.\n2. **Click/tap selects a row**: a single left click/tap on a visible list row moves the selection to that item (equivalent of the up/down arrow keys). Verified by unit tests on the row-mapping + dispatch logic.\n3. **Double-click/tap opens details**: two clicks within the double-click window on the same row activate that item (equivalent of Enter), including from the detail view back to the list. Verified by unit test.\n4. **Wheel and touch-scroll navigate**: wheel-up/wheel-down (buttons 64/65) and equivalent touch scrolls move the selection in list mode and scroll the detail content in detail mode (page/line scroll consistent with keyboard `j`/`k`). Verified by unit test.\n5. **Filter prompt is pointer-sensitive**: tapping a stage option row in the filter prompt applies that stage filter (equivalent of pressing the digit key). Verified by unit test.\n6. **Keyboard navigation is unaffected**: all existing key sequences still behave as before; unparsable/unknown mouse sequences are ignored without crashing or corrupting state. Existing `handleKeypress` tests continue to pass.\n7. **All related documentation is updated** to reflect the changes, including code comments, README, and any relevant docs site entries (e.g., docs/dev/ input-handling notes).\n8. **Full project test suite must pass** with the new changes.\n\n## Constraints\n\n- Mouse support is **additive** — existing keyboard navigation (arrows, `j`/`k`, Enter, Escape, chords) must keep working unchanged.\n- Must work through the terminal's standard mouse-reporting protocol: SGR mouse events (`\\x1b[<b;x;yM` press / `\\x1b[<b;x;y m` release), with coordinates 1-based. Touch events arrive translated as mouse events (tap = left click, drag = motion, two-finger scroll = wheel) in terminals that support it — the plugin must not require any terminal-specific feature beyond SGR reporting (widely supported: xterm, tmux, kitty, wezterm, iTerm2, Windows Terminal).\n- Drag-motion events (button code with the 32 motion bit) must not cause accidental selection jumps; motion is either used for an explicit touch-drag scroll or ignored safely.\n- Mouse sequences arrive as raw stdin chunks (e.g. `\\x1b[<0;10;5M`); they must be detected and consumed before the existing keypress parser sees them, and must not be mistaken for arrow-key escape sequences.\n- The pane-hidden pause logic (pause-when-hidden) is out of scope — input is not read while the pane is hidden anyway.\n- Fallback: terminals without mouse reporting simply never send mouse events; the list remains fully keyboard-usable (fail-soft).\n\n## Existing state\n\n- `packages/herdr/src/worklist.ts` (2790 lines) is the selection-list TUI: `runWorklistTui` sets raw mode (`process.stdin.setRawMode(true)`), reads input via `process.stdin.on('data', onData)`, and dispatches through `handleKeypress(state, key, termSize)` → `keyToAction(key)`.\n- No mouse-tracking escape sequences are emitted; no SGR mouse events are parsed. Unknown sequences currently fall through `keyToAction` to `default → null`.\n- `ANSI` constants object (line 132) already holds terminal escapes — the natural home for mouse enable/disable sequences.\n- Rendering computes row geometry in `createListRenderer`: a header row at the top, the list area (`listArea = rows - 1 - panelHeight`), a notification row, and a metadata panel at the bottom. Detail mode renders a `formatDetailView` block; filter mode renders `formatFilterPrompt` with digit-key stage options.\n- Tests live in `packages/herdr/src/worklist.test.ts` (vitest); `handleKeypress` is a pure function tested directly — the new mouse logic should follow the same pattern (pure, unit-testable).\n\n## Desired change\n\n1. Add ANSI constants for mouse tracking enable (`\\x1b[?1000h\\x1b[?1002h\\x1b[?1006h`) and disable (`\\x1b[?1000l\\x1b[?1002l\\x1b[?1006l`).\n2. In `runWorklistTui`, emit enable sequences when entering raw mode and disable sequences in `cleanup()`.\n3. Add a pure `parseMouseEvent(key: string)` helper that recognizes `\\x1b[<...M`/`\\x1b[<...m` and returns `{ button, x, y, release } | null`.\n4. Add a pure row-mapping helper that converts a click's 1-based `y` into a list row (accounting for the header offset and `scrollOffset`), a detail-view scroll position, or a filter-prompt option, depending on `state.mode`.\n5. Add `mapMouseToAction(state, mouseEvent, termSize)` returning an action (`select-row`, `open-detail`, `wheel-up/down`, `scroll-detail`, `filter-stage`, etc.) and dispatch it from `onData` before the keypress path; track last-click time/position for double-click detection (~400 ms window).\n6. Add unit tests mirroring the existing `handleKeypress` suite: parser, row mapping per mode, wheel dispatch, double-click window, filter-tap, and \"unknown mouse sequence is ignored\" regression.\n\n## Key Files\n\n- `packages/herdr/src/worklist.ts` — mouse enable/disable sequences, SGR event parser, row→item mapping, mouse dispatch in `onData`, double-click detection (primary change).\n- `packages/herdr/src/worklist.test.ts` — unit tests for the parser, per-mode mapping, wheel/double-click/filter-tap dispatch, and unknown-sequence regression.\n- `packages/herdr/dist/` — rebuilt artifacts via `npm run build` (tsc), kept in sync with source.\n- `docs/dev/pause-when-hidden.md` — documents the input/refresh loop; add a note about mouse input if behavior interacts with it (minor).\n\n## Related work\n\n- `Create a work item selection list in Herdr (WL-0MS1W8Y1L0059AKP)` — the parent feature that built this list; its AC #5 defined keyboard navigation, this item extends input to pointers.\n- `Impl: visibility-gated auto-refresh/sync in worklist TUI (WL-0MSB4473J004NYO5)` — owns the input loop/timers this change touches (input is only read while the pane is visible).\n- `Mouse click on Work Items tree does not update details (WL-0MLAJ3Z750G25EJE)` — historical pointer bug in the older Pi TUI; the click-select behavior contract should match (click selects, details follow).\n- `Critical: TUI closes when clicking anywhere (WL-0MKX2C2X007IRR8G)` — historical mouse-handling crash; regression tests here must prove unknown mouse sequences are inert.\n- `docs/dev/pause-when-hidden.md` — architecture notes for the worklist input/refresh loop.\n\n## Risks and assumptions\n\n- **Assumption:** Herdr's terminal supports SGR mouse reporting (1006) — all mainstream terminals do; if not, events simply never arrive (fail-soft, keyboard remains fully usable).\n- **Risk — mouse sequences misparsed as keys:** raw chunks like `\\x1b[<0;10;5M` must be fully consumed by the mouse path; partial-buffer splits (a chunk split across reads) must be buffered until the terminating `M`/`m`. Mitigated by dedicated parser tests including split-chunk cases.\n- **Risk — drag-motion events:** button codes with the 32 motion bit arrive during drags; guard so motion never jumps the selection. Out of scope: implementing drag-to-scroll as a scroll gesture (recorded as a follow-up opportunity rather than expanding scope).\n- **Risk — double-click timing:** a ~400 ms window is standard; on slow terminals two intentional clicks may land outside it — acceptable, Enter remains available.\n- **Risk — regression to chord/agent shortcuts:** mouse input must not intercept or swallow chord leader keys; the keypress path is unchanged except that mouse-shaped sequences are consumed first.\n- **Scope-creep guard:** hover highlighting, drag-to-scroll, right-click context menus, and horizontal touch gestures are NOT in scope; if valuable, they should be captured as linked work items, not added to this item.\n\n## Related work (automated report)\n\n### Related work items\n- **WL-0MLLGKZ7A1HUL8HU** – Add links to dependencies in the metadata output of the details pane in the TUI. (open)\n- **WL-0MSGAEXER000707R** – Pi agent pane opened via a+a (audit) starts in herdr plugin source dir, not the project root (completed)\n- **WL-0MNC79G3P004NZXH** – Markdown formatting in Description and Comments field (completed)\n\n### Repository file matches\n- `.worklog/worktrees/wl-SA-0MS1T7A5W003UJUH-remove-skill-ralph-directory-tree/skill/intakeall/tests/test_intakeall.py` — matched: events, item, list, make, packages, sensitive, support, touch, work\n- `.worklog/worktrees/wl-SA-0MS1T7A5W003UJUH-remove-skill-ralph-directory-tree/command/intake.md` — matched: events, intent, item, list, seed, sensitive, support, work\n- `.worklog/worktrees/wl-SA-0MS1T7A5W003UJUH-remove-skill-ralph-directory-tree/docs/workflow/validation-rules.md` — matched: intent, item, list, make, select, selection, support, work\n\n\n---\n\n## Plan (generated 2026-08-07 by /skill:plan)\n\n**Approach:** Additive mouse/touch support over the existing SGR mouse-reporting protocol, behind the existing raw-mode lifecycle, with a pure-function parser/dispatch design mirroring `handleKeypress` (unit-testable, fail-soft). Decomposed into 4 vertical slices (test-first) + 1 recorded follow-up.\n\n### Features (child work items)\n\n1. **Test: mouse input handling suite** (WL-0MSI720DX002E9WC) — `task`, high. Test-first suite covering all 8 parent ACs: lifecycle sequence emission, SGR parser incl. split chunks, per-mode row mapping, click/double-click/wheel/filter-tap dispatch, drag-motion guard, unknown-sequence inertness, keyboard regression.\n2. **Implement: mouse tracking lifecycle + SGR parser** (WL-0MSI7278X0008XGM) — `feature`, medium. ANSI enable/disable constants, emit on raw-mode entry / cleanup, pure `parseMouseEvent` with chunk buffering. Depends on 1.\n3. **Implement: mouse dispatch and interactions** (WL-0MSI72EYW00050N2) — `feature`, medium. Row mapping per mode, `mapMouseToAction`, dispatch from `onData` before keypress path, ~400 ms double-click window, wheel/touch scroll (list selection + detail scroll incl. ToC-focus nuance), filter-prompt tap, drag-motion guard. Depends on 1, 2.\n4. **Docs, build and verification** (WL-0MSI72L3O005BIW5) — `task`, medium. Update `docs/dev/pause-when-hidden.md` + README notes, rebuild `packages/herdr/dist`, full suite green, manual smoke-run evidence comment. Depends on 3.\n\n### Recorded follow-up (out of scope here, per scope-creep guard)\n\n- **Follow-up: drag-to-scroll in Herdr worklist** (WL-0MSI72S0Y0060BRK) — `feature`, low, sibling with `discovered-from:WL-0MSGHM5BQ0096BNJ`. Drag/touch-drag scroll; must respect the drag-motion guard.\n\n### Assumptions recorded during planning\n\n- A1: Clicks on non-list areas (header, notification row, metadata panel) are inert (ignored safely) — derived from constraints.\n- A2: Wheel events in filter mode are ignored (filter options have no arrow-key path today; tap and digit keys are the selectors).\n- A3: Wheel in detail mode mirrors j/k semantics, including the detail-ToC-focus nuance (WL-0MSHWHULZ001FL8I): wheel over the ToC moves ToC selection; wheel over the document scrolls it.\n- A4: Double-click maps to the Enter-equivalent action for the current mode; exact Enter semantics in detail mode are mirrored as-is (implementer verifies during implementation; this plan captures intent only).\n- A5: SGR 1006 is the only required protocol; terminals without it fail soft (keyboard fully usable) — already stated in parent constraints.\n\n## Plan: changelog\n\n- 2026-08-07: Created 4 child features + 1 discovered sibling (WL-0MSI720DX002E9WC, WL-0MSI7278X0008XGM, WL-0MSI72EYW00050N2, WL-0MSI72L3O005BIW5, WL-0MSI72S0Y0060BRK); added dependency edges (test-first); ran six automated review stages (all passed, no changes needed); stage set to `plan_complete`.","effort":"Small","id":"WL-0MSGHM5BQ0096BNJ","issueType":"feature","needsProducerReview":true,"parentId":null,"priority":"medium","risk":"Medium","sortIndex":2000,"stage":"in_review","status":"completed","tags":[],"title":"Herdr worklist: mouse and touch support in the selection list","updatedAt":"2026-08-23T12:14:59.244Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-05T20:53:54.667Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Show plugin version in Herdr work item selection list (WL-0MSGKEBFU004T3N3)\n\n## Problem statement\n\nThe Herdr Worklog work item selection list gives no indication of which plugin version is running, making it hard to verify installs, spot stale builds, or report bugs accurately. A dimmed, right-aligned version number in the pane header solves this without visual clutter.\n\n## Users\n\n- **Herdr users** who want to confirm which version of the Worklog Selection List plugin is running.\n- **Developers/support** who need the version handy when filing bug reports or verifying an updated build is live.\n\n### User stories\n\n- As a Herdr user, I want to see the plugin version in the corner of the work item list, so I can confirm which build I'm running.\n- As a developer, I want the version visible but unobtrusive, so it doesn't distract from the list content.\n\n## Acceptance Criteria\n\n1. The work item selection list header shows the plugin version (`0.1.0` from `packages/herdr/package.json`), right-aligned to the pane width.\n2. The version is rendered in the dimmed ANSI style (`\\x1b[2m`), matching the existing `[auto-refresh on]` treatment, and does not interfere with the left-aligned `Work Items — N item(s)` title.\n3. The version is read at runtime from `packages/herdr/package.json` (not hardcoded in the renderer), so it stays in sync with the manifest version.\n4. Unit tests in `packages/herdr/src/worklist.test.ts` cover the header rendering with the version suffix, including a case without a version available (graceful fallback).\n5. All related documentation is updated to reflect the changes, including code comments, README, and any relevant wiki or docs site entries.\n6. Full project test suite must pass with the new changes.\n\n## Constraints\n\n- Unobtrusive: dimmed font, right-aligned to the pane edge; must not change existing header content or alignment of the title.\n- Use existing patterns: `termSize.cols` for pane width, `ANSI.dim` for dimming, `readFileSync` for reading `package.json` (pattern already used in `worklist.ts`/`shortcut-config.ts`).\n- No changes to `herdr-plugin.toml` version unless a version bump is explicitly requested.\n- Do not update CHANGELOG.md (managed automatically by the ship skill release pipeline).\n\n## Existing state\n\n- The selection list header is rendered in `packages/herdr/src/worklist.ts` (~line 1585): ` Work Items — N item(s)` plus optional `[auto-refresh on]` (dimmed) and `[paused — hidden]` indicators.\n- `termSize` (with `cols`) is available in the render function; `truncateLine()` handles width overflow.\n- The plugin version is declared in two places: `packages/herdr/package.json` (`\"version\": \"0.1.0\"`) and `packages/herdr/herdr-plugin.toml` (`version = \"0.1.0\"`). There is currently no runtime version constant.\n- Existing header tests in `packages/herdr/src/worklist.test.ts` (lines ~86–100) assert `'Work Items'` presence.\n\n## Desired change\n\n1. Add a runtime helper that reads the plugin version from `packages/herdr/package.json` (e.g., parse JSON, fall back to a safe default like `'?'` or omit when unreadable).\n2. In the list-mode header renderer, append a dimmed `v<version>` string right-aligned to `cols` (using padding), while keeping the title left-aligned.\n3. Add unit tests covering: version suffix present and right-aligned, dim style applied, graceful fallback when the version can't be read.\n\n## Key Files (predicted)\n\n- `packages/herdr/src/worklist.ts` — Header rendering; add version read + right-aligned dimmed suffix (near line 1585).\n- `packages/herdr/src/worklist.test.ts` — Header assertions for version suffix, alignment, and fallback.\n- `packages/herdr/package.json` — Version source (`0.1.0`); no change expected.\n- `packages/herdr/README.md` — Optional: document the version display.\n\n## Related work\n\n- **WL-0MS1W8Y1L0059AKP** — Create a work item selection list in Herdr: parent feature that produced `packages/herdr/src/worklist.ts`.\n- **WL-0MQHYI4E60075SQT** — Fix icon prefix alignment in Pi TUI work item selection list: related header/line formatting work (Pi TUI counterpart).\n- **WL-0MSBVS4AS006ZQEZ** — Herdr selection list refreshes immediately when its tab regains focus: related Herdr list behavior work.\n- **WL-0MQL0T5TR0060AEH** — Add activity-indicator footer to Worklog Pi extension: related footer/status UI precedent (Pi TUI counterpart).\n\n## Related work (automated report)\n\n### Related work items\n- **WL-0MKX5ZV3D0OHIIX2** – Add regression tests for mouse click crash and TUI behavior (completed)\n- **WL-0MKZ34IDI0XTA5TB** – TUI: Reparent items without typing IDs (completed)\n- **WL-0MQL0T5TR0060AEH** – Add activity-indicator footer to Worklog Pi extension (completed)\n\n### Repository file matches\n- `.worklog/worktrees/wl-SA-0MS1T7A5W003UJUH-remove-skill-ralph-directory-tree/tests/test_ralph_loop.py` — matched: add, item, list, number, position, show, src, using, version, work\n- `.worklog/worktrees/wl-SA-0MSGKAWXQ009VVG2-enforce-git-worktree-usage-for-all-imple/skill/audit/scripts/audit_runner.py` — matched: add, item, list, number, position, show, using, version, work\n- `skill/audit/scripts/audit_runner.py` — matched: add, item, list, number, position, show, using, version, work\n\n## Risks & assumptions\n\n- **Assumption**: `package.json` is present next to the compiled/tsx module at runtime; the module-dir relative read pattern (as in `shortcut-config.ts`) works for both `npx tsx` and `dist` runs.\n- **Risk**: ANSI codes in a right-padded line can misalign padding calculations if padding is computed on raw text length; must compute visible-width padding before applying ANSI styles.\n- **Risk**: Very narrow panes could crowd the header; the version suffix should be dropped or truncated if it would overlap the title (guard on `cols`).\n- **Scope creep risk**: Bumping the version, adding the version elsewhere (footer/status bar), or showing `wl`/ContextHub versions — out of scope; record as follow-up work items if desired.\n\n## Appendix: Clarifying questions & answers\n\n- **Q1**: \"Which version number? I assume the plugin's own version (`0.1.0` from `packages/herdr/package.json` / `herdr-plugin.toml`) — or did you mean the `wl` CLI version or ContextHub's version?\"  \n  **Answer** (user): \"yes the plugins\" — the Worklog Selection List plugin's own version. Source: interactive reply.\n\n- **Q2**: \"Placement preference? Unobtrusive, dimmed — candidates: (a) right side of the header line, (b) footer hint line, (c) bottom-right corner.\"  \n  **Answer** (user): \"a (right aligned to width of pane)\" — right-aligned in the header line. Source: interactive reply.","effort":"Extra Small","id":"WL-0MSGKEBFU004T3N3","issueType":"task","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"Medium","sortIndex":3900,"stage":"idea","status":"open","tags":[],"title":"Show plugin version in Herdr work item selection list","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-06T12:49:55.338Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Herdr metadata panel: drop duplicate ID row from data section\n\n## Problem statement\n\nIn the herdr work item selection list, the metadata panel for the selected item shows the work item ID twice: once in the panel title/header line (`── WL-XXXX ──`) and again as the first row of the metadata data table (`ID  WL-XXXX`). The ID row in the data table is redundant and should be dropped, leaving the ID only in the panel title line.\n\n## Users\n\nUsers navigating work items in the herdr selection list (`prefix+l`).\n\n**User story:** As a user browsing work items in the herdr selection list, I want the item ID shown once in the metadata panel so the panel is not redundant and the layout is cleaner.\n\n## Acceptance Criteria\n\n- [ ] The metadata data table in the herdr metadata panel no longer contains an `ID` row.\n- [ ] The metadata panel title/header line (`── WL-XXXX ──`) still shows the item ID.\n- [ ] Other metadata rows (Status, Stage, Priority, Title, etc.) render unchanged.\n- [ ] The detail view (`formatDetailContent`) behavior is unchanged (it already omits ID/Title from its table since they appear in its header).\n- [ ] Tests are added/updated covering the metadata panel output (e.g. the `formatMetadataPanel` describe block in `packages/herdr/src/worklist.test.ts`).\n- [ ] Full project test suite passes with the new changes.\n\n## Constraints\n\n- Fix should follow the existing pattern used by the detail view (`formatDetailContent` filters `ID`/`Title` out of the table since they are in the header).\n- No behavior change to the CLI `show` output or other renderers.\n\n## Existing state\n\n`packages/herdr/src/worklist.ts`:\n\n- `formatMetadataPanel` (line ~874) renders the panel header separator `── ${item.id} ──` (line ~891) and then the metadata table from `buildMetaRows(item)` (line ~894).\n- `buildMetaRows` (line ~828) unconditionally adds the ID as the first row via `addMeta('ID', item.id)` (line ~835).\n- The detail view `formatDetailContent` (line ~969) already filters `ID` and `Title` out of the shared table because both appear in its header: `.filter(([label]) => label !== 'ID' && label !== 'Title')`.\n\nSo the metadata panel currently shows the ID in both the header line and the first data row — the inconsistency with the detail view.\n\n## Desired change\n\nRemove the `ID` row from the metadata table rendered in the metadata panel, consistent with how `formatDetailContent` handles it. The header line `── WL-XXXX ──` continues to display the ID. The `Title` row should remain in the metadata panel table since the panel header does not show the title.\n\nLikely approach: in `formatMetadataPanel`, filter the `ID` row from `buildMetaRows(item)` output (e.g. `.filter(([label]) => label !== 'ID')`), mirroring `formatDetailContent`. Alternatively, remove `addMeta('ID', item.id)` from `buildMetaRows` — but note `formatDetailContent` relies on `buildMetaRows` including ID then filtering it, so removing it from the shared builder is also safe as long as tests are updated. Prefer the filter-in-panel approach for minimal blast radius.\n\n## Key Files\n\n- `packages/herdr/src/worklist.ts` — `formatMetadataPanel` (~line 874) and/or `buildMetaRows` (~line 828); the ID row is added at line ~835.\n- `packages/herdr/src/worklist.test.ts` — `formatMetadataPanel — field rendering and scrolling` describe block (~line 896); add a test asserting the data table no longer contains an `ID` row while the header still shows the ID. Existing test `expect(joined).toContain('WL-RICH1')` may still pass via the header, but a negative assertion is needed.\n\n## Related work\n\n- **WL-0MSAYNVBY006LM9X** — \"Herdr plugin: reserve bottom 40% of selection list for item metadata and last command\" (completed): introduced the metadata panel and shared `buildMetaRows` (origin of the ID duplication).\n- **WL-0MSGIXHHI009KFW9** — \"Herdr selection list: metadata section should use the same icons as the list section\" (open): also touches metadata section presentation.\n- **WL-0MSFZKQL700381P3** — \"Show description preview in herder plugin selection list\" (open): related to the selection list metadata area.\n- **WL-0MNK7CB3F0008YT3** — \"Display work item ID in TUI metadata block\" (completed): the deprecated Blessed-TUI predecessor; closed as wont-fix/deprecated — not to be used as a pattern.\n\n(Note: an automated find-related scan flagged WL-0MQCU6R6V008JX62, WL-0MRERDU1J000W1IS, WL-0MM0DBM6I1PHQBFI and README worktree copies; these were false positives from keyword overlap and are not genuinely related — excluded per conservative policy.)\n\n## Risks & assumptions\n\n- Assumption: \"metadata section\" refers to the herdr selection-list metadata panel (the only place showing the ID both in a title line and as the first data-table row). The CLI `show` output shows ID once in its metadata table with no separate ID-bearing title line, so it is out of scope.\n- Low risk: change is localized; detail view and CLI outputs are unaffected.\n- Scope creep: do not change icon consistency (tracked by WL-0MSGIXHHI009KFW9) or add description previews (tracked by WL-0MSFZKQL700381P3) in this item.\n\n## Appendix: Clarifying questions & answers\n\n- Q: \"Which metadata section is this about?\" — Answer (user): \"The metadata section has the ID in the title line and in the first line of the data section. Drop it from the data section.\" Source: interactive reply. Interpretation: the herdr metadata panel, whose title line is `── <id> ──` and whose first data-table row is `ID  <id>`. Verified against `packages/herdr/src/worklist.ts` (lines 891 and 835).\n- Q: \"Should the Title row remain in the metadata panel table?\" — Assumption (agent, Map): Yes — the panel header shows only the ID, not the title, so Title is not duplicated and stays. Source: code inspection of `formatMetadataPanel` header (line 891 shows only `item.id`). Documented in Desired change.","effort":"Small","id":"WL-0MSHIJR7T007Q9R7","issueType":"task","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"Low","sortIndex":4000,"stage":"idea","status":"open","tags":[],"title":"Herdr metadata panel: drop duplicate ID row from data section","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-06T22:00:53.037Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Detail view ToC overflows pane — first lines scroll off the top of the screen\n\n## Problem statement\n\nIn the Herdr plugin's work-item detail view, when an item has a `Related Docs` Table of Contents (ToC) (i.e. ≥1 `.md` Key File), the rendered output exceeds the pane height: the header (id/title) and the first ToC lines scroll off the top of the screen, so the ToC is not entirely visible. The ToC is also rendered twice and the item header is dropped entirely.\n\n## Users\n\n- **Herdr users** viewing a work item's detail view in the plugin pane, who need the item header and the full `Related Docs` ToC visible at the top of the pane while the metadata/description/document body scrolls beneath.\n\n### Example User Stories\n\n- As a Herdr user, I want to see the item id, title, and the complete Related Docs ToC when I open a work item's detail view, so nothing important is cut off at the top of the screen.\n- As a Herdr user, I want the ToC to stay pinned while I scroll the document, so I can always see which documents are linked to the item.\n\n## Acceptance Criteria\n\n1. When an item has a ToC (≥1 `.md` Key File), the detail view shows the header (id, title, separator) and the **full** ToC at the top of the pane at scroll 0 — no lines cut off. The ToC is rendered **exactly once** (no duplication), and the item header is present.\n2. Detail-view output never exceeds the pane: `formatDetailView` returns at most `viewportHeight` lines, and the full renderer (`createListRenderer` detail mode) returns at most `rows - 1` lines — the line-count invariant from WL-0MSAAON63003N6LO holds for detail mode with a ToC present.\n3. The header + ToC block stays **pinned** at the top while the body (metadata table, description, md viewer) scrolls beneath it; scrolling is limited to the body region (viewport height minus the pinned block height), and `WorkItemListState.detailScrollDown` clamps against the same reduced body viewport. No dead/blank region at the bottom when fully scrolled.\n4. Items **without** `.md` Key Files (no ToC) keep the existing full-scroll behavior unchanged (regression check — no behavior change for the no-ToC path).\n5. Unit tests in `packages/herdr/src/worklist.test.ts` cover: line-count invariant for detail mode with ToC (renderer ≤ `rows - 1`; `formatDetailView` ≤ `viewportHeight`), header + ToC fully visible at scroll 0, ToC not duplicated, pinned-ToC scroll regression (existing `'keeps the ToC pinned at the top while the document scrolls'` test kept green/updated), and no-ToC behavior unchanged.\n6. All related documentation is updated to reflect the changes, including code comments and any relevant README/docs entries describing the detail-view ToC layout (`packages/herdr/README.md`).\n7. Full project test suite must pass with the new changes.\n\n> **Note:** CHANGELOG.md is excluded from this list — it is managed automatically by the ship skill's release pipeline.\n\n## Constraints\n\n- Preserve the pinned-ToC design from WL-0MSHWHULZ001FL8I / WL-0MSGTLSUT002NF29: the ToC stays visible on all renders; only the body below it scrolls.\n- Respect the line-count invariant established in WL-0MSAAON63003N6LO: the renderer must never emit more than `rows - 1` lines, otherwise the terminal scrolls the top of the output off the pane.\n- Surgical change: do not alter scrolling behavior for items without a ToC (the pre-existing full-scroll path).\n- No new dependencies; reuse the existing `formatDetailToC` / `formatDetailContent` helpers.\n\n## Existing state\n\n- `formatDetailView` (`packages/herdr/src/worklist.ts`, ~line 1223) builds the detail output as `[...tocLines, ...bodyLines.slice(safeOffset, safeOffset + viewportHeight)]` where `bodyLines = allLines.slice(tocLines.length)`.\n- The slicing assumes the ToC occupies the first `tocLines.length` lines of `allLines`, but `formatDetailContent` emits the header first (blank, id, title, separator = 4 lines) and the ToC **after** it. Consequences (verified by reproduction): (a) the header is dropped from the output, (b) the ToC is duplicated (once from `tocLines`, once inside `bodyLines`), (c) total output = `viewportHeight + tocLines.length` lines, which overflows the pane (`viewportHeight = rows - 1` in the renderer). Reproduction: `formatDetailView(item, 80, 0, 20)` with a 2-entry ToC returns 26 lines; the renderer on a 24-row terminal emits 29 lines (max allowed 23).\n- Regression introduced in commit `76a52c9f` (WL-0MSHWHULZ001FL8I, \"Implementation complete\").\n- `WorkItemListState.detailScrollDown` (~line 496) computes `maxScroll` against the full `viewportHeight` instead of the body region, so clamping is also wrong for the pinned layout.\n- The existing test `'keeps the ToC pinned at the top while the document scrolls'` only asserts the ToC appears in the first 8 lines — it never checks the total line count, so it passes despite the overflow.\n\n## Desired change\n\n1. In `formatDetailView`, compute a **pinned block** = header (4 lines: blank, id, title, separator) + `tocLines`; the scrollable **body** = `allLines.slice(pinnedHeight)`; the **body viewport** = `max(1, viewportHeight - pinnedHeight)`; `maxScroll = max(0, bodyLines.length - bodyViewport)`. Render `[...pinned, ...bodyLines.slice(safeOffset, safeOffset + bodyViewport)]` and update the scroll-indicator percentage math to use `bodyViewport`.\n2. Keep the no-ToC path (`tocLines.length === 0`) identical to the pre-ToC behavior (whole content scrolls, header included) to avoid scope creep.\n3. Update `WorkItemListState.detailScrollDown` to clamp against the reduced body viewport (subtract the pinned height) so `j`/`G` behave consistently with the renderer.\n4. Optionally introduce a named constant (e.g. `DETAIL_HEADER_LINES = 4`) documenting the fixed header shape so the slicing is self-explanatory.\n5. Add/extend unit tests per AC5; update `packages/herdr/README.md` if it describes the detail-view layout.\n\n## Key Files\n\n- `packages/herdr/src/worklist.ts` — `formatDetailView` pinned header+ToC slicing and body-viewport math; `WorkItemListState.detailScrollDown` clamp; optional header-lines constant.\n- `packages/herdr/src/worklist.test.ts` — line-count invariant tests for detail mode with ToC, header/ToC visibility at scroll 0, no-duplication check, no-ToC regression, updated pinned-ToC test.\n- `packages/herdr/README.md` — document the pinned header + ToC detail-view layout if it describes the ToC behavior.\n\n## Risks & assumptions\n\n- **Header-shape fragility:** the fix relies on the header being exactly 4 lines (blank, id, title, separator). Mitigate by introducing a named `DETAIL_HEADER_LINES` constant next to `formatDetailContent` and by an AC1 test asserting the header is visible at scroll 0.\n- **No-ToC regression:** altering `formatDetailView` slicing could accidentally change the full-scroll path for items without `.md` Key Files. Mitigate with AC4 regression test asserting the no-ToC path is unchanged.\n- **Viewport-height mismatch:** the state class uses `rows - 4` while the renderer uses `rows - 1`; today the renderer's clamp absorbs the difference. The fix must subtract the pinned height consistently in both `formatDetailView` and `detailScrollDown` so `j`/`G` and the on-screen clamp agree.\n- **Scope creep:** pinning the header for ALL items (even without a ToC) is deliberately out of scope — it is a UX change beyond the reported bug. Recorded as an assumption; can be tracked as a separate work item if desired.\n- **Assumption:** the fix pins header + ToC together (the header sits above the ToC in the content stream, so pinning the ToC keeps the header visible) and scrolls only the body (metadata/description/md viewer) beneath — faithful to the pinned-ToC design intent (WL-0MSGTLSUT002NF29 AC3) and the user's expectation that the first lines be visible.\n\n## Related work\n\n- `WL-0MSHWHULZ001FL8I` (completed) — \"Herdr: detail-view ToC for Related Docs (arrow/j/k navigate, Enter renders)\" — the pinned-ToC implementation that introduced this regression (commit `76a52c9f`).\n- `WL-0MSGTLSUT002NF29` (completed) — \"Herdr plugin: add Related Docs row to metadata section listing .md Key Files (openable in the markdown viewer)\" — defines the pinned-ToC design intent.\n- `WL-0MSAAON63003N6LO` (completed) — \"Herdr: pane line-count invariant (rows - 1)\" — the invariant the detail view now violates.\n- `WL-0MS31WX9C008FIQR` (completed) — \"Full scrollable detail view\" — original detail-view scrolling design (whole content scrolls).\n\n## Appendix: Clarifying questions & answers\n\nNo interview questions were required for this intake.\n\n- The seed report (\"The table of contents on the detail view of work items is not entirely visible — the first few lines are off the top of the screen\") combined with repository analysis was conclusive: the root cause was located by inspection of `formatDetailView`/`formatDetailContent` in `packages/herdr/src/worklist.ts`, confirmed by a reproduction test (26 lines emitted for a 20-line viewport; 29 lines for a 24-row terminal vs. the 23-line invariant), and pinned to the regression commit `76a52c9f` (WL-0MSHWHULZ001FL8I) via `git log -S`.\n- Assumption (documented, user can override): the fix pins the **header + ToC** block at the top of the detail view (the header is above the ToC in the content stream, so pinning the ToC necessarily keeps the header visible) while the body scrolls beneath — this is the faithful reading of the pinned-ToC design intent in WL-0MSGTLSUT002NF29 AC3 (\"The ToC stays visible on all renders\") and the user's expectation that the first lines be visible.\n- Assumption (documented, user can override): items **without** `.md` Key Files keep the existing full-scroll behavior unchanged (surgical scope).\n\n---\n\n## Related work (automated report)\n\n### Related work items\n- **WL-0MKXJPCDI1FLYDB1** – Prompt input box must resize on word wrap (completed)\n- **WL-0MRMCO4WX0073W30** – Arbitrary-depth chords in detail view (completed)\n- **WL-0MQQW534Q009JSX7** – Remove superfluous ## Stage section from wl create output (completed)\n\n### Repository file matches\n- `skill/audit/scripts/audit_runner.py` — matched: detail, entirely, first, items, lines (+11 more)\n- `skill/audit/SKILL.md` — matched: first, items, medium, off, priority (+7 more)\n- `AGENTS.md` — matched: detail, first, items, lines, medium (+6 more)","effort":"Extra Small","id":"WL-0MSI28AP80002F5S","issueType":"bug","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"Low","sortIndex":1900,"stage":"in_review","status":"in-progress","tags":[],"title":"Detail view ToC overflows pane — first lines scroll off the top of the screen","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"herdr","createdAt":"2026-08-07T00:15:57.814Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Summary\n\nVitest suite for mouse/touch support in the Herdr worklist TUI, written before implementation (test-first). Covers all eight acceptance criteria of the parent WL-0MSGHM5BQ0096BNJ: lifecycle sequence emission, SGR parser (incl. split chunks), per-mode row mapping, click/double-click/wheel/filter-tap dispatch, drag-motion guard, and unknown-sequence inertness.\n\n## Acceptance Criteria\n\n- [ ] Suite verifies mouse-tracking enable sequences (`\\x1b[?1000h\\x1b[?1002h\\x1b[?1006h`) are emitted on raw-mode entry and disable sequences on cleanup (parent AC1)\n- [ ] Suite covers `parseMouseEvent`: SGR press (`\\x1b[<b;x;yM`) and release (`\\x1b[<b;x;y m`), 1-based coordinates, button decoding (incl. wheel 64/65 and motion bit 32), split-chunk partial-sequence buffering, unknown/non-mouse sequences returning null (parent AC1, AC6)\n- [ ] Suite covers row mapping per mode: list (header offset + scrollOffset), detail (scroll position), filter prompt (stage option row) (parent AC2–AC5)\n- [ ] Suite covers dispatch: single click/tap selects row; double-click within 400 ms opens detail; wheel/touch scroll navigates list and scrolls detail; tapping a stage option applies the filter; drag-motion never jumps selection (parent AC2–AC5)\n- [ ] Suite covers regression: unknown mouse sequences are inert (no crash, no state corruption) and all existing keyboard `handleKeypress` tests continue to pass (parent AC6)\n- [ ] All tests pass via the herdr package vitest run\n\n## Minimal Implementation\n\n- Extend `packages/herdr/src/worklist.test.ts` following the existing pure-function test pattern (`handleKeypress` describe blocks)\n- Add describe blocks: ANSI mouse sequences, parser, row mapping per mode, dispatch actions, double-click window, unknown-sequence regression\n- Reuse existing fixtures/term-size constants (e.g. TERM_80x24)\n\n## Dependencies\n\nNone — created first (test-first ordering).\n\n## Deliverables\n\n- `packages/herdr/src/worklist.test.ts` additions (new + updated tests)","effort":"","id":"WL-0MSI720DX002E9WC","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSGHM5BQ0096BNJ","priority":"medium","risk":"","sortIndex":4100,"stage":"plan_complete","status":"open","tags":[],"title":"Test: mouse input handling suite","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-07T00:16:06.705Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Summary\n\nAdd SGR mouse-tracking enable/disable sequences tied to the TUI lifecycle (raw-mode entry / cleanup) and implement the pure SGR mouse-event parser. First implementation slice for WL-0MSGHM5BQ0096BNJ; covered by the test-first suite in WL-0MSI720DX002E9WC.\n\n## Acceptance Criteria\n\n- [ ] ANSI constants added for mouse enable (`\\x1b[?1000h\\x1b[?1002h\\x1b[?1006h`) and disable (`\\x1b[?1000l\\x1b[?1002l\\x1b[?1006l`) in the existing `ANSI` object (worklist.ts ~line 133) (parent AC1)\n- [ ] Enable sequences are written to stdout when the TUI enters raw mode; disable sequences are written in `cleanup()` (parent AC1) — verified by the test suite and a manual smoke-run\n- [ ] Pure `parseMouseEvent(key: string)` recognizes `\\x1b[<b;x;yM` (press) and `\\x1b[<b;x;y m` (release) and returns `{ button, x, y, release } | null` (parent AC1, AC6)\n- [ ] Partial sequences split across stdin chunks are buffered until the terminating M/m; non-mouse or unknown sequences return null without corrupting the keypress path (parent AC6 + risk mitigation)\n- [ ] All tests from the test-first suite (WL-0MSI720DX002E9WC) that cover this slice pass; no existing `handleKeypress` tests regress (parent AC6)\n\n## Minimal Implementation\n\n- Extend the `ANSI` object in `packages/herdr/src/worklist.ts` with `mouseEnable` / `mouseDisable` entries\n- Write enable sequences in the `runWorklistTui` raw-mode block and disable sequences in `cleanup()` (only when raw mode was entered)\n- Add exported pure `parseMouseEvent` + a small module-level buffer for split chunks; document the SGR format in a comment\n\n## Dependencies\n\n- WL-0MSI720DX002E9WC (test suite defines expected behavior)\n\n## Deliverables\n\n- `packages/herdr/src/worklist.ts` changes (ANSI constants, lifecycle emission, parser)\n- Green tests for this slice","effort":"","id":"WL-0MSI7278X0008XGM","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSGHM5BQ0096BNJ","priority":"medium","risk":"","sortIndex":11400,"stage":"plan_complete","status":"blocked","tags":[],"title":"Implement: mouse tracking lifecycle + SGR parser","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-07T00:16:16.712Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Summary\n\nMap parsed SGR mouse events to TUI actions — click/tap selects, double-click/tap opens details, wheel/touch scroll navigates, filter-prompt tap applies a stage — dispatched from onData before the keypress path. Core interaction slice for WL-0MSGHM5BQ0096BNJ.\n\n## Acceptance Criteria\n\n- [ ] Single left click/tap on a visible list row moves the selection to that item (equivalent of up/down arrow keys) (parent AC2)\n- [ ] Double-click/tap within ~400 ms on the same row activates that item — the Enter-equivalent action for the current mode (list: open detail; detail: existing Enter semantics, mirrored as-is). Double-click state does not leak to unrelated rows (parent AC3)\n- [ ] Wheel-up/wheel-down (buttons 64/65) and translated touch scrolls move the selection in list mode and scroll the detail content consistent with keyboard j/k (including the detail ToC-focus nuance from WL-0MSHWHULZ001FL8I: wheel over the ToC moves ToC selection, wheel over the document scrolls it) (parent AC4)\n- [ ] Tapping a stage option row in the filter prompt applies that stage filter (equivalent of the digit key) (parent AC5)\n- [ ] Drag-motion events (button code with the 32 motion bit) never cause selection jumps; clicks on non-list areas (header, notification row, metadata panel) are ignored safely (parent constraints + risk mitigation)\n- [ ] Mouse-shaped sequences are fully consumed before the keypress path; unknown/unparsable sequences are inert — no crash, no state corruption; all existing keyboard navigation and chord/shortcut behavior is unchanged; existing `handleKeypress` tests pass (parent AC6)\n\n## Minimal Implementation\n\n- Add a pure row-mapping helper converting 1-based y to a list row (header offset + scrollOffset), a detail scroll position, or a filter option depending on `state.mode` — reuse geometry from `createListRenderer` (header row, listArea, panelHeight)\n- Add `mapMouseToAction(state, mouseEvent, termSize)` returning a dispatchable action (select-row, open-detail, wheel-up/down, scroll-detail, filter-stage, etc.)\n- Dispatch from `onData` before the keypress path; track last-click time/position for the ~400 ms double-click window\n\n## Dependencies\n\n- WL-0MSI720DX002E9WC (test suite)\n- WL-0MSI7278X0008XGM (parser + lifecycle)\n\n## Deliverables\n\n- `packages/herdr/src/worklist.ts` changes (row mapping, mouse dispatch, double-click tracking)\n- Green tests for this slice, including unknown-sequence regression (parent AC6)","effort":"","id":"WL-0MSI72EYW00050N2","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSGHM5BQ0096BNJ","priority":"medium","risk":"","sortIndex":11500,"stage":"plan_complete","status":"blocked","tags":[],"title":"Implement: mouse dispatch and interactions","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-07T00:16:24.660Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Summary\n\nUpdate input-handling documentation, rebuild the herdr package (dist sync), and verify the full project test suite passes — the closing slice for WL-0MSGHM5BQ0096BNJ.\n\n## Acceptance Criteria\n\n- [ ] `docs/dev/pause-when-hidden.md` updated with a note about mouse input (sequences, fail-soft behavior) if the input/refresh loop notes are affected (parent AC7)\n- [ ] README / plugin docs updated where they document selection-list navigation input (parent AC7); no unrelated doc churn\n- [ ] `npm run build` succeeds for the herdr package (tsc) and `packages/herdr/dist/` is rebuilt and in sync with source (parent Key Files)\n- [ ] Full project test suite passes (root `npm test` / vitest) — parent AC8\n- [ ] Manual smoke-run in a supporting terminal (xterm-compatible SGR) confirms: click selects, double-click opens details, wheel scrolls list and detail, filter tap applies; results recorded as a comment on the parent (parent AC1 + AC8)\n\n## Minimal Implementation\n\n- Edit `docs/dev/pause-when-hidden.md` (add mouse-input note referencing the new parser/dispatch)\n- Run `npm run build` in `packages/herdr` and commit rebuilt `dist/` artifacts\n- Run the full vitest suite; triage any failures before completion\n\n## Dependencies\n\n- WL-0MSI72EYW00050N2 (dispatch and interactions must be complete first)\n\n## Deliverables\n\n- Updated docs, rebuilt dist, green full test suite, smoke-run evidence comment","effort":"","id":"WL-0MSI72L3O005BIW5","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSGHM5BQ0096BNJ","priority":"medium","risk":"","sortIndex":11600,"stage":"plan_complete","status":"blocked","tags":[],"title":"Docs, build and verification","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-07T00:16:33.634Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Summary\n\nRecorded as a follow-up opportunity by WL-0MSGHM5BQ0096BNJ (Herdr worklist: mouse and touch support in the selection list): implement drag-to-scroll (touch drag on touch devices / click-drag on desktop terminals) for the selection list and detail view. Explicitly OUT OF SCOPE of the parent; this item exists so the opportunity is not lost.\n\ndiscovered-from:WL-0MSGHM5BQ0096BNJ\n\n## Acceptance Criteria\n\n- [ ] (placeholder — to be refined during intake/planning) Touch drag scrolls the list/detail content proportionally to finger movement; drag-release does not trigger a click\n- [ ] Click-drag on desktop (where supported) scrolls without moving the selection; drag-motion does not jump the selection\n- [ ] Keyboard and click/tap behaviors from WL-0MSGHM5BQ0096BNJ remain unchanged\n- [ ] Full project test suite passes\n\n## Notes\n\n- Depends on the parent's mouse parsing/dispatch foundation (WL-0MSI720DX002E9WC, WL-0MSI7278X0008XGM, WL-0MSI72EYW00050N2)\n- Must respect the drag-motion guard (button 32 motion bit) already specified in the parent","effort":"","id":"WL-0MSI72S0Y0060BRK","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"low","risk":"","sortIndex":10000,"stage":"idea","status":"open","tags":[],"title":"Follow-up: drag-to-scroll in Herdr worklist","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-07T01:57:22.687Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Summary\n\nEnsure the herdr audit-freshness predicate enforces the canonical \"at-or-near\"\nsemantics and is the **single source of truth** shared by both the icon display\nand the downtime (audit-dispatch) worker.\n\nAudit freshness currently lives only in `isAuditFresh()` in\n`packages/herdr/src/icons.ts`. It is the basis for the icon display\n(`auditIcon` in `icons.ts`) AND is the documented freshness gate reused by the\ndowntime dispatch audit tier (`selectAuditCandidate` /\n`getNextAuditCandidate` in `packages/herdr/src/downtime-worker.ts`, which\ntreats `!isAuditFresh(auditedAt, updatedAt)` as \"needs a fresh `/skill:audit`\").\n\nRunning an audit itself bumps the work item's `updatedAt` to within a fraction\nof a second of its `auditedAt` (`wl audit-set` + `wl update --audit-text` +\nthe verdict-driven status transition). A freshness rule that requires a strict\ngap on one side (e.g. only `auditedAt` comfortably ahead of `updatedAt`) can\ntherefore disagree with the just-persisted state. The canonical fix in\nSorraAgents (SA-0MSI3XH34001LLU4) treats an audit as fresh when its\n`auditedAt` is **at or near** the item's `updatedAt` (within a small\ntolerance) because a near-simultaneous `updatedAt` bump is the audit's own\npersistence write.\n\nThis item makes ContextHub's herdr audit-freshness logic follow the same\nat-or-near rule so the icon display and the downtime dispatcher agree, and\nnever disagree on a just-persisted audit.\n\n## Expected behaviour\n\n- `isAuditFresh(auditedAt, updatedAt)` returns true (fresh) when `auditedAt`\n  is at-or-near `updatedAt` within a small, named tolerance (the audit's own\n  persistence write), and false otherwise.\n- The icon (`auditIcon` in `icons.ts`) shows the fresh icon for a\n  just-persisted audit (no longer shows the stale glyph for an audit the run\n  just persisted).\n- The downtime audit candidate selection (`selectAuditCandidate` /\n  `getNextAuditCandidate` in `downtime-worker.ts`) uses the SAME\n  `isAuditFresh` predicate, so a just-persisted fresh audit is NOT a dispatch\n  candidate (no redundant `/skill:audit` re-run).\n- Only one freshness definition exists; neither consumer re-implements its\n  own comparison.\n\n## Acceptance criteria\n\n1. `isAuditFresh` in `icons.ts` treats `auditedAt` at-or-near `updatedAt`\n   (within a documented tolerance constant) as fresh, covering the\n   audit-persistence case (`auditedAt ~ updatedAt`, delta well under the\n   tolerance).\n2. A unit test asserts `isAuditFresh(auditedAt, updatedAt)` is true for a\n   just-persisted audit (auditedAt and updatedAt differ by milliseconds).\n3. A unit test asserts a genuinely stale audit (item updated well after the\n   audit) is still false (stale), unchanged.\n4. `selectAuditCandidate` / `getNextAuditCandidate` in `downtime-worker.ts`\n   continue to use the imported `isAuditFresh` as the sole filter; a\n   just-persisted fresh item is excluded from candidates.\n5. Both consumers import the same helper; no competing freshness comparison\n   is introduced in the dispatch or icon paths.\n\n## Notes / Open questions\n\n- `isAuditFresh` currently hard-codes a magic `60000` ms literal. Prefer\n  naming a constant (e.g. `AUDIT_FRESHNESS_AT_NEAR_TOLERANCE_MS`) and routing\n  both consumers through it.\n- Reconcile the comparison shape of `isAuditFresh` (`auditTime > updateTime -\n  buffer`) with the canonical at-or-near semantics from the SorraAgents fix so\n  both projects express the same intent. Confirm the target semantics with the\n  operator before implementing.\n\n## References\n\n- discovered-from: SA-0MSI3XH34001LLU4 (SorraAgents audit_runner child\n  freshness at-or-near fix) — canonical reference implementation.\n- related-to: WL-0MSI9FK2Q0074C31 (Audit candidate selection),\n  WL-0MSI9WPNI0044R (Audit dispatch tier), parent WL-0MSI8H3HP700K0RG.","effort":"","id":"WL-0MSIAOFI70075REE","issueType":"task","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":4200,"stage":"idea","status":"open","tags":["freshness","audit","herdr"],"title":"Unify audit-freshness at-or-near logic across icon display and downtime dispatcher","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-07T02:56:08.906Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Tests for header version rendering\n\n## Summary\nUnit tests (TDD, written first) for the Herdr work item selection list header version suffix: presence, right-alignment to pane width, dimmed ANSI style, and graceful fallback when no version is available. Expected to FAIL against the current header renderer (no version suffix) and PASS after implementation (F2).\n\n## Acceptance Criteria\n- New/extended tests in `packages/herdr/src/worklist.test.ts` exercise the list-mode header renderer with a version value available.\n- Test asserts the header contains a dimmed `v<version>` suffix rendered with the dim ANSI style (`\\x1b[2m`), matching the `[auto-refresh on]` treatment.\n- Test asserts the version suffix is right-aligned to the pane width (`cols`): visible-width padding computed with ANSI codes stripped before padding.\n- Test asserts graceful fallback when the version cannot be read (suffix omitted or replaced with a safe placeholder), and that existing `'Work Items'` presence assertions still hold.\n- Tests FAIL against the current implementation (no version suffix in header) and PASS after the implementation feature (F2) lands.\n\n## Minimal Implementation\n- Extend the existing header tests in `packages/herdr/src/worklist.test.ts` (lines ~86-100 region) with version-suffix cases.\n- Prefer injecting the version via the renderer's existing testability seam (e.g. a `readFile`-style callback already present in the render signature, or an explicit version parameter) so tests stay hermetic.\n\n## Dependencies\n- None (first item, TDD precursor). The implementation item (F2) is blocked by this item.\n\n## Deliverables\n- Extended `packages/herdr/src/worklist.test.ts` (+ `.meta` if applicable).","effort":"","id":"WL-0MSICS0CQ009AYSQ","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSGKEBFU004T3N3","priority":"medium","risk":"","sortIndex":4300,"stage":"idea","status":"open","tags":[],"title":"Tests for header version rendering","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-07T02:56:12.564Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Implement version read + dimmed right-aligned header display\n\n## Summary\nRead the plugin version at runtime from `packages/herdr/package.json` and render it as a dimmed, right-aligned `v<version>` suffix in the work item selection list header, without disturbing the left-aligned title.\n\n## Acceptance Criteria\n- A runtime helper reads the plugin version from `packages/herdr/package.json` (JSON parse via `readFileSync`, following the `shortcut-config.ts` pattern); when the file is missing or unparseable it falls back gracefully (e.g. `'?'` or omit) without throwing.\n- The list-mode header renders `v<version>` right-aligned to `cols` (visible-width padding computed with ANSI codes stripped before padding), dimmed via `ANSI.dim`, matching the `[auto-refresh on]` treatment; the left-aligned `Work Items — N item(s)` title is unchanged.\n- The version is read at runtime from the manifest, not hardcoded in the renderer (stays in sync with the manifest version).\n- On narrow panes where the suffix would overlap the title, the suffix is dropped or truncated (guard on `cols`) — no layout breakage, no overflow beyond the pane width.\n- Full project test suite passes with the new changes, including the header version tests (F1).\n\n## Minimal Implementation\n- Add a small version-read helper in `packages/herdr/src/worklist.ts` (or a sibling module) using the `readFileSync`/`dirname` pattern from `shortcut-config.ts`, with a safe fallback.\n- In the list-mode header renderer (near line ~1585): compute the header's visible width, pad to `cols`, and append the dimmed `v<version>` suffix right-aligned; reuse `truncateLine`/visible-length logic; guard on `cols` for narrow panes.\n- Keep the renderer testable via the existing `readFile`-style injection seam where practical.\n\n## Dependencies\n- Blocked by: header version rendering tests (F1) — test-first ordering.\n\n## Deliverables\n- Updated `packages/herdr/src/worklist.ts`; version helper; no change to `packages/herdr/package.json` (version source only).","effort":"","id":"WL-0MSICS36B0072K9B","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSGKEBFU004T3N3","priority":"medium","risk":"","sortIndex":11700,"stage":"in_progress","status":"blocked","tags":[],"title":"Implement version read + dimmed right-aligned header display","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-07T02:56:16.029Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Document version display\n\n## Summary\nUpdate the Herdr plugin documentation to describe the dimmed, right-aligned plugin version shown in the work item selection list header.\n\n## Acceptance Criteria\n- `packages/herdr/README.md` documents that the work item selection list header shows the running plugin version (dimmed, right-aligned).\n- Documentation is consistent with the implemented behaviour (F2), including the fallback when the version is unavailable.\n- CHANGELOG.md is NOT modified (managed automatically by the ship skill release pipeline).\n\n## Minimal Implementation\n- Add a bullet under the Features section (and/or a short note in the relevant UI section) of `packages/herdr/README.md` describing the version display.\n\n## Dependencies\n- Blocked by: implementation (F2) — document the implemented behaviour, not the intended one.\n\n## Deliverables\n- Updated `packages/herdr/README.md`.","effort":"","id":"WL-0MSICS5UL0098443","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSGKEBFU004T3N3","priority":"medium","risk":"","sortIndex":11800,"stage":"intake_complete","status":"blocked","tags":[],"title":"Document version display","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-07T12:39:47.266Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Problem statement\n\nSorraAgents is the canonical installer for pi agent guidance: its scripts/install_pi.sh symlinks the global guidance (AGENTS_GLOBAL.md -> ~/.pi/agent/AGENTS.md, plus skills and prompts) into every project. The global AGENTS.md already contains the full agent workflow (work-item lifecycle, implement/build-test-commit order, push policy, triage policy).\n\nContextHub's 'wl init' (src/commands/init.ts) currently has overlapping responsibilities: it prompts for a workflow choice (promptWorkflowChoice: N / B / M, where M is 'Manage custom workflow by editing AGENTS.md directly') and, via ensureWorkflowTemplateInstalled / insertWorkflowLoaderIntoAgents, can inline WORKFLOW content into the project's AGENTS.md. When the SorraAgents global install is present this creates two sources of truth for agent workflow guidance and can conflict with the canonical global file.\n\n## User story\n\nAs a developer running wl init on a machine with the SorraAgents install, I want wl init to delegate agent-guidance/workflow setup to the SorraAgents install so the global guidance is the single source of truth and the project AGENTS.md only carries project-specific content.\n\n## Desired change\n\n- When the SorraAgents global install is detected (i.e. ~/.pi/agent/AGENTS.md resolves to a SorraAgents AGENTS_GLOBAL.md symlink), wl init should NOT inline workflow content into the project AGENTS.md. Instead it should emit/keep the canonical reference structure used by SorraAgents projects:\n  - '## Global agent guidance' section referencing ~/.pi/agent/AGENTS.md\n  - '## Project-specific guidance' section for local rules\n- wl init should either invoke the SorraAgents install (scripts/install_pi.sh) or clearly document that the SorraAgents install is the canonical mechanism, rather than duplicating guidance.\n- The existing pointer-line behavior from 'wl init: prefer global AGENTS.md pointer' (WL-0MLD6N0GW12MNKK0) must be preserved and aligned with the canonical structure above.\n- Standalone Worklog users without the SorraAgents install keep the current workflow options (N/B/M) unchanged.\n\n## Success criteria\n\n1. With the SorraAgents global install present, running wl init in a fresh project does not inline WORKFLOW content into AGENTS.md; the file carries the canonical global-reference structure only.\n2. wl init output/help documents the SorraAgents install as the canonical global-guidance mechanism (delegation), with no duplicated workflow content.\n3. Without the SorraAgents install, existing wl init workflow behavior (N/B/M) is unchanged and existing tests pass.\n4. Tests cover both environments (with and without the global SorraAgents symlink).\n5. Full ContextHub test suite passes.\n\n## Related work\n\n- ContextHub: 'wl init: prefer global AGENTS.md pointer' (WL-0MLD6N0GW12MNKK0, completed) established the pointer-line behavior this item extends.\n- SorraAgents: 'AGENTS.md installer: emit single reference line for new projects' is the complementary installer-side item; the AGENTS.md split (AGENTS_GLOBAL.md canonical source + symlink install) landed in SA-0MSIUUYRD002GC8W.","effort":"","id":"WL-0MSIXMKOX0052514","issueType":"task","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":4400,"stage":"idea","status":"open","tags":[],"title":"Align wl init with SorraAgents install: delegate agent-guidance/workflow setup instead of duplicating","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-07T14:33:20.506Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Herdr selection list: gate background/foreground sync on DB-update check before spawning wl commands\n\n## Problem statement\n\nThe Herdr work-item selection list spawns a burst of `wl` CLI processes on a fixed cadence — background `wl sync` every 60s plus a full refresh (`wl next`, 2× `wl list` mandatory subsets, expanded-children fetches) on every 30s auto-refresh tick — **regardless of whether the worklog database changed**. When the pane is idle (no agents writing, no remote activity), each cycle wastes 3–5 process spawns and a full git sync round-trip for zero visible change.\n\n## Users\n\n- **Herdr users running the selection list with auto-refresh/auto-sync on** — want the list to stay current without constant background `wl` process churn (spawn overhead, git fetch/pull/merge/push, lock contention with other panes).\n- **Multi-pane / multi-agent setups** — the reduction in unnecessary `wl sync` invocations reduces lock-storm pressure between panes and cross-process DB churn.\n\nExample user stories:\n- As a Herdr user with an idle backlog, I want the selection list to avoid spawning any `wl` commands when nothing changed, so my machine isn't churning processes and git round-trips every 30–60s.\n- As a Herdr user, I want background sync to still run at least once per configurable window (default 60s) even when nothing changed locally, so I still receive remote changes pushed by other machines.\n\n## Acceptance Criteria\n\n1. On an auto-refresh tick, when the worklog DB has not changed since the last refresh cycle, the pane spawns **zero** `wl` processes for that tick (no `wl next`, no `wl list`, no children fetches, no `wl sync`, no action-count fetch).\n2. On an auto-sync tick, `wl sync` is skipped when (a) the DB has not changed since the last cycle **and** (b) the last successful sync is fresher than the configurable max-staleness cap (default 60s). If either condition fails, the sync runs (subject to the existing heartbeat / single-flight / `--if-idle` guards).\n3. The max-staleness cap is configurable via the Herdr settings file (e.g. new `maxSyncStalenessMs` setting, default `60000`), clamped to a sane minimum like the sync interval clamp.\n4. Manual user actions — `S` (sync) and `r` (refresh) keypresses — always run, ignoring the gate.\n5. The very first cycle after pane start always runs (no prior \"last seen\" signal), and any failure to read the DB-change signal fails **open** (the cycle runs), so the list can never go stale due to a signal-read error.\n6. The existing cross-instance heartbeat (`last-sync-time`), single-flight, and `--if-idle` guards remain in place for syncs that do run.\n7. Unit tests cover: gate skip/run decisions for refresh and sync ticks (changed/unchanged DB, stale/fresh last-sync, manual override, first-cycle, fail-open), plus the new setting's parse/clamp behavior.\n8. All related documentation is updated to reflect the changes, including code comments, README, and any relevant wiki or docs site entries.\n9. Full project test suite must pass with the new changes.\n\n## Constraints\n\n- The Herdr plugin package (`packages/herdr`) has **zero npm dependencies** and intentionally duplicates small shared helpers per TUI (decision Q2c in WL-0MS8W5LTW006YZ4B) — a small pure helper duplicating the counter-read logic is consistent with this pattern.\n- The DB-change signal must be cheap (a file read or two) and must NOT spawn a `wl` process itself — that is the whole point of the gate.\n- The signal must be cross-process stable: the wl CLI read cache (F2) deliberately avoids SQLite file mtimes because \"the SQLite files are rewritten by the app on every open/close cycle… file-based fingerprints are not stable across processes\" (`src/read-cache.ts`). The gate must not produce false \"changed\" signals from the pane's own reads.\n- Do not change wl CLI sync behavior itself; the incremental-sync work (WL-0MSAKUBKW006FN8Q) is separate scope.\n\n## Existing state\n\n- `packages/herdr/src/worklist.ts` — `runWorklistTui` registers two scheduler tasks: `refresh` (every `refreshIntervalMs`, default 30s, always calls `doRefresh(false)` → spawns read commands) and `sync` (every `syncIntervalMs`, default 60s, calls `doSync(true, heartbeatTtlMs)` then `doRefresh(false)`). Plus a `resume-poll` task that calls `doRefresh(true)` on hidden→visible transition.\n- `packages/herdr/src/auto-sync.ts` — `runSync` spawns `wl sync` fire-and-forget with cross-instance heartbeat skip (`last-sync-time` marker, `isSyncHeartbeatFresh`), single-flight guard, and `--if-idle`.\n- `packages/herdr/src/fetcher.ts` — all read commands (`wl next`, `wl list`, children, action-count) go through `runWl`; the CLI serves many from its on-disk read cache (F2), but a process spawn still occurs per command.\n- `src/read-cache.ts` / `src/read-cache-cli.ts` — the wl CLI maintains a monotonic per-worklog-dir **state counter** (`~/.cache/wl/state/<sha256(worklogDir)>.json`, `{ \"state\": N }`), bumped by every wl write command and by post-sync invalidation (`invalidateCacheForWrite`). This is the natural \"DB changed\" signal: cross-process, stable across reads, and already the CLI's freshness authority.\n- `packages/herdr/src/settings.ts` — typed settings JSON with clamped intervals; `syncIntervalMs` has a 60s minimum clamp.\n- Manual `S`/`r` keypresses dispatch to `doSync()` / `doRefresh(true)` unconditionally.\n\n## Desired change\n\n- Add a small pure module in `packages/herdr` (e.g. `db-change.ts`) that:\n  - resolves the wl cache dir (mirroring `resolveCacheDir()` precedence: `WL_CACHE_DIR` → `$XDG_CACHE_HOME/wl` → `~/.cache/wl`);\n  - reads the state counter for the resolved worklog dir (`state/<sha256(worklogDir)>.json`), returning the counter value (0 when absent); and\n  - exposes a `DbChangeTracker`/helper that remembers the last-seen counter per pane and answers `dbChangedSinceLastCycle()` (fail-open: any read error ⇒ true).\n- Gate the scheduler tasks in `runWorklistTui`:\n  - `refresh` tick: skip (spawn nothing) when the DB is unchanged since the last cycle.\n  - `sync` tick: skip `wl sync` when DB unchanged **and** last successful sync (heartbeat marker) is within the configurable max-staleness cap; otherwise run `doSync` + `doRefresh` as today.\n  - `resume-poll` hidden→visible transition: apply the same DB-change gate (list content is identical when nothing changed).\n  - Manual `S`/`r` paths unchanged (always run).\n- Add the configurable cap to settings (e.g. `maxSyncStalenessMs`, default 60s, clamped ≥1s or aligned with the sync-interval clamp) and pass it through `packages/herdr/src/index.ts`.\n- Keep all existing guards (heartbeat, single-flight, `--if-idle`) for syncs that do run.\n\n## Key Files\n\n- `packages/herdr/src/db-change.ts` — NEW: pure DB-change signal reader (wl cache-dir resolution + state-counter read + last-seen tracker), fail-open.\n- `packages/herdr/src/auto-sync.ts` — expose/adapt staleness helpers (reuse `readSyncHeartbeatMs`) so the sync tick can decide \"DB unchanged AND sync fresh ⇒ skip\".\n- `packages/herdr/src/worklist.ts` — wire the gate into the `refresh` / `sync` / `resume-poll` scheduler tasks; manual `S`/`r` untouched.\n- `packages/herdr/src/settings.ts` — new `maxSyncStalenessMs` setting (default `60000`) with clamping.\n- `packages/herdr/src/index.ts` — pass the new setting into `runWorklistTui`.\n- `src/read-cache.ts` — read-only reference for the counter-file format/path derivation (no changes expected).\n- Tests: `packages/herdr/src/db-change.test.ts` (NEW), plus updates to `auto-sync.test.ts`, `worklist.test.ts`, `settings.test.ts`.\n\n## Related work\n\n- `WL-0MSGAEJQA005QG3W` — Cross-instance sync heartbeat (F3): the `last-sync-time` marker and heartbeat TTL this change composes with. Completed.\n- `WL-0MSAB7ZUC004SK7E` — wl sync lock storm: auto-sync single-flight / `--if-idle` guards; gating reduces spawn pressure further. Completed.\n- `WL-0MSGAEC5N006W5QA` — wl CLI read cache (F2) with the state-counter fingerprint this gate reuses as its signal. Completed.\n- `WL-0MSAKUBKW006FN8Q` — Incremental sync: only export/push changed work items (CLI-side; explicitly out of scope here). Open.\n- `WL-0MS8W5LTW006YZ4B` — Smart-selection duplication decision (Q2c): precedent for duplicating a small pure helper into the Herdr package. Completed.\n- `docs/dev/release-process.md` — release workflow (no code impact; doc reference).\n\n## Risks & assumptions\n\n- **Remote-pull latency (accepted tradeoff, user-confirmed):** skipping `wl sync` when the DB is unchanged delays pulling remote changes until the next sync that does run. Bounded by the max-staleness cap (default 60s) so remote freshness is never worse than the cap.\n- **Signal completeness:** the state counter is bumped by wl CLI writes and post-sync invalidation. External tools writing the SQLite DB directly (not via wl) would not bump it — mitigated by the staleness cap, which forces a periodic sync regardless. Assumed acceptable.\n- **Configurable cap naming/units:** `maxSyncStalenessMs` in ms, default 60000, clamped to ≥1s (aligned with `heartbeatTtlForInterval`'s clamp) — exact name subject to implementer/plan review.\n- **Scope-creep guard:** incremental sync (WL-0MSAKUBKW006FN8Q), event-driven window subscription (WL-0MSHB7DHO004RHBJ), and WAL checkpointing (WL-0MSG8EG7P002MX2I) are tracked separately and must NOT be pulled into this work item.\n- **Behavioral parity:** when the DB IS changing, cadence must match today (no additional latency); only the no-change case adds skipping.\n\n## Appendix: Clarifying questions & answers\n\n- Q1 (asked 2026-08-07): \"If the local DB hasn't changed, should the background `wl sync` also be skipped? That means remote changes pushed by other machines would only be pulled when local activity bumps the counter (or via manual `S`). Acceptable — or should the sync path stay ungated while only the refresh reads are skipped?\" — Answer (user): \"Partially acceptable. wl sync can be skipped if no local DB changes. However, it should not go more than 1 minute (configurable) without being run.\" Source: interactive reply. → Implemented as AC2/AC3: sync is gated by (DB unchanged AND last successful sync within configurable max-staleness cap, default 60s).\n- Q2 (asked 2026-08-07): \"Should manual refresh (`r`) and manual sync (`S`) always run regardless of the check? (My assumption: yes — user-initiated actions always execute.)\" — Answer (user): \"yes\". Source: interactive reply. → Implemented as AC4.\n- Q3 (asked 2026-08-07, /skill:find-related): \"Which work items are related?\" — Research: the find-related script (`find_related.py --work-item-id WL-0MSJ1OLTL009N4IQ`) resolved the wrong worklog store (SorraAgents framework repo) on both runs and returned unrelated items (OpenBrain submission, file-path convention, a-y shortcut). The matches were excluded per the skill's conservative policy (false negatives preferred); the curated Related work section above (verified via `wl show` against the ContextHub store) is authoritative.","effort":"Small","id":"WL-0MSJ1OLTL009N4IQ","issueType":"feature","needsProducerReview":true,"parentId":null,"priority":"medium","risk":"Medium","sortIndex":4500,"stage":"intake_complete","status":"open","tags":[],"title":"Herdr selection list: gate background/foreground sync on DB-update check before spawning wl commands","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-07T14:57:18.817Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Intake Brief: Colour code work items by priority in selection lists (CLI + Herdr)\n\n**Work Item ID:** WL-0MSJ2JFMO007PGQ6\n\n**Headline:** Colour coding of work items in the selection list should be based on **priority**, not stage: critical are red, low are dimmed, medium is white, high is orange — recorded in a shared theme file.\n\n## Problem Statement\n\nWork item titles in selection lists are currently coloured by **stage** (progression: gray → blue → cyan → yellow → green → white, from WL-0MQ53H78W000DQ08). Stage progression communicates workflow position, but operators scanning a list need to see **priority** at a glance — critical items should jump out in red, low-priority items should recede. The colour scheme must be defined once in a shared theme file.\n\n## Users\n\nAll Worklog users viewing work item lists:\n\n- **Operators and agents** scanning `wl list` / `wl search` / `wl recent` need immediate visual cue of what matters (critical = red, high = orange).\n- **Herdr users** browsing the Worklog selection list pane need the same priority cue.\n\nExample user stories:\n- \"As an operator, I want critical work items to appear red so I can immediately spot what needs attention.\"\n- \"As an agent, I want low-priority items dimmed so they recede while scanning the list.\"\n- \"As a project manager, I want a consistent priority colour scheme across the CLI and the Herdr selection list, defined in one shared theme file.\"\n\n## Acceptance Criteria\n\n1. The canonical priority colour mapping is recorded in the shared theme file `src/theme.ts`:\n   - `critical` → red\n   - `high` → orange\n   - `medium` → white\n   - `low` → dimmed\n2. Work item **titles** are coloured by priority and work item **ids** are coloured by stage, everywhere they appear: `wl list`, `wl search`, `wl recent`, `wl show`, `wl next`, and the Herdr worklist selection list (`formatItemLine`).\n3. Blocked items no longer receive the unconditional red override; they are coloured by their natural priority like every other item (per clarification Q3b).\n4. Unknown/missing priority falls back to a sensible default colour (e.g. medium/white) and unknown/missing stage keeps a sensible default (gray) — matching current fallback behaviour.\n5. Unit tests cover the new priority colour mapping and the title/priority + id/stage split in both the CLI helpers (`src/commands/helpers.ts`) and the Herdr icons (`packages/herdr/src/icons.ts`), including blocked and unknown-priority cases.\n6. Full project test suite must pass with the new changes.\n7. All related documentation is updated to reflect the changes, including code comments, README, and any relevant wiki or docs site entries.\n\n## Constraints\n\n- The Herdr plugin (`packages/herdr/`) is a separate package depending only on `@worklog/shared`; it renders raw ANSI 256-colour escape codes and cannot import `src/theme.ts` (which depends on chalk).\n- The Pi-based TUI extension (`packages/tui/`) is **out of scope** — it is slated for removal (see WL-0MSGI7PV9004UD0N) and colours via Pi theme tokens, not the theme file.\n- \"Orange\" should use a colour consistent with the existing `theme.text.readyNo` (`chalk.hex('#FFA500')`).\n- No new dependencies.\n\n## Existing state\n\n- `src/theme.ts` already has a `priority` map (`critical: chalk.redBright`, `high: chalk.yellowBright`, `medium: chalk.blueBright`, `low: chalk.gray`), a `stage` progression map, and a `blocked: chalk.redBright` override.\n- `src/commands/helpers.ts` — `renderTitle()` colours the title by stage (`titleColorForStage`), with a blocked override checked first; `formatTitleAndId()` renders the id muted (`theme.text.muted`); `humanFormatWorkItem()` also renders ids muted in several formats.\n- Herdr: `packages/herdr/src/icons.ts` has `stageColor()` / `applyStageColour()`; `packages/herdr/src/worklist.ts` `formatItemLine()` colours the title by stage and leaves the id uncoloured. `STAGE_COLORS` (worklist.ts) is reused by the stage-filter dialog and separators.\n\n## Desired change\n\n- **`src/theme.ts`:** update `priority` colours to red / orange / white / dim. Keep the `stage` map (now used for ids). Remove the `blocked` override (per Q3b) or leave it unused — implementer decision, but `renderTitle` must no longer apply it.\n- **`src/commands/helpers.ts`:** `renderTitle()` colours by priority (fallback for unknown priority); `formatTitleAndId()` and `humanFormatWorkItem()` colour ids by stage instead of muted. This automatically covers list, search, recent, show, next.\n- **Herdr `packages/herdr/src/icons.ts`:** add `priorityColor(priority)` / `applyPriorityColour(text, priority)` using ANSI 256 codes (red ≈ 196, orange ≈ 208, white ≈ 15, dim ≈ 241). Keep `stageColor` / `applyStageColour` for ids.\n- **Herdr `packages/herdr/src/worklist.ts`:** `formatItemLine()` — title via priority colour, id via stage colour. Keep `STAGE_COLORS` for the stage filter dialog and separators.\n- **Shared theme consideration:** the priority colour *spec* lives in `src/theme.ts`. To prevent drift between the CLI (chalk) and Herdr (ANSI), consider extracting a colour-name spec (e.g. a `packages/shared` module mapping priority → colour name, consumed by both) — at minimum the two maps must stay consistent. Flag as design decision for the implementer; do not over-engineer.\n\n## Key Files:\n\n- `src/theme.ts` — canonical priority colour mapping (red/orange/white/dim); stage map retained for ids\n- `src/commands/helpers.ts` — `renderTitle` (priority colour), `formatTitleAndId` / `humanFormatWorkItem` (stage-coloured ids)\n- `packages/herdr/src/icons.ts` — add `priorityColor` / `applyPriorityColour` (ANSI 256)\n- `packages/herdr/src/worklist.ts` — `formatItemLine` title/priority + id/stage split\n\n## Related work\n\n- **WL-0MQ53H78W000DQ08** (completed, high) — \"Refactor colour mappings: remove status-based colours, use stage progression with blocked override\". Established the current stage-based scheme this item replaces for titles. See `src/theme.ts`, `src/commands/helpers.ts`.\n- **WL-0MQ8LKXH20058N1M** (completed, high) — \"Consistent colour scheme for work item titles in Blessed and Pi TUIs\". Earlier consistency work; Pi TUI out of scope here.\n- **WL-0MP15YIQ200748RC / WL-0MP15YJ4A0031YGR** (completed) — \"Implement colour mapping in TUI and CLI\" and its test item.\n- **WL-0MSGI7PV9004UD0N** (open, high) — \"Remove the Pi-based TUI from the repo; keep the agent-side Pi plugin features\" — Pi TUI excluded from this scope.\n- **WL-0MSI1LVTJ001M9EY** (open, high) — \"Herdr selection list: sort by priority first, then stage\" — sibling priority-related change to the same Herdr worklist.\n\n## Risks & assumptions\n\n- **Assumption:** \"Selection list\" = `wl list` / `wl search` / `wl recent` / `wl show` outputs plus the Herdr worklist pane (confirmed by user: \"both a and b plus the herdr plugin\").\n- **Assumption:** Title = priority colour, id = stage colour, everywhere (confirmed: \"Always use priority in title and stage colouring in id (everywhere they appear)\").\n- **Assumption:** Blocked items show their natural priority colour; the always-red blocked override is removed (confirmed: Q3 answer \"b\").\n- **Risk:** CLI and Herdr colour maps drifting apart → mitigate by keeping both maps in the shared theme spec / one source of truth.\n- **Risk:** ANSI 256 approximations of \"orange\" / \"white\" / \"dim\" in Herdr may differ slightly from chalk's named colours → verify visually against the CLI.\n- **Scope-creep guard:** Pi TUI colouring, sorting changes (WL-0MSI1LVTJ001M9EY), and other Herdr list features are separate work items and must not be expanded into this one.\n\n## Appendix: Clarifying questions & answers\n\n- Q1: \"Scope of 'selection list' — should priority-based colours apply to (a) `wl list` / `wl search` / `wl recent`, (b) `wl show` detail view, (c) both?\" — Answer (user): \"both a and b plus the herdr plugin\". Source: interactive reply.\n- Q2: \"Priority vs Stage relationship — should priority (a) replace stage as primary colour source, (b) supersede only for title colour keeping stage in metadata, (c) only affect list views?\" — Answer (user): \"Always use priority in title and stage colouring in id (everywhere they appear)\". Source: interactive reply.\n- Q3: \"Blocked items — currently `blocked` is a red override that beats stage. Should blocked items (a) still override everything (always red), (b) show their natural priority colour?\" — Answer (user): \"b\". Meaning: blocked items are coloured by their priority like every other item; the unconditional red override in `renderTitle` is removed. Source: interactive reply.","effort":"Small","id":"WL-0MSJ2JFMO007PGQ6","issueType":"task","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"Medium","sortIndex":4600,"stage":"idea","status":"open","tags":[],"title":"Colour code work items by priority in selection lists (CLI + Herdr)","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-07T15:49:15.970Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Intake Brief: Herdr pane title should reflect how it was spawned\n\n## Problem statement\n\nWhen a pi agent pane is spawned from the Herdr worklist, its title is generic\n(\"Pi Agent\" for agent commands, \"Command Output\" for `!!`/`!` shell commands),\nso the user cannot tell at a glance how each pane was spawned or which work\nitem it belongs to. The downtime dispatcher already names its panes\n(`Downtime plan` / `Downtime intake` / `Downtime audit`), but the manual spawn\npaths do not.\n\n## Users\n\n- **Herdr users running skill/workflow commands** — want pane titles that\n  identify the spawn source (manual vs downtime), the skill invoked, and the\n  target work item, so multi-pane sessions are navigable.\n  - As a user, I trigger `/skill:implement <id>` from the worklist, and the\n    new pane is titled `Manually triggered implement <title> - WL-<id>` so I\n    can find it among other panes.\n  - As a user running a `!!` shell shortcut (e.g. audit approve/reject), the\n    resulting pane identifies the command and work item instead of a generic\n    \"Command Output\".\n  - As a user, I see downtime-dispatched panes in the same descriptive format\n    so all pi panes follow one naming convention.\n\n## Acceptance Criteria\n\n1. Agent-command panes spawned from the worklist (skill shortcuts,\n   `/intake`, `/plan`) get a descriptive title of the form\n   `Manually triggered <skill> <work item title> - <id>`; the skill token is\n   derived from the command (e.g. `/skill:implement` → `implement`).\n2. Free-form `/prompt:` commands produce a title like\n   `Manually triggered prompt <first words…>` (truncated), or a sensible\n   fallback when no prompt text remains.\n3. `!!`/`!` shell-command panes (routed via `run-in-pane.sh`) get a\n   descriptive title instead of the generic \"Command Output\", identifying the\n   command/work item where available.\n4. Downtime-dispatched panes are updated from the short `Downtime <kind>` to\n   the same full format (e.g. `Downtime triggered <skill> <title> - <id>`),\n   keeping one consistent convention across all pi panes.\n5. Title length is bounded (truncated) so pane names stay usable in the Herdr\n   UI; commands without a work item still get a descriptive-but-safe title.\n6. Unit tests cover title construction for each spawn path (manual skill,\n   `/prompt:`, `!!`/`!` shell, downtime), including missing work-item and\n   missing-title cases.\n7. Full project test suite passes with the new changes.\n\n## Constraints\n\n- No change to the interactive `open-pi-agent.sh` pane (keeps \"Pi Agent\").\n- The work-item title must be sourced without adding blocking process spawns\n  in the TUI loop — prefer threading the already-loaded item title from the\n  worklist state into the spawn path.\n- Pane titles must remain safe for herdr pane-rename (no shell metacharacter\n  injection; the rename already runs with quoting).\n\n## Existing state\n\n- `packages/herdr/shared/send-to-pi.sh` supports `--pane-name` (default\n  `Pi Agent`) and renames the pane after spawning pi.\n- Manual agent dispatches go through `buildSendToPiArgs` in\n  `packages/herdr/src/index.ts`, which does **not** pass `--pane-name` →\n  panes are named `Pi Agent`.\n- The downtime worker (`packages/herdr/src/downtime-worker.ts`) uses\n  `buildDowntimePaneArgs` → `--pane-name \"Downtime <kind>\"`.\n- `!!`/`!` shell commands route through `packages/herdr/scripts/run-in-pane.sh`\n  which renames the pane `Command Output` (override via `RUN_IN_PANE_NAME`).\n- `packages/herdr/src/worklist.ts` `resolveAndRouteCommand` has the selected\n  item (with `id` + `title`) at dispatch time, but `onCommand` only forwards\n  `(command, model)`.\n\n## Desired change\n\n- Extend the agent-command spawn path (`buildSendToPiArgs` call site in\n  `index.ts`) to pass `--pane-name` with a title built from a descriptor\n  (`Manually triggered`), the skill name parsed from the command, the work\n  item title, and the work-item ID.\n- Thread the selected item's title from `worklist.ts` into the dispatch\n  (extend the `onCommand` seam or pass a prebuilt title) so no extra `wl`\n  spawn is needed.\n- Add the same `--pane-name` behavior to the `!!`/`!` shell path\n  (`run-in-pane.sh` invocation in `index.ts`), replacing \"Command Output\".\n- Update `buildDowntimePaneArgs` / the downtime spawn to the full format so\n  downtime panes match (e.g. `Downtime triggered <kind> <title> - <id>`).\n- Centralize title construction (descriptor + skill + title + id, with\n  truncation) so all spawn paths share one function.\n\n## Key Files\n\n- `packages/herdr/src/index.ts` — agent/pane dispatch; add `--pane-name` to\n  `buildSendToPiArgs` and the `run-in-pane.sh` spawn.\n- `packages/herdr/src/worklist.ts` — `resolveAndRouteCommand` / `onCommand`\n  seam; forward the selected item's title (or a prebuilt pane title).\n- `packages/herdr/src/downtime-worker.ts` — `buildDowntimePaneArgs` / pane\n  name; adopt the full format (`Downtime triggered …`).\n- `packages/herdr/scripts/run-in-pane.sh` — pane-name plumbing if the title\n  is passed via env/arg.\n- `packages/herdr/src/index.test.ts`, `packages/herdr/src/downtime-worker.test.ts`\n  — tests for the new title construction per spawn path.\n\n## Related work\n\n- `WL-0MSBQUJQX005RAT9` — \"Herdr worklist: track agents spawned per work item\n  and show their status icon\". Related: pane spawn plumbing (pane-id capture)\n  in the same dispatch path.\n- `WL-0MSI8H3HP000K0RG` — \"Allow /skill:audit on complete/in_review items\n  without a valid audit\". Related: downtime audit tier uses the same\n  `buildDowntimePaneArgs` path that will adopt the full title format.\n- `WL-0MSF49FMW009M06K` — downtime worker (dispatch panes open in the\n  resolved worklog root); pane naming originates here.\n\n## Risks & assumptions\n\n- **Assumption:** the work-item title is threaded from the worklist state\n  (already loaded) rather than fetched via `wl show` — avoids extra spawns in\n  the TUI loop. If threading proves invasive, fall back to `wl show` at spawn\n  (documented tradeoff: extra subprocess per dispatch).\n- **Risk:** long work-item titles make pane names unusable — mitigated by\n  truncation (bounded length) in the shared title builder.\n- **Assumption:** `/prompt:` panes use `Manually triggered prompt <first\n  words…>` per user confirmation.\n- **Assumption:** `!!`/`!` panes keep the manual descriptor with the command\n  and work-item context; exact token order to be confirmed during planning.\n- **Scope-creep guard:** interactive `open-pi-agent.sh` naming is explicitly\n  out of scope; any further spawn-source descriptors discovered during\n  implementation should be recorded as linked work items, not folded in.\n\n## Appendix: Clarifying questions & answers\n\n- Q1: \"Which panes should get descriptive titles? (a) Only agent-command\n  panes spawned from skill shortcuts (`/skill:*`, `/intake`, `/plan`,\n  `/prompt:`), (b) also the `!!`/`!` shell-command panes (currently\n  'Command Output'), (c) also the interactive `open-pi-agent.sh` pane\n  (currently 'Pi Agent')?\"\n  - Answer (user, interactive reply): **b** — agent-command panes AND the\n    `!!`/`!` shell-command panes; the interactive open-pi-agent pane is\n    excluded.\n- Q2: \"Should the downtime dispatcher's panes be updated to match the new\n  full format (e.g. `Downtime triggered plan <title> - WL-<id>`), or keep its\n  current short form (`Downtime plan`) while only the manual path gets the\n  descriptive format?\"\n  - Answer (user, interactive reply): **update** — downtime panes adopt the\n    same full format for a single consistent convention.\n- Q3: \"Free-form `/prompt:` commands have no skill name and may have no\n  work-item ID. What should their title be (e.g. `Manually triggered prompt\n  <first words…>`), or should they keep the default 'Pi Agent'?\"\n  - Answer (user, interactive reply): **use the suggested title**\n    (`Manually triggered prompt <first words…>`).\n\n---\n\n## Related work (automated report)\n\n### Related work items\n- **WL-0MSHWHULZ001FL8I** – Detail view ToC for Related Docs (completed)\n- **WL-0MMLXTBCH0F4NV3T** – Migrate GitHub helpers to use throttler (completed)\n- **WL-0MMS4EVN10RG8T47** – Migration & Safety Flow (completed)\n\n### Repository file matches\n- `.worklog/tmp/intake-draft-Herdr-pane-title-should-reflect-how-it-was-spawned-WL-0MSJ4E8UA005KG9Y.md` — matched: already, descriptors, dispatcher, downtime, end (+18 more)\n- `.worklog/worktrees/wl-WL-0MSI4TAT70058921-rename-podcast-editing-tab-to-worklog/packages/herdr/README.md` — matched: already, downtime, end, herdr, how (+14 more)\n- `packages/herdr/README.md` — matched: already, downtime, end, herdr, how (+14 more)","effort":"Small","id":"WL-0MSJ4E8UA005KG9Y","issueType":"task","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"Low","sortIndex":4700,"stage":"idea","status":"open","tags":[],"title":"Herdr pane title should reflect how it was spawned","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-08T00:05:29.863Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Auto-downgrade critical priority when item is marked completed/in_review\n\n## Problem statement\n\nA work item that is marked **completed/in_review** can still carry `critical` priority today. Critical is a signal for urgent *open* work — it drives `wl next` ordering, TUI selection lists, and grouping — so a finished item left at critical competes with genuinely urgent items and makes dashboards misleading. When an item transitions to a terminal state (`status=completed` and/or `stage=in_review`), its priority must not remain `critical`.\n\n## Users\n\n- **Operators and agents** who close out work items.\n  - *As an operator, when I mark a critical item as completed/in_review, I want its priority automatically downgraded to `high`, so that finished work stops competing with genuinely urgent open items.*\n  - *As a developer, I want the automatic downgrade reported in the command output, so that I can verify the change happened.*\n\n## Acceptance Criteria\n\n1. When a work item with `priority=critical` transitions to a terminal state — `status=completed` or `stage=in_review` (via `wl update`) or `status=completed` (via `wl close`) — its priority is automatically downgraded to `high`.\n2. Priorities other than `critical` (`high`, `medium`, `low`) are never modified by a terminal transition.\n3. Command output reports the downgrade: a machine-readable field in `--json` output and a summary line in human-readable output, consistent with the existing `downgradedChildren` reporting in `wl update`.\n4. Unit tests cover: `wl update --status completed`, `wl update --stage in_review`, `wl close`, and no-op cases (non-critical priorities unchanged; non-terminal transitions like `plan_complete` leave `critical` intact).\n5. Full project test suite passes, and `CLI.md` documentation for `wl update` / `wl close` is updated to describe the auto-downgrade behavior.\n\n## Constraints\n\n- Canonical priorities are `critical | high | medium | low` (see `src/validators/priority.ts`); the downgrade target is `high` per operator specification.\n- Only terminal transitions trigger the downgrade — moving `critical` items between non-terminal stages (e.g., `idea` → `plan_complete`) must not touch priority.\n- Must not conflict with the existing child cascade: `cascadePriorityDowngrade` in `packages/shared/src/database.ts` (parent downgrade → children). A completed item is itself terminal, so no child cascade is implied by this rule.\n- Prefer reusing existing reporting patterns from `src/commands/update.ts` over inventing new output formats.\n\n## Existing state\n\n- The database allows any priority with any status/stage; there is no rule preventing `critical` on completed/in_review items today.\n- Terminal transitions are: `wl update <id> --status completed --stage in_review` (`src/commands/update.ts`) and `wl close <id>` (`src/commands/close.ts` sets `status=completed`, `stage=done`).\n- A directly related cascade already exists: `cascadePriorityDowngrade(parentId, newPriority)` in `packages/shared/src/database.ts` (line ~1230) downgrades critical *children* to `high` when a parent drops from `critical`, reported via `downgradedChildren` in `src/commands/update.ts` (lines ~383–408).\n- `wl doctor` validates status/stage combinations (`src/doctor/status-stage-check.ts`) but has no priority-vs-stage rule.\n\n## Desired change\n\n1. Add a small helper (e.g., `downgradeIfTerminal(updatedItem)` or an inline check alongside the existing cascade) that, when an item is transitioning to a terminal state, downgrades `critical` → `high`.\n2. Invoke it from the terminal-transition paths in `src/commands/update.ts` (stage/status changes) and `src/commands/close.ts` (close path). Decide between explicit call sites vs. enforcement inside the shared `update()` — prefer explicit call sites to avoid side effects on internal cascade `update()` calls unless a guard makes it safe.\n3. Report downgraded items in JSON and human-readable output, mirroring the `downgradedChildren` pattern.\n4. Add unit tests and update `CLI.md`.\n\n## Key Files\n\n- `src/commands/update.ts` — terminal transition handling (`--status completed` / `--stage in_review`); add downgrade enforcement and reporting.\n- `src/commands/close.ts` — close path sets `status=completed`; enforce downgrade here too.\n- `packages/shared/src/database.ts` — home of `cascadePriorityDowngrade`; natural location for the new helper (e.g., `downgradeIfTerminal`) if a DB-layer helper is preferred.\n- `src/validators/priority.ts` — canonical priority constants to reuse.\n- `tests/database.test.ts` (and/or `tests/cli/*`) — unit tests for the new behavior.\n- `CLI.md` — document auto-downgrade in `wl update` / `wl close` sections.\n\n## Related work\n\n- `Auto-downgrade critical children when parent priority is reduced (WL-0MS7YCCLV000YJCR)` — completed; the direct precedent: same `critical` → `high` downgrade rule and reporting pattern, but for parent→child cascades.\n- `Add priority normalization to create, update and doctor commands (WL-0MP14WFW6001VE3G)` — completed; priority validation infrastructure (`src/validators/priority.ts`).\n- `Bubble up critical items (WL-0MR1COW20005IO1C)` — completed; critical-priority semantics in `wl next`.\n- `Exclude in-review items from wl next (WL-0ML2TS8I409ALBU6)` — completed; in_review semantics.\n- `Demote completed/in_review parent to plan_complete when a child is added (WL-0MSJL00P5004Y0L6)` — open; sibling rule about terminal-state invariants on parents.\n\n## Risks & assumptions\n\n- **Scope-creep risk:** `wl doctor` detection/fix of legacy `critical` + completed/in_review items is a natural extension but is NOT part of this work item's ACs. If wanted, record as a separate linked work item (`discovered-from:WL-0MSJM4EIV001A0V9`).\n- **Over-enforcement risk:** blanket enforcement inside the shared DB `update()` could interfere with internal calls (e.g., the child cascade calls `db.update()`); guard conditions must be precise or enforcement kept at explicit call sites.\n- **Assumption:** \"completed/in_review\" means terminal transitions — `status=completed` and/or `stage=in_review`, plus `stage=done` via `wl close`.\n- **Assumption:** the downgrade target is `high`, as explicitly specified by the operator.\n\n## Appendix: Clarifying questions & answers\n\n- Q: The request ended with `--priority low` — what does it refer to?\n  - A (assumed, rgardler): `--priority low` sets the priority of this new work item itself (matching `wl create --priority low` CLI syntax), not the downgrade target — the feature spec explicitly downgrades to `high`. Source: interactive reply; interpretation documented here. If wrong, the item priority is a one-line `wl update` fix.\n- No further clarification was needed; the seed context was sufficient to draft the brief (interview skipped per intake skill step 4).","effort":"Small","id":"WL-0MSJM4EIV001A0V9","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"low","risk":"Medium","sortIndex":9900,"stage":"plan_complete","status":"in-progress","tags":[],"title":"Auto-downgrade critical priority when item is marked completed/in_review","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-08T12:16:42.158Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"Add the ability to filter by priority in the Herdr plugin, to complement the existing filter by stage. This adds another key to the filter chord: current (by stage) filters become f-s-* and filter by priority will be f-p-*.","effort":"","id":"WL-0MSKC8QLP009R2I0","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":4800,"stage":"idea","status":"open","tags":[],"title":"Herdr: filter by priority via f-p-* chords (stage filters become f-s-*)","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-08T22:56:06.597Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Summary\n\n`wl sync`'s merge logic can enter a self-sustaining conflict loop that silently corrupts item state. Observed in the field (LP-0MSDRRPV0001TCLX in /home/rgardler/projects/llm): a completed, audit-approved item's local `updatedAt` advanced at every sync (21:36:19Z → 21:53:40Z → 21:57:35Z) and the final sync chose a local `deleted` state (empty `deletedBy`/`deleteReason`) over the remote `open` state — soft-deleting the item while the remote `worklog/data` ref still had it `open`/`plan_complete`. No agent ran `wl delete`; the state arose purely from merge behavior. The item had to be manually restored via `StatusLifecycle`.\n\n## Root-cause hypothesis (verified against src/sync.ts)\n\n1. **Same-timestamp merge bumps `updatedAt` unconditionally.** `mergeSameTimestampItems` (src/sync.ts ~L423) sets `merged.updatedAt = new Date().toISOString()` whenever local and remote have the same `updatedAt` but different content (e.g. a `sortIndex` conflict from concurrent `wl re-sort`, or any divergent field). This bump makes the item *newer* than the remote on the next sync, so the local value keeps winning and the timestamp keeps advancing on every sync — a conflict loop with no real user edit behind it. Under auto-sync / multi-pane / multi-agent use this can repeat indefinitely.\n\n2. **Unattributed soft-delete wins over a live remote item.** `deleteSingle` (node_modules/@worklog/shared/src/database.ts) sets `status: 'deleted'` but leaves `deletedBy`/`deleteReason` empty. In `mergeDifferentTimestampItems`, `status` is resolved by plain timestamp recency: `status='deleted'` is a non-default value, so if the local copy is (artifactually) newer, `status` flips to `deleted` even though `deletedBy`/`deleteReason` are empty — silently soft-deleting an item the remote still considers open. There is no delete-side equivalent of the close-preservation logic that protects `status='completed'`/`stage='done'`.\n\n## Expected behaviour\n\n- Repeated syncs of the same divergent state must converge: once both sides hold the merged item, `updatedAt` must stop advancing and the item must not flip-flop.\n- An unattributed delete marker (status='deleted' with empty deletedBy/deleteReason) must never override a remote item that is open/in-progress/completed. Deletes should only propagate when they are real, attributed deletes (non-empty deletedBy or deleteReason), mirroring the close-preservation model.\n\n## Suggested approach\n\n- In `mergeSameTimestampItems`, only bump `updatedAt` when the merge actually changed a user-visible field (or converge without bumping when the merged result equals one side). Consider whether `sortIndex` conflicts alone should bump at all.\n- Add delete-side protection in `mergeDifferentTimestampItems` (and same-timestamp path): treat `status='deleted'` like `status='completed'` — require the delete to be the newer change AND attributed (deletedBy/deleteReason non-empty) before propagating it; otherwise keep the remote live status.\n- Add regression tests in tests/sync.test.ts reproducing both failure modes.\n\n## Acceptance criteria\n\n- AC1: Regression test: two divergent copies with the same `updatedAt` (differing only in `sortIndex` or one metadata field) synced repeatedly converge — `updatedAt` stops advancing after the merged state is present on both sides (no unbounded bumping).\n- AC2: Regression test: local `status='deleted'` with empty `deletedBy`/`deleteReason` vs remote `status='open'` — merge keeps the remote `open` state (or at minimum never produces a soft-deleted item with no attribution).\n- AC3: Regression test: an *attributed* delete (non-empty `deletedBy`) that is newer than the remote still propagates (delete remains possible when intentional).\n- AC4: Full existing test suite (npm test) passes with no regressions.","effort":"","id":"WL-0MSKZ30SK007K9TO","issueType":"task","needsProducerReview":false,"parentId":null,"priority":"high","risk":"","sortIndex":2700,"stage":"idea","status":"open","tags":[],"title":"Fix sync-conflict loop: same-timestamp merge bumps updatedAt indefinitely and unattributed soft-delete wins over remote open item","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-10T10:33:38.849Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Herdr plugin: activity-gated proxy mode switching (cheap on operator idle, fast on operator command)\n\n## Problem statement\n\nThe llama-proxy runs in two operating modes — **fast** (cloud-backed, 3-slot pool) and **cheap** (2-slot local-first pool). Today mode is switched manually (admin API/UI) or on a fixed time schedule; there is no link between *operator presence* and mode. When an operator is actively working through the herdr plugin, every Pi command should be served by **fast** mode; when the operator leaves (no agent-route Pi commands through the plugin for a configurable window), the proxy should drop to **cheap** mode so the downtime worker's autonomous dispatches consume only the cheap local pool.\n\n## Users\n\n- **Operator running the herdr worklog plugin + llama-proxy** — wants fast responses while actively working and zero-cost cheap mode when away, without touching the mode API by hand.\n\nExample user stories:\n\n- *\"As an operator, when I issue an agent command (/skill:*, /intake, /plan, /prompt:) through the herdr plugin, I want the proxy switched to fast mode immediately so my session is served by the cloud-backed pool.\"*\n- *\"As an operator, when I have issued no agent-route Pi commands through the plugin for 15 minutes, I want the proxy switched to cheap mode (only when it is idle) so the downtime dispatcher's work runs on the cheap local pool.\"*\n\n## Acceptance Criteria\n\n- [ ] **Operator-activity tracking:** the plugin records a timestamp on every **agent-route** command (`/skill:*`, `/intake`, `/plan`, `/prompt:` — the routes that spawn a pi agent pane); shell shortcuts (`!!`/`!`) and plain commands do NOT count as operator activity.\n- [ ] **Cheap switch on idle:** when no operator agent-route command has been routed for ≥ `modeSwitchIdleThresholdMs` (default `900000` = 15 min, configurable) **AND** the proxy reports idle (reusing the existing `/llama/local/status` idle evaluation: llama-server running, no active local query, no model switch, no local lease, all required slots free), the worker POSTs `/admin/set-mode {\"mode\":\"cheap\"}`. A busy proxy (active local query) **delays** the switch (never kills in-flight work).\n- [ ] **Fast switch on command:** when a new operator agent-route command is routed through the plugin, it immediately POSTs `/admin/set-mode {\"mode\":\"fast\"}` (fire-and-forget; a failed switch never blocks or delays the command dispatch — the pane opens regardless, fail-open).\n- [ ] **No redundant switching / no hammering:** the worker skips a switch when the persisted mode already matches the target (track last-known mode; refresh via `GET /admin/mode` on poll). At most one switch per state change; a `409` (mode-switch restart in progress) is treated as a no-op and retried on a later tick.\n- [ ] **Fail-closed everywhere:** endpoint failures, timeouts, network errors, and ambiguous responses yield no switch and never crash or block the plugin. On plugin restart the idle clock resets to \"active now\" (fail-safe: the proxy stays fast until a fresh full idle window passes).\n- [ ] **Configurable via plugin settings** (`~/.config/herdr/worklog-plugin.json`): enabled toggle, idle threshold ms, poll interval ms; settings re-read every tick so changes apply without a plugin restart. The proxy URL is shared with the downtime worker (`downtimeProxyUrl`).\n- [ ] **Coexistence with the proxy's built-in schedule:** the plugin's switches are manual overrides (the API's native semantics, LP-0MSMF25V9002AY1J); the proxy's time-based schedule (cheap 01:00–10:00, fast 10:00–01:00) reclaims control at its next boundary. Documented behavior, not a conflict.\n- [ ] **Testing & validation:** unit tests cover idle-window evaluation, last-command tracking, cheap/fast switch triggers, restart reset, fail-closed paths, and settings clamps. All related documentation is updated to reflect the changes, including code comments, the herdr README, and any relevant wiki or docs site entries. Full project test suite must pass with the new changes.\n\n## Constraints\n\n- Only **agent-route** commands count as operator activity (operator decision Q1).\n- The cheap switch is gated on proxy idle — it must never kill in-flight work (operator decision Q4b).\n- Mode switches never block the TUI or command dispatch: all HTTP calls are fire-and-forget (fast switch) or scheduled task work (cheap switch).\n- Fail-closed at every boundary: no crash, no hammering, no repeated identical POSTs.\n- The seed mentions \"the `/admin/mode` API\": `GET /admin/mode` only *reads* the mode; the switch endpoint is `POST /admin/set-mode`. The brief targets `/admin/set-mode`.\n\n## Existing state\n\n- Mode API + UI exist on the proxy: `GET /admin/mode`, `POST /admin/set-mode` (LP-0MSLMYEEU002IBH6); mode persisted in `proxy/.mode`, defaults to `fast`.\n- Time-based automatic mode switching exists on the proxy (cheap 01:00–10:00, fast 10:00–01:00, LP-0MSM5K4TX004MICX) with manual-override persistence until the next scheduled change (LP-0MSMF25V9002AY1J). A mode switch restarts the proxy (terminates in-flight requests; clients retry — documented accepted behavior).\n- The herdr plugin already has: settings system (`~/.config/herdr/worklog-plugin.json`, re-read per tick), a consolidated scheduler (worklist.ts), and a downtime worker that polls `/llama/local/status` with fail-closed idle evaluation (WL-0MSF49FMW009M06K + children). The mode-switch worker reuses this infrastructure.\n\n## Desired change\n\nAdd a mode-switch worker to the herdr plugin:\n\n1. `index.ts` `onCommand` (agent route) records `lastOperatorCommandAt = Date.now()` and fires the fast switch (`POST /admin/set-mode {\"mode\":\"fast\"}`, fire-and-forget, fail-open).\n2. A new scheduler task (pattern-matched on the existing `downtime` task) ticks on `modeSwitchPollIntervalMs`: if `lastOperatorCommandAt` is null/older than the threshold AND the proxy is idle (reuse the local-status poller/idle evaluation), POST `/admin/set-mode {\"mode\":\"cheap\"}` — skipping when the persisted mode already is cheap.\n3. New settings: `modeSwitchEnabled`, `modeSwitchIdleThresholdMs`, `modeSwitchPollIntervalMs` (reusing `downtimeProxyUrl`); re-read each tick.\n4. Restart behavior: worker construction sets `lastOperatorCommandAt = Date.now()` (reset to active).\n\n## Key Files (predicted)\n\n- `packages/herdr/src/mode-switch-worker.ts` — new worker: idle-window evaluation, mode-state tracking, `/admin/set-mode` HTTP client (POST) + `GET /admin/mode` read, fail-closed\n- `packages/herdr/src/mode-switch-worker.test.ts` — unit tests (test-first)\n- `packages/herdr/src/settings.ts` — new settings keys + clamps + defaults\n- `packages/herdr/src/worklist.ts` — scheduler task wiring + TUI options plumbing\n- `packages/herdr/src/index.ts` — agent-route hook: record last-command timestamp + fire fast switch\n- `packages/herdr/README.md` — settings and behavior documentation\n\n## Related work\n\n- **WL-0MSF49FMW009M06K** — \"Herdr to consume local LLM in downtime\" (parent of the downtime worker; the poller/settings/scheduler infrastructure this feature reuses)\n- **LP-0MSLMYEEU002IBH6** (llm-manager) — Web API/UI to toggle fast/cheap modes (the `/admin/mode` + `/admin/set-mode` endpoints)\n- **LP-0MSM5K4TX004MICX** (llm-manager) — Time-based automatic mode switching (built-in schedule)\n- **LP-0MSMF25V9002AY1J** (llm-manager) — API mode override persists until the next scheduled change\n- **WL-0MSMIPPJI007GU9N / LP-0MSMZOAJW002UR2A** (llm-manager) — cheap-mode profile definition (2-slot pool)\n- find-related automated report: WL-0MRCTZZ82000X7TM, WL-0MP15AMKV002F3U4, WL-0MS6AAW3M004JK9U (weak keyword matches; not design-relevant)\n\n## Risks & assumptions\n\n- **Mode switch restarts the proxy** (kills in-flight requests). Mitigated for the cheap direction by the idle gate (Q4b); the fast direction can still kill a cheap-mode downtime request when the operator returns — accepted (documented proxy behavior; the downtime dispatch's work retries on the next idle period).\n- **Multiple plugin panes** could each run a mode-switch worker → duplicate POSTs. Same-mode POSTs are API noops and the worker skips already-matching modes, so the system converges; per-process single-flight is applied.\n- **Schedule reassertion at boundaries** may flip the mode shortly after a plugin switch near a boundary — accepted; every new operator command re-switches to fast immediately (design intent).\n- **`GET /admin/mode`/`POST /admin/set-mode` may not be reachable** from the plugin host — fail-closed (no switch), never crash; the plugin logs a warning.\n- **Scope-creep risk:** related but out-of-scope opportunities (e.g., reflecting the active mode in the herdr UI, per-slot idle gating for the cheap switch, or activity-based mode tracking for non-plugin pi panes) are recorded as linked work items, not scope.\n- **Assumption:** \"operator-issued Pi command\" ≡ agent-route commands routed through the herdr plugin (Q1). Commands issued directly in a pi pane (not via the plugin) are out of scope.\n- **Assumption:** the existing idle evaluation (`evaluateIdle` with `requiredFreeSlots = 0`, all slots free) is the busy/idle gate for the cheap switch; if per-slot data later arrives (WL-0MSG7P9N8009PCKG), the gate follows the same semantics.\n\n## Appendix: Clarifying questions & answers\n\n- Q1: \"Should only the agent-route commands (/skill:*, /intake, /plan, /prompt: → new pi pane) count as 'operator Pi commands' for the idle timer, or any command routed through the plugin?\" — Answer (operator): \"only agent-route\". Source: interactive reply. Research: `packages/herdr/src/index.ts` `routeCommand`/`isAgentCommand` distinguishes agent vs pane vs stdout routes.\n- Q2: \"Should the herdr plugin's activity-based switching (a) coexist as manual overrides — the schedule eventually reclaims control — or (b) fully own the mode while enabled?\" — Answer (operator): clarified intent: \"if the operator issues an agent-command it is *always* processed as 'fast', but if the operator leaves and the downtime dispatcher starts sending commands they are 'cheap'.\" Interpretation recorded: plugin switches are immediate manual overrides (fast on command, cheap on idle); the proxy's time schedule remains as a background fallback (coexistence, option a). Source: interactive reply.\n- Q3: \"After a plugin restart, should the idle clock (a) reset to 'active now' — stay fast until a fresh full idle window — or (b) treat restart as idle and switch to cheap immediately?\" — Answer (operator): \"a\". Source: interactive reply.\n- Q4: \"Cheap-switch gating vs proxy busy state: (a) switch at the threshold unconditionally (can kill in-flight downtime dispatch), or (b) switch only when the proxy also reports idle?\" — Answer (operator): \"b\". Source: interactive reply. Research: `packages/herdr/src/downtime-worker.ts` `evaluateIdle`/`isIdleStatus`; `llm-manager/proxy/proxy/handlers.py` `/admin/set-mode` restart semantics.\n\n## Related work (automated report)\n\n### Related work items\n- **WL-0MRCTZZ82000X7TM** – RCA: wl sync pre-push hook destroyed Tableau-Card-Engine repository (completed)\n- **WL-0MP15AMKV002F3U4** – Event emitter and UI hook API (completed)\n- **WL-0MS6AAW3M004JK9U** – Reselect item after list reorder from status change (completed)\n\n### Repository file matches\n- `packages/herdr/README.md` — matched: api, auto, command, commands, configurable, fast, herdr, immediately, intent, last, minutes, mode, new, operator, passed, plugin, proxy, sent, switch, using\n- `.worklog/worktrees/wl-WL-0MSI4TAT70058921-rename-podcast-editing-tab-to-worklog/packages/herdr/README.md` — matched: api, auto, command, commands, configurable, fast, herdr, immediately, last, minutes, mode, new, operator, passed, plugin, proxy, sent, switch, using\n- `.worklog/worktrees/wl-WL-0MSGAEC5N006W5QA-wire-read-commands-through-cache-spawn-i/packages/herdr/README.md` — matched: auto, command, commands, configurable, herdr, immediately, last, minutes, mode, new, operator, passed, plugin, proxy, sent, switch, using","effort":"Small","id":"WL-0MSN3FWV5008KQE9","issueType":"feature","needsProducerReview":true,"parentId":null,"priority":"medium","risk":"Medium","sortIndex":2000,"stage":"in_review","status":"in-progress","tags":[],"title":"Herdr plugin: activity-gated proxy mode switching (cheap on operator idle, fast on operator command)","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-10T17:26:29.118Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"When the herdr plugin has lots of content the info line at the top can scroll off the screen. Prevent this happening","effort":"","id":"WL-0MSNI6TQ5003JY1Z","issueType":"","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":4900,"stage":"idea","status":"open","tags":[],"title":"Prevent top line info bar from scrolling off screen","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-10T17:46:56.611Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Compress the space used in herdr plugin meta-data\n\n## Problem Statement\n\nThe metadata section in the herdr plugin's list-mode metadata panel and detail view\noccupies too much vertical space. Key related fields are displayed on separate rows\nwhen they could be compacted, wasting limited terminal real estate.\n\n## Users\n\n- **Terminal users viewing work items** — want to see more metadata at a glance without\n  scrolling through verbose, line-by-line display.\n- **Users with limited terminal height** — benefit from denser metadata presentation.\n\n## Acceptance Criteria\n\n1. **Status + Stage** are displayed on the same row, e.g. `Status+Stage: 🔄 in_progress / 🛠️ in_progress` (or similar compact format).\n2. **Priority + Type** are displayed on the same row, e.g. `Priority+Type: ⭐ high / feature`.\n3. **Created + Updated** are displayed on the same row, e.g. `Created+Updated: 01/08/26 10:00 / 02/08/26 10:00`.\n4. **Audit + Audited At** are displayed on the same row, e.g. `Audit+AuditedAt: ✅ ready to close / 03/08/26 10:00`.\n5. The detail view metadata table is also compressed consistently (the same field pairings).\n6. All other metadata rows (ID, Title, Risk, Effort, Children, Parent, Tags, GitHub Issue, Reviewed) remain unchanged.\n7. Full project test suite passes with changes.\n\n## Constraints\n\n- Existing field labels and icon behavior must be preserved (no changes to icon mapping).\n- The compact format should be readable — avoid over-compression that harms scannability.\n- No changes to the `WorkItem` type or data model — purely a display change.\n- `noIcons` mode (plain text) must also be handled correctly for compacted rows.\n\n## Existing State\n\n- `buildMetaRows()` in `packages/herdr/src/worklist.ts` returns an array of `[label, value]` pairs, one per row.\n- `formatMetadataPanel()` renders each pair as a separate line: `  <label padded> <value>`.\n- `formatDetailContent()` similarly renders the detail view table.\n- Key files:\n  - `packages/herdr/src/worklist.ts` — `buildMetaRows()`, `formatMetadataPanel()`, `formatDetailContent()`\n  - `packages/herdr/src/worklist.test.ts` — tests for metadata row building and formatting\n\n## Desired Change\n\nIntroduce paired-row rendering for the four specified field pairs. Options considered:\n\n1. **Change `buildMetaRows` to emit compound rows** — modify the data structure to return\n   pairs-of-pairs for the four groupings, then update rendering.\n2. **Keep `buildMetaRows` unchanged, introduce a post-processing step** — leave the row\n   data intact but compress during rendering in `formatMetadataPanel` and `formatDetailContent`.\n\n**Recommendation:** Option 2 — less invasive, keeps `buildMetaRows` as the single source\nof truth for field data, and localizes the rendering logic change.\n\n## Related Work\n\n- WL-0MSGIXHHI009KFW9 — Icon + text metadata values (ensures icon handling is consistent).\n- WL-0MSQ44MDX008U69J — Dynamic metadata panel sizing.\n- WL-0MSEPP0MB009K9PR — Responsive metadata panel layout implementation.\n- WL-0MSHIJR7T007Q9R7 — Herdr metadata panel: drop duplicate ID row from data section.\n","effort":"Small","id":"WL-0MSNIX4V60012266","issueType":"task","needsProducerReview":false,"parentId":null,"priority":"high","risk":"Medium","sortIndex":2800,"stage":"intake_complete","status":"open","tags":[],"title":"Compress the space used in herdr plugin meta-data","updatedAt":"2026-08-23T12:36:38.588Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-10T17:51:06.177Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Ensure Qwen is Preserving Thinking\n\n## Problem Statement\n\nVerify that the Qwen model configuration has the preserve-thinking feature enabled and confirm that proxy modifications do not break the thinking output.\n\n## Users\n\n- **AI Agent (Map)**: Needs reasoning/thinking output to be visible and preserved in the model responses for better task completion.\n- **Operator**: Needs transparent reasoning from Qwen models for auditability and debugging.\n\n## Acceptance Criteria\n\n1. Qwen model entries in the model configuration have `thinkingFormat` set to an appropriate value (e.g., `\"qwen\"` or `\"qwen-chat-template\"`) to enable preserve-thinking.\n2. The proxy configuration does not strip or suppress `<think>` blocks or reasoning content from Qwen model responses.\n3. All Qwen model variants (qwen3.5-plus, qwen3.6-plus, qwen3.7-max, qwen3.7-plus, qwen3.8-plus) are reviewed for consistent thinking support.\n4. Testing confirms that when a Qwen model is used with a thinking level set, the thinking content appears in the session transcript.\n\n## Constraints\n\n- Changes must be limited to model configuration (models.json / models-store.json) and proxy configuration review.\n- No changes to model serving or inference code.\n\n## Existing State\n\n- `opencode-go` provider has `qwen3.6-plus` with `\"thinkingFormat\": \"qwen\"` already configured.\n- `opencode` provider's `qwen3.5-plus` and `qwen3.6-plus` use `api: \"anthropic-messages\"` without `thinkingFormat` set.\n- Other Qwen variants (qwen3.7-max, qwen3.7-plus, qwen3.8-max) on `opencode-go` have no `thinkingFormat` configured.\n\n## Desired Change\n\n1. Ensure all Qwen model entries have appropriate `thinkingFormat` values configured.\n2. Review proxy configuration to confirm thinking content is not being stripped.\n3. Document findings and any required configuration changes.\n\n## Key Files (predicted)\n\n- `~/.pi/agent/models-store.json` — Model configuration with Qwen entries\n- `~/.pi/agent/models.json` — Live model configuration\n- Pi agent source code for thinking format handling (`@earendil-works/pi-coding-agent`)\n\n## Related Work\n\n- WL-0MSNJ2HFL007OQJC — This item (ensure Qwen is preserving thinking)\n\n## Risks & Assumptions\n\n- **Assumption**: The proxy layer (llm-manager) correctly passes through thinking content when configured.\n- **Risk**: Adding `thinkingFormat` to models that don't support it may cause API errors.\n- **Risk**: Proxy modifications may have been made in the past that strip thinking content — requires verification.\n- **Scope creep**: This task is investigation-focused; implementation of fixes is a follow-up task.\n","effort":"Extra Small","id":"WL-0MSNJ2HFL007OQJC","issueType":"task","needsProducerReview":true,"parentId":null,"priority":"high","risk":"Low","sortIndex":2900,"stage":"plan_complete","status":"open","tags":[],"title":"ensure Qwen is preserving thinking","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-11T10:53:45.295Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"Sometimes work items get left stuck in in_progress. Every 30 seconds the herdr plugin should retrieve all in_progress work items and attempt to match an active pane to the work item. If none is found then set to either completed, blocked or open. This work item should be blocked on the one to add work item id to pane titles, which can then be used to match panes. All tabs in the current workspace should be searched.","effort":"","id":"WL-0MSOJLMFJ005Q8IM","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":5000,"stage":"idea","status":"open","tags":[],"title":"Herdr hydrator: rescue in_progress work items with no active pane","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-11T10:54:02.062Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Intake Brief: Herdr hydrator — rescue in_progress work items with no active pane\n\n## Headline\n\nAdd a hydration task to the herdr worklist plugin that, every 30 seconds, fetches all `in_progress` work items, matches each against the work-item IDs embedded in active pane titles across all tabs of the current workspace (via the pane-title work WL-0MSJ4E8UA005KG9Y), and demotes any item with no matching pane out of `in_progress` to `completed` (stage `in_review`), `blocked` (dependency-blocked), or `open` (otherwise). The check also runs immediately when the tab regains focus.\n\n## Problem statement\n\nWork items are sometimes left stuck in `status=in_progress` even though no agent pane is actually working on them (e.g. the agent pane was closed, the session died, or the item was claimed and abandoned). There is currently no automatic mechanism to detect this, so stale in-progress items linger at the top of work queues and are never re-selected.\n\n## Users\n\n- **Producers / operators** — want a self-healing work queue where an item only stays `in_progress` while an active agent pane is genuinely attached to it.\n  - As a producer, I want items whose agent pane has died to be demoted automatically so they re-enter the selection pool instead of rotting in `in_progress`.\n- **Herdr worklist users** — benefit from a queue that reflects reality without manual cleanup.\n  - As a user, I want the worklist to keep `in_progress` items only when a matching pane exists, so the list stays honest.\n\n## Acceptance Criteria\n\n1. Every 30 seconds the hydrator fetches all `in_progress` work items (`wl list --status in_progress`).\n2. For each item, the hydrator attempts to match an **active pane**: a pane, in **any tab of the current workspace**, whose pane title contains the work item's ID (`WL-…`) and that is still running (present in `herdr pane list`). Matching relies on the pane-title format introduced by the blocking work item (e.g. `Manually triggered implement <title> - WL-<id>`).\n3. Any `in_progress` item with **no matching active pane** is demoted:\n   - stage `in_review` → `status=completed`\n   - item has an active outbound dependency blocker (target not `completed`/`deleted`, not stage `in_review`/`done`) → `status=blocked`\n   - otherwise → `status=open`\n   Transitions respect the project's status/stage compatibility rules (e.g. `blocked` only allows stages `idea`/`intake_complete`/`plan_complete`, so a stage `in_progress` item demoted to `blocked` must also have its stage demoted).\n4. When the worklist tab regains focus, the hydration check runs **immediately** (not waiting for the next 30s tick), reusing the existing visibility resume-poll mechanism.\n5. The hydrator is **safe**: no transition is made for an item whose matching pane is found; the task is single-flight; failures (CLI missing, unparseable output) are logged and never crash the plugin; no `wl` spawns happen while the worklist pane is hidden.\n6. Unit tests cover: pane-title ID extraction, active-pane matching across tabs, each demotion decision (in_review→completed, dependency-blocked→blocked, else→open), stage/status compatibility handling, focus-resume immediate run, and fail-open error paths.\n7. Full project test suite passes with the new changes.\n\n## Constraints\n\n- **Blocked on WL-0MSJ4E8UA005KG9Y** (\"Herdr pane title should reflect how it was spawned\") — the hydrator matches panes by the work-item ID embedded in pane titles; it must not be implemented before that title format lands. (The in-progress duplicate WL-0MSJ4E5BM000LE3D carries the same work; block on the intake-complete one.)\n- The 30s loop runs inside the worklist pane process (like the downtime worker) — acceptable per the operator; the focus-resume immediate check is required.\n- \"Active pane\" = any running pane whose title contains the work-item ID, across **all tabs** in the current workspace; focus/zoom state does not matter (matching is by title presence + pane existence, not focus).\n- Reuse the existing `TaskScheduler` (packages/herdr/src/scheduler.ts) and the visibility `PollGate`/resume-poll machinery in `worklist.ts`; do not add a second interval loop.\n- Fail-open at every boundary: never crash the plugin, never demote on ambiguous evidence (missing pane list, unparseable output → treat as \"no match\" only when the CLI output is actually trustworthy).\n- No schema changes; pure status/stage updates via the `wl` CLI.\n\n## Existing state\n\n- `packages/herdr/src/worklist.ts` — the worklist TUI registers a single `TaskScheduler` with tasks: `refresh` (auto-refresh), `sync`, `resume-poll` (hidden→visible immediate refresh), and `downtime` (downtime-worker tick, 30s default). The hydrator would be a sibling task.\n- `packages/herdr/src/scheduler.ts` — `TaskScheduler` with 1s base tick, per-task `intervalMs`, `singleFlight`, and `runTimeoutMs` watchdog; a rejecting task never takes down the loop.\n- `packages/herdr/src/visibility.ts` — `isPaneVisible()` + `PollGate` (TTL memoizer); tab focus via `herdr tab get <id>` is the visibility signal; resume-poll uses it to detect hidden→visible.\n- `packages/herdr/src/agent-tracker.ts` — existing work-item ↔ pane association file (`.worklog/agent-panes.json`) and `herdr agent list` parsing; a possible secondary signal but the hydrator matches by pane-title per the operator's decision.\n- `packages/herdr/src/fetcher.ts` — `runWl(...)`, `extractJson`, `extractItems` helpers for wl CLI JSON.\n- `packages/herdr/scripts/toggle.sh` — precedent for `herdr pane list` JSON parsing (panes with `label`/`pane_id` across the workspace).\n- `src/commands/dep.ts` — `wl dep list <id>` returns `outbound` edges with target `id`/`status`/`priority`; `packages/shared/src/database.ts:isDependencyActive` defines an active blocker (target not `completed`/`deleted`, not stage `in_review`/`done`).\n- `src/config.ts` — status/stage compatibility: `in-progress` allows `intake_complete`/`plan_complete`/`in_progress`; `blocked` allows `idea`/`intake_complete`/`plan_complete`; `completed` allows `in_review`/`done`; `open` allows `idea`/`intake_complete`/`plan_complete`/`in_progress`.\n\n## Desired change\n\n1. Add a hydrator module (e.g. `packages/herdr/src/hydrator.ts`):\n   - `extractWorkItemIdsFromPaneTitles(panes)` — parse `WL-[0-9A-Z]+` from pane titles.\n   - `isActivePaneMatch(itemId, panes)` — item ID present in any running pane title across all tabs.\n   - `decideDemotion(item)` — pure decision: `in_review` → `completed`; dependency-blocked → `blocked`; else → `open` (with stage/status compatibility handling).\n   - `runHydrationOnce(deps)` — fetch `wl list --status in_progress`, fetch `herdr pane list`, match, apply demotions via `wl update`.\n2. Register the hydrator in `worklist.ts` as a scheduler task (`id: 'hydrate'`, `intervalMs: 30_000`, `singleFlight: true`, `runTimeoutMs` set) that is visibility-gated (skip while hidden, zero wl spawns when hidden).\n3. Hook the hidden→visible transition: when the resume-poll detects visibility, run the hydration check immediately in addition to the existing refresh.\n4. Add unit tests (see AC6) mirroring the existing downtime-worker test harness (`packages/herdr/src/downtime-worker.test.ts`, injectable exec seam from `fetcher.ts`).\n\n## Key Files (predicted)\n\n- `packages/herdr/src/hydrator.ts` — new module: ID extraction, pane matching, demotion decision, orchestration (the bulk of the work).\n- `packages/herdr/src/worklist.ts` — register the `hydrate` scheduler task; trigger immediate check on focus-resume.\n- `packages/herdr/src/scheduler.ts` — reused as-is (no changes expected).\n- `packages/herdr/src/fetcher.ts` — reused `runWl`/`extractJson`/`extractItems` helpers.\n- `packages/herdr/src/visibility.ts` — reused `PollGate`/resume-poll machinery (no changes expected).\n- `packages/herdr/src/hydrator.test.ts` — new test suite (AC6).\n- `src/config.ts` — reference only: status/stage compatibility rules that constrain demotion.\n- `src/commands/dep.ts` / `packages/shared/src/database.ts` — reference only: dependency-blocked semantics (`isDependencyActive`).\n\n## Related work\n\n- **WL-0MSJ4E8UA005KG9Y** — \"Herdr pane title should reflect how it was spawned\" (open, intake_complete) — **BLOCKING**: introduces the pane-title format containing the work-item ID that the hydrator matches on. In-progress duplicate: **WL-0MSJ4E5BM000LE3D** (same title).\n- **WL-0MSBQUJQX005RAT9** — \"Herdr worklist: track agents spawned per work item and show their status icon\" (in_review) — pane↔work-item association machinery; `agent-panes.json` is a candidate secondary signal.\n- **WL-0MS9NPHQU005Y3VE** — \"Herdr plugin should set work-item status to in_progress when instigating actions\" (completed) — the counter-direction: sets `in_progress` on dispatch; the hydrator demotes when no pane remains.\n- **WL-0MSF49FMW009M06K** — \"Herdr to consume local LLM in downtime\" (completed) — downtime worker: the 30s scheduler-task precedent the hydrator mirrors.\n- **WL-0MSJNJPRM009RM35** — \"Gate worklist auto-refresh on tab focus so a visible pane in a multi-pane tab keeps refreshing\" (in_progress) — visibility-gating precedent reused by the hydrator.\n- **WL-0MSB1N0HB0007N6N** — \"Pi extension polls wl list / wl next even when selection list is not visible\" (completed) — related zero-spawn-when-hidden concern.\n\n## Risks & assumptions\n\n- **Assumption:** pane titles will contain the work-item ID once WL-0MSJ4E8UA005KG9Y lands — the hydrator is blocked on that and must not guess other signals.\n- **Risk:** demoting an item the operator considers genuinely in-progress (pane title missing/renamed by hand, pane in a different workspace). Mitigation: matching is by title + pane existence across all tabs; the 30s cadence + focus-resume means demotions are re-visible quickly; keep the demotion decision pure and well-tested.\n- **Risk:** status/stage compatibility rejects a demotion (e.g. `blocked` with stage `in_progress`). Mitigation: `decideDemotion` returns a compatible (status, stage) pair; transitions that fail validation are logged, not retried blindly.\n- **Risk:** `herdr pane list` output shape differs (tabs vs panes). Mitigation: fail-open parsing like toggle.sh/agent-tracker; treat unparseable output as \"no demotion this cycle\".\n- **Risk:** scope creep (agent-panes.json matching, notifications, configurable intervals). Mitigation: record as linked work items; this intake covers pane-title matching only.\n- **Scope-creep guard:** secondary signals (agent-panes.json, `herdr agent list` states) are explicitly out of scope for the initial implementation.\n\n## Appendix: Clarifying questions & answers\n\n- Q: \"When no active pane matches an in_progress item, HOW should the hydrator choose between completed / blocked / open? (a) Signal-based: stage in_review → completed; blocked-by dependencies → blocked; otherwise → open; (b) always open; (c) operator picks per item; (d) other\" — Answer (user): **a** — signal-based. Source: interactive reply. Final: yes.\n- Q: \"The herdr plugin's 30s scheduler loop runs inside the worklist pane process (like the downtime worker). That means the hydrator only runs while a worklist pane is open. Acceptable, or should it run standalone?\" — Answer (user): **acceptable**, ensure the check runs immediately when the tab gets focus. Source: interactive reply. Final: yes.\n- Q: \"Does 'active pane' mean any pane whose title contains the work-item ID and is still running (regardless of focus/zoom), across all tabs in the current workspace?\" — Answer (user): **yes**. Source: interactive reply. Final: yes.\n## Related work (automated report)\n\n### Related work items\n- **WL-0MQFIYPZK00680H1** – wl next should return parent items instead of surfacing children individually (completed)\n- **WL-0MSBQUJQX005RAT9** – Herdr worklist: track agents spawned per work item and show their status icon (completed)\n- **WL-0MSC0O8HN000EBFH** – Herdr plugin: free-form prompt shortcut opens new pi pane (option B) (completed)\n\n### Repository file matches\n- `.worklog/tmp/intake-draft-Herdr-hydrator-rescue-in-progress-work-items-with-no-active-pane-WL-0MSOJLZD9004P8PI.md` — matched: 000, 0ms9nphqu005y3ve, 0msb1n0hb0007n6n, 0msbqujqx005rat9, 0msf49fmw009m06k (+409 more)\n- `.worklog/worktrees/wl-WL-0MSOKOI4J0046GE5-remove-stray-compiled-artifacts-committe/packages/herdr/README.md` — matched: 000, 0msb1n0hb0007n6n, 0msf49fmw009m06k, 0msjnjprm009rm35, 30s (+272 more)\n- `packages/herdr/README.md` — matched: 000, 0msb1n0hb0007n6n, 0msf49fmw009m06k, 0msjnjprm009rm35, 30s (+272 more)","effort":"Small","id":"WL-0MSOJLZD9004P8PI","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"High","sortIndex":11900,"stage":"intake_complete","status":"blocked","tags":[],"title":"Herdr hydrator: rescue in_progress work items with no active pane","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-11T18:02:30.101Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Intake Brief: Sync — Regression Guard\n\n## Problem Statement\n\nWhen `wl sync` merges remote state that is newer than local, it can silently import regressions: fields the local store had set to non-default values (e.g. `effort: 'Small'`, `status: completed`) get overwritten with defaults by a stale/divergent second store, as observed Aug 6–10, 2026 on the llm worklog ref.\n\n## Users\n\n- **Primary**: Operators of multi-store worklog setups — anyone running more than one `wl sync` instance against the same remote.\n- **Secondary**: Producers/reviewers whose audit results (status/stage transitions) are silently reverted by stale syncs.\n\n## Acceptance Criteria\n\n1. During merge, for each incoming remote item with a newer `updatedAt` than local, detect regressions where a field moves from a non-default value to its default on the fields that participate in the merge: `status`/`stage` (non-default → default, e.g. `completed/in_review` → `open/idea`), `priority` (non-default → default), and `assignee` (set → unset).\n\n   **`effort`/`risk` (producer resolution, 2026-08-23): stay PERMANENTLY EXCLUDED from the merge field lists** — ping-pong prevention by construction. A remote `effort: ''` / `risk: ''` never enters the merge; local non-default values are always kept. The guard additionally REPORTS any attempted effort/risk regression (remote non-empty → default) as a protected conflict (local kept, nothing imported) for observability, but no override path is needed for these two fields since they cannot be imported through the merge.\n2. On detecting a regression, `wl sync` refuses to import that item's regressed fields: reports the conflict (item id, field, old local value, new remote default value) and keeps the local value. An explicit override flag (`--accept-regressions` or equivalent) allows the operator to accept the regression.\n3. Genuine concurrent edits (both stores changed different non-default fields, or both changed the same field to different non-default values) still merge and re-timestamp exactly as today — this guard must NOT change legitimate conflict resolution.\n4. The regression guard applies only to the `import()`/merge path used by `wl sync` — it does NOT affect explicit user `wl update` commands, which remain authoritative.\n5. Unit tests cover: (a) newer remote with `effort: ''` against local `effort: 'Small'` → local kept (exclusion by construction), conflict REPORTED as protected, remote value NOT imported; (b) newer remote with `status: open` against local `completed` → refused, local kept, conflict reported; (c) both sides non-default differing → normal merge + re-timestamp (no regression path — guard must NOT alter legitimate conflict resolution); (d) override flag accepts the regression for merge-participating fields only; (e) `--dry-run` reports the same refusals.\n6. Manual verification: sync against the known commit `5fc880a` (effort/risk cleared) → regressions refused and reported; sync against `54b1375` (restored values) → no regression flags.\n\n## Constraints\n\n- Must not change behavior for legitimate concurrent edits — the guard is regression-only, not conflict-resolution.\n- Must be deterministic: dry-run and normal sync report the same refusals.\n- Does NOT prevent a second store from pushing — it prevents the primary store from absorbing regressive state.\n- Complements the identity gate (WL-0MSOYWWS4009HTCB) — the identity gate catches the writer; this guard catches the damage even if the writer's identity is unknown.\n\n## Existing State\n\nThe merge functions (`mergeSameTimestampItems`, `mergeDifferentTimestampItems` in `src/sync.ts`) already implement a non-default-preference heuristic: when one side has a non-default value and the other has a default, the non-default is preserved. The `isDefaultValue` helper in `src/sync/merge-utils.ts` classifies empty strings, `undefined`, and empty arrays as defaults.\n\nThe merge field arrays include `status`, `stage`, `priority`, and `assignee` — these fields **already** keep the local non-default value when the remote sends a default (via the `!localIsDefault && remoteIsDefault` branch). They do **not** produce an explicit regression report — the local value is silently kept.\n\nThe merge field arrays do **not** include `effort` or `risk` — these fields are never merged at all; the local value is always preserved regardless of the remote.\n\n**Contradiction — RESOLVED (producer decision, 2026-08-23)**: The 13-field merge list currently excludes `effort`/`risk` (code-confirmed), yet the original ACs listed them as regression targets. Producer directive: *\"The goal is to prevent ping-pong — do what you need to.\"* Resolution: **Reading A** — `effort`/`risk` remain excluded from the merge lists; adding them to the merge would re-introduce the ping-pong (the DB re-timestamp amplifier in `hasWorkItemChanged` already tracks these fields, so a single unguarded import becomes the newest record and propagates). The guard therefore targets the merge-participating fields (`status`/`stage`/`priority`/`assignee`), and `effort`/`risk` regressions are reported as protected (local kept). Effort/risk merge-participation is a non-goal.\n\n## Desired Change\n\nAdd a regression detection layer on top of the merge path that:\n- Identifies fields regressing from non-default → default in newer remote state, for the merge-participating fields: `status`/`stage`/`priority`/`assignee`\n- Reports conflicts with details (item id, field, old value, new value)\n- Refuses to import regressed fields (keeping local) unless overridden (`--accept-regressions`, merge-participating fields only)\n- Preserves all existing merge behavior for non-regression cases\n- Reports (without importing) attempted `effort`/`risk` regressions as protected — these fields remain excluded from the merge lists (non-goal: adding them back)\n\n## Risks & Assumptions\n\n| Risk | Impact | Mitigation |\n|------|--------|------------|\n| Regression detection logic breaks existing merge behavior | High | Unit tests must verify non-regression cases still merge correctly (AC3) |\n| Scope creep: \"regression\" definition might be too broad (e.g., should a valid transition like `in_progress → open` count as regression?) | Medium | Strict definition: only non-default → default transitions count |\n| Override flag (`--accept-regressions`) could be misused, allowing silent data loss | Medium | Require explicit flag; report what was accepted in output |\n| **DECIDED**: effort/risk must NOT be added to the merge list | — | Producer resolution 2026-08-23: adding them re-introduces ping-pong (DB re-timestamp amplifier `hasWorkItemChanged` tracks these fields; one unguarded import becomes the newest record) |\n| `isDefaultValue` may not correctly identify defaults for all fields (e.g., custom stages) | Low | Only built-in defaults (`open`, `idea`, `medium`, empty strings) are guarded; custom stages fall through to normal merge |\n| **Scope-creep risk**: The guard could be extended to \"merge strategies\" or general conflict-policy work — those are separate concerns. | Medium | Record as future work items; do not implement here |\n\n**Assumptions**:\n- The `isDefaultValue` function correctly classifies all default values for the fields in question\n- Regression detection can be cleanly layered on top of existing merge logic without restructuring\n- The operator will accept a new CLI flag for override\n\n## Key Files (predicted)\n\n- `src/sync.ts` — `mergeSameTimestampItems`, `mergeDifferentTimestampItems`: add regression detection alongside the existing merge logic (field lists UNCHANGED for `effort`/`risk` — exclusion is the ping-pong prevention)\n- `src/sync/merge-utils.ts` — `isDefaultValue`: may need no changes (already classifies empties as defaults)\n- `src/commands/sync.ts` — CLI flag for `--accept-regressions`, conflict reporting in output\n- `tests/sync-regression-guard.test.ts` — new test file for regression detection scenarios\n\n## Related Work\n\n| ID | Title | Relationship |\n|----|-------|-------------|\n| WL-0MSOYWWS4009HTCB | Sync: identity gate — refuse merge of foreign/empty-author commits | Related: same root cause incident (Aug 2026 llm worklog pollution); identity gate catches the writer, regression guard catches the damage |\n| WL-0MSORD6HC005QVZX | Sync: do not change updatedAt on whitespace-only changes | Completed; orthogonal concern |\n| WL-0MKVZ510K1XHJ7B3 | Sync & data integrity | Completed; foundational sync merge logic |\n| WL-0MSUEOAQO0081TJH | Wire identity gate into wl sync + config flag + sync-point persistence | Completed; identity gate now wired into sync |\n\n## Appendix: Clarifying Questions\n\n- **Q (auto-resolved by code inspection)**: The description says \"the 13-field merge list currently excludes effort/risk\" — the code confirms `effort` and `risk` are NOT in the merge field arrays. Does this mean: (a) the ACs about effort/risk are stale and should be removed, or (b) the description is outdated and effort/risk regressions need explicit detection and reporting? → **Answer (producer, 2026-08-23): \"The goal is to prevent ping-pong — do what you need to.\" Interpreted as Reading A: effort/risk stay excluded from the merge lists (prevention by construction); the guard targets merge-participating fields (status/stage/priority/assignee) and reports protected effort/risk conflict attempts without importing them. Adding effort/risk to the merge is a non-goal.** Source: interactive reply, this intake session (2026-08-23).\n- **Q**: What is the exact override flag name? The ACs mention `--accept-regressions` or equivalent. → **Flagged for producer confirmation.**\n## Related work (automated report)\n\n### Related work items\n- **WL-0MS8XLS0Z00759QI** – Pi TUI tools.ts: smart selection (helper copy + fetch merge + apply) (completed)\n- **WL-0MSVRMAWM007QNR5** – Downtime worker: parseLlamaStatus rejects numeric slot_id — every poll fail-closes to busy, zero dispatches (completed)\n- **WL-0MSHWHFAK005WKDB** – [test-failure] defaultSettings has default browseItemCount of 10 — failing test (completed)\n\n### Repository file matches\n- `scripts/audit_runner.py` — matched: ac3, accept, acceptance, accepted, accepts (+231 more)\n- `packages/herdr/README.md` — matched: ac3, accept, acceptance, accepted, accepts (+204 more)\n- `CLI.md` — matched: accept, accepts, add, adding, affect (+191 more)\n","effort":"Small","id":"WL-0MSOYWZTH003JFVD","issueType":"bug","needsProducerReview":false,"parentId":null,"priority":"high","risk":"Medium","sortIndex":3000,"stage":"intake_complete","status":"open","tags":["sync","data-integrity","multi-store"],"title":"Sync: regression guard — refuse import of default-value regressions from newer remote state","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-12T13:16:10.198Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Herdr selection list: let list take as much space as it needs; metadata fills the rest and scrolls (WL-0MSQ44MDX008U69J)\n\n## Headline\n\nFlip the space priority in the Herdr worklist pane: the selection list may use up to the full available pane height (including its header lines), and the metadata section fills whatever space remains — expanding when the list is short, sitting below the fold (revealed by scrolling down) when the list is long — scrolling internally when its content overflows.\n\n## Problem statement\n\nThe metadata section below the selection list currently reserves a fixed 20–40% of the pane height (`computeMetadataPanelHeight`), squeezing the list. When the list needs more vertical space than it is allotted, it scrolls and the header/top lines scroll off the top of the pane, even though the metadata section below is eating rows the list could use. The metadata section should no longer force the list to scroll off the top.\n\n## Users\n\n- **Herdr users (human and AI agent)** browsing the work item list in a pane, who want the selection list — including its header line — to use as much vertical space as it needs, up to the maximum available, so items don't scroll off the top prematurely.\n\n### User stories\n\n- As a Herdr user, when the list is long, I want the list (header + items) to take up to the full pane height so I can see as many items as possible without the top scrolling off.\n- As a Herdr user, when the list is short, I want the metadata section to expand into the leftover space so the pane still shows the selected item's metadata.\n- As a Herdr user, when the list fills the full pane, I want to scroll the pane down to expose the rest of the list and then the metadata section, with the initial view starting at the top of the selection list.\n\n## Acceptance Criteria\n\n1. **List takes as much space as it needs, up to the max available**: The selection list (header line + items + group separators + footer) can use up to the maximum number of vertical lines available in the pane (pane rows minus the reserved notification row). The metadata section no longer reserves a fixed 20–40% share that forces the list to scroll.\n2. **Initial view at top**: When the list fills the full vertical space, the view starts at the top of the selection list (header + first items visible). Scrolling the pane down exposes the rest of the list and, at the end, the metadata section.\n3. **Metadata fills the rest**: When the list content is shorter than the available height, the metadata section expands to fill the leftover space below the list.\n4. **Metadata scrolls if necessary**: When the metadata content overflows the space allotted to it, it scrolls independently using the existing `m`/`M` keys and `metaScrollOffset` (with the existing `[m/M scroll NN%]` indicator), and the selection-driven reset behaviour is preserved.\n5. **No regression**: Keyboard navigation (arrows/j/k, PgUp/PgDn, g/G, wrap), stage filtering, group separators, auto-refresh, code-freeze banner, and the reserved notification row continue to work; rendered output never exceeds the pane's `rows` budget.\n6. **Testing**: Unit tests cover (a) list height = min(content need, max available), (b) metadata expansion when the list is short, (c) reveal-by-scroll when the list fills the pane and initial view at top, (d) metadata scroll clamping under the new layout, and (e) the output line-count invariant (≤ `rows`).\n7. **Docs**: All related documentation is updated to reflect the changes, including code comments, README, and any relevant wiki or docs site entries.\n8. **Full project test suite must pass** with the new changes.\n\n> **Note:** CHANGELOG.md is **excluded** — managed automatically by the ship skill's release pipeline.\n\n## Constraints\n\n- Plugin-local change in `packages/herdr/src/worklist.ts`; no `wl` CLI or backend schema changes.\n- The rendered output must never exceed the pane's `rows` budget (last row reserved for the notification line; group-separator accounting must not overflow) — the invariant from WL-0MSAAON63003N6LO must hold.\n- Preserve existing keyboard bindings: `m`/`M` for metadata scrolling, and all list-navigation keys unchanged.\n- Must compose with the description preview feature (WL-0MSFZKQL700381P3, in progress) which adds up to 3 preview lines to the metadata panel content.\n- The metadata panel keeps a small minimum height (~3 rows) whenever it is rendered (short list or scrolled into view) so it remains usable, even if that means its content scrolls internally.\n\n## Risks & assumptions\n\n- **Assumption — document-flow layout**: the user's answers describe the list and metadata section as stacked content within one scrollable viewport (view starts at top; scrolling down reveals the rest of the list, then the metadata). An alternative pinned-footer layout (metadata always visible at the pane bottom) does not match the answers and is out of scope.\n- **Assumption — header behavior**: \"start the view at the top of the selection list\" is interpreted as the initial render showing header + first items; the exact pin-vs-scroll behaviour of the header line during scroll is an implementation detail, but the header must be visible initially (matching the WL-0MSAAON63003N6LO invariant that the header is never cut off).\n- **Risk — line-count regression**: changing the list/metadata space split could reintroduce output overflowing the pane (the WL-0MSAAON63003N6LO bug). Mitigation: keep the ≤ `rows` invariant as an explicit test.\n- **Risk — metadata scroll clamping**: if the panel's allotted height changes dynamically, `metaScrollDown`'s clamp against content height must still hold; keep the existing clamp tests and update them for the new sizing.\n- **Risk — scope creep**: \"below the fold\" indicators (WL-0MSG8YXYJ008PWJJ), mouse/touch scrolling (WL-0MSGHM5BQ0096BNJ), and metadata panel content changes (WL-0MSHIJR7T007Q9R7) are related but separate items — do not implement them here.\n\n## Existing state\n\n- `packages/herdr/src/worklist.ts`:\n  - `computeMetadataPanelHeight(rows)` (lines ~85–119) — reserves a 20–40% share of pane rows for the metadata panel (min 3 rows), from WL-0MSAYNVBY006LM9X.\n  - `WorkItemListState._listHeight()` (line ~681) — `max(3, rows - 4 - panelHeight)`; used by `_adjustScroll` and `getVisibleItems`.\n  - `createListRenderer()` (line ~1830) — computes `panelHeight = computeMetadataPanelHeight(rows)`, `listArea = rows - 1 - panelHeight`, `listHeight = rows - 4 - panelHeight`; renders header + items + separators + fill + footer + metadata panel lines, clamped to `rows - 1`.\n  - `formatMetadataPanel()` (line ~982) — renders exactly `panelRows` lines with independent scroll offset and `[m/M scroll NN%]` indicator; `metaScrollDown` clamps against content height.\n  - `metaScrollOffset` reset on selection change (`_resetMetaScroll`).\n- `packages/herdr/src/worklist.test.ts` — `computeMetadataPanelHeight` responsive-ramp tests (lines ~1448), metadata scroll clamp tests (line ~1663), renderer line-count invariant tests.\n- `packages/herdr/src/index.ts` — entry point wiring (render callback passes `metaScrollOffset`/`metaLastCommand`).\n\n## Desired change\n\n- In `createListRenderer()`: compute the list's space need from its content (header + banner + items + separators + footer), capped at the maximum available rows (`rows - 1` minus the notification budget). The metadata section is allotted the remainder: it expands to fill leftover space when the list is short, and when the list fills the pane it is positioned below the list content so scrolling down reveals the rest of the list and then the metadata section.\n- Initial view starts at the top of the selection list (scroll offset 0 / header visible) even when the list is long.\n- Keep `computeMetadataPanelHeight` (or replace its call sites) such that the fixed 20–40% reservation no longer forces the list to scroll; the metadata panel keeps a small minimum height (~3 rows) and scrolls internally via `metaScrollOffset`/`m`/`M` when its content overflows.\n- Keep `formatMetadataPanel`'s independent-scroll contract (exactly `panelRows` lines) and the `[m/M scroll NN%]` indicator.\n\n## Key Files\n\n- `packages/herdr/src/worklist.ts` — list/metadata space split, initial view-at-top, metadata scroll clamping; primary change location.\n- `packages/herdr/src/worklist.test.ts` — update responsive-ramp tests; add list-height/content-need, metadata-expansion, reveal-by-scroll, and line-count invariant tests.\n- `packages/herdr/src/index.ts` — render callback wiring (`metaScrollOffset`, `metaLastCommand`), only if the space split needs state/context passed through.\n\n## Related work\n\n- **WL-0MSAYNVBY006LM9X** (completed) — introduced the metadata panel and the fixed 20–40% reservation this item reverses; its AC1 said the list \"always keeps the majority of the space (≥60%)\" — this item changes that priority.\n- **WL-0MSFZKQL700381P3** (in progress) — adds a description preview to the metadata panel (up to 3 lines), which makes the metadata content taller; this item must compose with it.\n- **WL-0MSAAON63003N6LO** (completed) — the output line-count invariant (`≤ rows`) this item must preserve.\n- **WL-0MSG8YXYJ008PWJJ** (open) — \"no indication of items below the fold\" — related scrolling UX; not required by this item but related.\n- **WL-0MSHIJR7T007Q9R7** (open) — drops the duplicate ID row from the metadata panel; touches the same `formatMetadataPanel` output.\n\n## Appendix: Clarifying questions & answers\n\n- Q1: \"When the list is long and fills the pane, should the metadata panel keep a small guaranteed minimum (e.g., ~3 rows, as today) so it stays visible and scrolls within that, or may it shrink all the way to zero (hidden)?\" — Answer (user): \"If the selection list takes up the full vertical space scrolling the pane down should expose the rest of the list and the meta-data section. However we should start the view at the top of the selection list.\" Source: interactive reply. Interpretation: when the list fills the pane, the metadata section sits below the list content and is revealed by scrolling the pane down; the initial view starts at the top of the selection list (header visible). Research: inspected `computeMetadataPanelHeight` (20–40% fixed reservation) and the renderer's `listArea`/`listHeight` budget in `packages/herdr/src/worklist.ts`.\n- Q2: \"When the list has only a few items, should the list take only the rows it needs (header + items + separators + footer) and let the metadata panel expand to fill the leftover space, or should the list always occupy the full available height and the metadata panel stay at a fixed small size?\" — Answer (user): \"meta-data can expand to fill space.\" Source: interactive reply. Interpretation: the list takes only what it needs when short; the metadata section expands into the leftover space.\n- Q3: \"Should the existing m/M metadata scrolling behavior stay as-is, just operating in the new (possibly smaller) panel space?\" — Answer (user): \"yes\". Source: interactive reply. Research: `metaScrollOffset`, `_resetMetaScroll` on selection change, `metaScrollUp/Down` clamping and the `[m/M scroll NN%]` indicator already exist in `worklist.ts`.","effort":"Extra Small","id":"WL-0MSQ44MDX008U69J","issueType":"bug","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"Low","sortIndex":2100,"stage":"in_review","status":"in-progress","tags":[],"title":"Herdr selection list: let list take as much space as it needs; metadata fills the rest and scrolls","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-13T11:39:33.200Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Problem\n\nThe `herdr-podcast-editor` plugin is linked in herdr but its manifest is unavailable — it points at /home/rgardler/projects/open_source_llm/experimental/herdr-podcast-editor/herdr-plugin.toml which no longer exists:\n\n- `herdr plugin list` output: 'herdr-podcast-editor (herdr-podcast-editor) enabled [local:...experimental/herdr-podcast-editor; 1 warning(s)] ... warning: manifest unavailable: No such file or directory (os error 2)'\n\nThe project was presumably moved/deleted; all herdr elements of the plugin must be removed.\n\n## Scope (verify each)\n\n- [ ] Unlink the plugin: `herdr plugin unlink herdr-podcast-editor` (or via registry)\n- [ ] Confirm ~/.config/herdr/plugins.json no longer contains a herdr-podcast-editor entry\n- [ ] Check ~/.config/herdr/plugins/config/herdr-podcast-editor/ for leftover config and remove if present\n- [ ] Check for any keybindings referencing herdr-podcast-editor actions in ~/.config/herdr/config.toml and remove\n- [ ] Check for plugin-owned panes/logs (e.g. herdr plugin log / pane management) referencing herdr-podcast-editor\n- [ ] Verify `herdr plugin list` shows no herdr-podcast-editor entry and no warnings remain\n- [ ] Confirm the worklog-selection-list plugin still works afterwards (no collateral damage)\n\n## Notes\n\n- If the plugin dir still exists somewhere, reconsider (re-link vs remove) — but the experimental dir is gone, so removal is the expected outcome.\n- Keep changes to herdr config minimal; do not touch unrelated plugins.\n\nDiscovered from: support session 2026-08-13 (prefix+l not working — dangling worklog-selection-list manifest; noticed herdr-podcast-editor had the same dangling-manifest warning).\nSession: 019ffad5-8406-7ff2-bcac-c25cc77c3e70","effort":"","id":"WL-0MSRG4828007ITI4","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"low","risk":"","sortIndex":10100,"stage":"idea","status":"open","tags":["herdr","plugin","cleanup"],"title":"Remove all elements of herdr-podcast-editor plugin","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-13T19:16:23.226Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"Discovered-from: WL-0MSGI7UIH008USVB (audit noted minor gap). packages/herdr/shared/README.md Contents table omits the two new files added in WL-0MSGI7UIH008USVB: run-pi-agent.sh and release-lease-on-exit.mjs. Add the two rows with accurate one-line descriptions.","effort":"","id":"WL-0MSRWFPRT009S0SO","issueType":"docs","needsProducerReview":false,"parentId":null,"priority":"low","risk":"","sortIndex":10200,"stage":"idea","status":"open","tags":[],"title":"Herdr shared README: add run-pi-agent.sh and release-lease-on-exit.mjs to Contents table","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-13T19:19:32.378Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"Discovered-from: WL-0MSGI7UIH008USVB. skills/test/scripts/run_tests.py run_suite() (line ~318) and run_summary() (line ~526) call node_suite_commands() with NO project_root, so commands are built from the framework REPO_ROOT (~/.pi/agent → SorraAgents) while executing with cwd=detected project root (ContextHub). Result: for non-framework repos with npm 'test' script (vitest), the canonical node suite is emitted as framework-relative 'node --test tests/<dir>/**/*.mjs' globs that match nothing in the target repo — vacuous green, no real tests run. The audit skill's _effective_suite_commands() correctly calls full_suite_commands(project_root), so it queries 'npm --silent test -- tests/cli' etc. — causing a cache MISS mismatch unless the cache is populated with the project-root-aware commands directly. Fix: pass project_root through to node_suite_commands() in run_suite()/run_summary().","effort":"","id":"WL-0MSRWJRQ1003NX4U","issueType":"bug","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":5100,"stage":"idea","status":"open","tags":[],"title":"test skill: node_suite_commands() ignores detected project root (vacuous node suite for non-framework repos)","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-13T22:22:37.653Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"Quality Improvement epic for tracking code quality findings discovered during automated code review. Closed when all child work items are resolved; a new epic is created if new findings arrive after closure.","effort":"","id":"WL-0MSS3380K0002ZVO","issueType":"epic","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":5200,"stage":"plan_complete","status":"open","tags":[],"title":"Quality Improvement - Refactoring","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-13T22:22:38.724Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Code quality finding\n\n- **Severity**: low\n- **File**: /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py\n- **Line**: 1\n- **Message**: The file is executable but no shebang is present\n- **Linter**: ruff\n- **Code**: EXE002\n\nDiscovered during automated code quality review.","effort":"","id":"WL-0MSS338UC003D7Z0","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSS3380K0002ZVO","priority":"low","risk":"","sortIndex":10300,"stage":"intake_complete","status":"open","tags":["Refactor"],"title":"[LOW] /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — The file is executable but no shebang is present (EXE002)","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-14T15:26:58.156Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Refactor Downtime Dispatcher: leader election with shared coordination file\n\n## Problem statement\n\nThe current Downtime Dispatcher has every herdr plugin instance polling the llama-proxy independently, using priority, round-robin, and jitter to share dispatch load. This approach still allows one instance to dominate dispatches and doesn't scale well when many instances are active. A leader-election model with a shared coordination file would simplify the dispatch logic: all instances contribute their most important work item to a shared list, one elected leader dispatches work from that list, and the coordination file acts as the single source of truth.\n\n## Users\n\n- **Operator running multiple herdr instances** — wants fair dispatch distribution without per-instance polling complexity; each instance contributes its worklog's most important item to a shared queue.\n- **Operator with idle local LLM** — wants work dispatched efficiently: the leader polls for idle slots and pulls from the coordination list, removing dispatched items so others can re-queue.\n\n### User stories\n\n- As an operator running N herdr instances, each instance should automatically join a shared coordination list with its most important work item from its own worklog, identified by instance and worklog directory.\n- As an operator, I want one elected leader to handle all LLM idle checks and dispatches, so the other instances don't waste cycles polling the proxy.\n- As an operator, if the leader crashes, another instance should automatically take over within 5 minutes.\n- As an operator, when the leader dispatches an item, that item is removed from the coordination list and the instance that owned it will add a new item on its next 30-minute check-in.\n\n## Acceptance Criteria\n\n1. **Leader election** — Instances use a file lock to elect a single leader; the first instance to acquire the lock wins. If no leader is elected (lock unavailable), the instance continues operating without dispatching.\n2. **Lease-based leadership** — The leader holds a 5-minute lease, refreshed every time it polls the llama-proxy. If the lease expires (leader crashed or idle), instances detect the expiry and run a new election.\n3. **Coordination file** — A shared JSON file at `<worklog-root>/.worklog/downtime-coordination.json` stores one entry per instance: `{instanceId, workItemId, directory, assignedAt, lastUpdated}`. Each instance:\n   - On startup and every 30 minutes, reads the coordination file\n   - If it has an entry, verifies its work item is still its most important; if not, updates the entry\n   - If no entry exists, adds its most important item to the list\n   - If its entry was removed (by leader dispatch), adds a new entry on the next check-in\n4. **Leader dispatch logic** — The leader reads the coordination file, determines priority order (audit tier first, then implement, then plan, then intake), checks LLM idle status (same 30s proxy poll + 4 min continuous idle threshold), and dispatches the highest-priority available item when a slot opens. The dispatched item is removed from the coordination file.\n5. **Multi-worklog support** — Each instance operates on its own worklog directory (identified in the coordination file). The leader compares work items across directories using the standard tier priority system.\n6. **All related documentation is updated** to reflect the changes, including code comments, README, and any relevant wiki or docs site entries.\n7. **Full project test suite must pass** with the new changes.\n\n## Constraints\n\n- **Single-machine only for v1** — coordination file access uses flock; no cross-machine support in this version.\n- **30-minute check-in interval** — instances update the coordination file every 30 minutes (first check-in on startup).\n- **5-minute lease** — leader lease timeout is 5 minutes, refreshed on each proxy poll cycle.\n- **Fail-safe** — missing/unreadable coordination file or failed leader election degrades to current behavior (no dispatch from that instance).\n- **Existing dispatch guard preserved** — the dispatched-marker exclusion and CAS claim mechanisms continue to prevent double-dispatch.\n\n## Existing state\n\n- `dispatchDowntimeWork` runs tiers in order: audit → implement → plan → intake.\n- Each herdr instance polls the llama-proxy independently using fixed jittered intervals.\n- Dispatch markers (`downtime-dispatches.log`) + CAS claim prevent double-dispatch.\n- Coordination file concept doesn't exist; all state is instance-local.\n- Key files: `packages/herdr/src/downtime-worker.ts`, `packages/herdr/src/downtime-log.ts`, `packages/herdr/src/worklist.ts`, `packages/herdr/src/scheduler.ts`.\n\n## Desired change\n\n1. **Leader election module** — New file (`leader-election.ts`) implementing file-lock-based leader election with lease management. Leader refreshes lease on each proxy poll; lease expiry triggers re-election.\n2. **Coordination file module** — New file (`coordination.ts`) managing the shared coordination JSON file. Each instance reads/writes its entry; entries contain instanceId, workItemId, worklog directory, and timestamps.\n3. **Refactored dispatch worker** — Leader-only dispatch logic reads the coordination file, applies tier priority, checks LLM idle, dispatches the highest-priority item, and removes it. Non-leader instances skip proxy polling and dispatch; they only update their coordination entry on the 30-minute check-in.\n4. **Updated scheduler** — Non-leader instances use a 30-minute check-in timer for coordination updates; the leader uses the existing proxy-poll cadence (30s) for LLM idle checks plus coordination updates.\n\n## Key Files (predicted)\n\n- `packages/herdr/src/leader-election.ts` — NEW: File-lock leader election with lease management (TTL, expiry detection, re-election).\n- `packages/herdr/src/coordination.ts` — NEW: Shared coordination file read/write (flock, JSON format, entry management).\n- `packages/herdr/src/downtime-worker.ts` — Refactored: leader checks before dispatch, coordination file integration, tier priority from list.\n- `packages/herdr/src/downtime-log.ts` — May need minor updates to record coordination-file operations alongside dispatches.\n- `packages/herdr/src/worklist.ts` — Updated: scheduler wiring for leader vs. non-leader timers.\n- `packages/herdr/src/scheduler.ts` — May need updates for the 30-minute check-in cadence on non-leader instances.\n- `packages/herdr/src/index.ts` — Wire coordination file path and leader election into `createDowntimeDeps`.\n\n## Related work\n\n- **WL-0MSSRED76008LGB6** — Downtime dispatcher: priority-first selection with round-robin tie-break (open) — this refactoring **supersedes** the round-robin/jitter approach; that work item should be closed or marked absorbed. This relationship should be recorded as a blocker/absorbed link.\n- **WL-0MSRDEWES0059TZN** — Downtime dispatcher: conditional claim + plan/intake marker exclusion (completed) — CAS/claim infrastructure this builds on.\n- **WL-0MSGPI4AR000YOK8** — Log herdr downtime dispatches (completed) — dispatch log pattern reused.\n\n## Risks & assumptions\n\n- **Assumption:** Each instance operates on its own worklog directory, so \"most important item\" is computed independently per instance using the standard tier priority.\n- **Assumption:** The coordination file is stored in a shared `.worklog/` directory accessible to all instances (local filesystem, not network storage in v1).\n- **Risk:** Race conditions during coordination file writes (two instances updating simultaneously). Mitigation: flock on write; brief lock-hold (read-modify-write is fast JSON manipulation).\n- **Risk:** Leader election race when multiple instances start simultaneously. Mitigation: file lock is atomic; one wins, others detect lock held and retry after a short backoff (e.g., 1-2 seconds).\n- **Risk:** Stale entries from crashed instances linger in the coordination file until they expire or are cleaned up. Mitigation: include `lastUpdated` timestamp per entry; the leader can prune entries older than the lease timeout (5 min) on each dispatch cycle.\n- **Risk:** Instance's worklog becomes inaccessible during a check-in (disk error, permission change). Mitigation: fail-open — skip the check-in for that instance, do not remove the entry, log the error.\n- **Risk (scope creep):** Ensure the refactoring doesn't inadvertently break existing dispatch guard mechanisms (CAS claim, dispatched-marker exclusion). Extra opportunities (e.g., cross-machine support) are recorded as linked work items, not scope.\n- **Risk:** 5-minute lease means up to 5 minutes of no dispatch if leader crashes. Acceptable for v1; could be reduced in a follow-up.\n\n## Appendix: Clarifying questions & answers\n\n- Q1: \"Leader election mechanism?\" — Answer (user): \"(a) Simple: first to acquire a file lock wins\". Source: interactive reply.\n- Q2: \"Coordination file contents?\" — Answer (user): \"work item ID and the directory in which the .worklog folder can be found\". Source: interactive reply.\n- Q3: \"Leader failure / handoff?\" — Answer (user): \"(b) Lease-based: leader gets a time-limited lease and must re-acquire it periodically\". Source: interactive reply.\n- Q4: \"Multi-tier scheduling on leader?\" — Answer (user): \"if an audit tier work item exists, use that; if not check for an implement item; if none try a plan etc.\" Source: interactive reply.\n- Q5: \"LLM idle checking?\" — Answer (user): \"(a) Leader still checks LLM idle status before dispatching (current behavior)\". Source: interactive reply.\n- Q6: \"Multi-machine support?\" — Answer (user): \"not supported in first version; flock is OK\". Source: interactive reply.\n- Q7: \"Lease duration?\" — Answer (user): \"5 minutes, refreshed every time the leader polls the proxy\". Source: interactive reply.\n- Q8: \"Re-queueing after dispatch?\" — Answer (user): \"yes — when leader dispatches and removes an item, the instance adds its next most important item at the next 30-minute check-in\". Source: interactive reply.\n- Q9: \"Dispatch cadence?\" — Answer (user): \"same as current: poll proxy every 30s, require ~4 min continuous idle, then dispatch one item and wait for a fresh idle period\". Source: interactive reply.\n\n## Related work (automated report)\n\n### Related work items\n- **WL-0MKVYN4HW1AMQFAV** – Add 'Update' UI to change work-item stage (shortcut: U) (completed)\n- **WL-0MSI9FDPI005Z0BV** – Audit-tier downtime dispatch tests (completed)\n- **WL-0MSKUG2WW0058A7W** – Evaluate downtime-worker config and silent-failure paths (completed)\n\n### Repository file matches\n- `.worklog/tmp/intake-draft-Refactor-Downtime-Dispatcher-WL-0MST3OJ8S0001ROL.md` — matched: coordination, dispatcher, downtime, elect, election, file, important, instance, instances, items, jitter, key, leader, list, llm, model, polling, polls, priority, refactor, robin, round, running, shared, their, update, work\n- `packages/herdr/README.md` — matched: dispatcher, downtime, elect, election, file, idea, instance, instances, items, key, leader, list, llm, model, polling, polls, priority, round, running, shared, their, update, work\n- `.worklog/worktrees/wl-WL-0MSI4TAT70058921-rename-podcast-editing-tab-to-worklog/packages/herdr/README.md` — matched: downtime, elect, election, file, idea, instance, instances, items, key, leader, list, llm, model, polling, polls, priority, round, running, shared, their, update, work\n\n## Plan: changelog\n\n| Time | Action |\n|------|--------|\n| 2026-08-17 | Plan proposed: 6 features (leader election, coordination file, dispatch logic, scheduler, logging/docs, integration tests) |\n| 2026-08-17 | User approved plan (reply: \"LGTM\") |\n| 2026-08-17 | Created 6 child features with dependency edges; ran 6 automated review stages; AC coverage review: proceed |\n\n### Plan appendix: questions asked & answers received\n\n- Q: \"This plan requires your confirmation because its effort is Medium scale and its risk is High. Please accept, edit, reorder, or split/merge features as needed.\" Answer (user): \"LGTM\" — approved as proposed. Final: accepted unchanged. Source: interactive reply in session.\n- OPEN QUESTION (directed to producer): Whether existing round-robin work item WL-0MSSRED76008LGB6 should be closed or marked absorbed — currently recorded as \"Absorbs:WL-0MSSRED76008LGB6\" comment on this item (Map, 2026-08-14) but the item itself is still open; recommend closing/marking it as part of implementation.\n- OPEN QUESTION (directed to producer): Exact behavior when coordination file is corrupt/missing mid-election — fail-safe path (AC5 of Leader Election child) covers graceful degradation to no-dispatch, but a corrupt-file recovery action (e.g., quarantine + rewrite) is not specified.\n","effort":"Medium","id":"WL-0MST3OJ8S0001ROL","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"high","risk":"Medium","sortIndex":1400,"stage":"in_progress","status":"in-progress","tags":[],"title":"Refactor Downtime Dispatcher: leader election with shared coordination file","updatedAt":"2026-08-23T12:31:55.701Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-15T17:39:22.546Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"","effort":"","id":"WL-0MSUNUNUA000Y27E","issueType":"","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":5300,"stage":"idea","status":"open","tags":[],"title":"Batch test item","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-17T13:39:34.201Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — This is a library module (shared/grid.py), not a script; EXE002 targets executable scripts missing shebangs, so flagging a library is a rule misfire—the executable bit is likely a mistake, but adding a shebang is not the correct remediation.. candidate false positive — producer decision required.","effort":"","id":"WL-0MSXA5Z20001Y50H","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":5400,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-17T16:53:46.851Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — This is a library module (packages/herdr/shared/grid.py), not an entry-point script — shebangs are not required for importable Python packages, regardless of file permission bits.. candidate false positive — producer decision required.","effort":"","id":"WL-0MSXH3QAQ000709E","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":5500,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-17T16:58:32.635Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Shared Coordination File Module\n\n## Summary\nNew `coordination.ts` managing the shared coordination JSON file at `<worklog-root>/.worklog/downtime-coordination.json` with per-instance entry management.\n\n## Acceptance Criteria\n1. Each instance reads/writes its own entry using flock for safe concurrent access\n2. Entry format: `{instanceId, workItemId, directory, assignedAt, lastUpdated}`\n3. On startup and every 30 minutes, instance verifies its entry matches its most-important item\n4. If no entry exists, adds its most-important item from its worklog\n5. Leader can prune stale entries (lastUpdated > 5 min) during dispatch cycles\n\n## Minimal Implementation\n- `readCoordinationFile()` — reads JSON under flock\n- `writeCoordinationFile(data)` — writes JSON under flock\n- `upsertEntry(instanceId, worklogDir, workItemId)` — adds or updates instance entry\n- `pruneStaleEntries(maxAgeSec)` — removes entries older than threshold\n- `getMostImportantItem(worklogDir)` — finds highest-priority item for an instance\n\n## Deliverables\n- `packages/herdr/src/coordination.ts` — NEW: Shared coordination file read/write\n- `packages/herdr/tests/coordination.test.ts` — Unit tests for read/write/lock/prune\n- Updated code comments reflecting coordination architecture\n\n## Key Files\n- `packages/herdr/src/coordination.ts` — NEW file\n- `packages/herdr/src/leader-election.ts` — Dependent on leader election for stale pruning\n- `packages/herdr/tests/coordination.test.ts` — NEW test file\n","effort":"","id":"WL-0MSXH9UT6008151F","issueType":"feature","needsProducerReview":false,"parentId":"WL-0MST3OJ8S0001ROL","priority":"high","risk":"","sortIndex":2300,"stage":"intake_complete","status":"open","tags":[],"title":"Implement shared coordination file module","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-17T16:58:41.053Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Leader-Only Dispatch Logic\n\n## Summary\nRefactored `downtime-worker.ts` where only the leader performs dispatch; non-leader instances skip proxy polling and only do 30-min coordination check-ins.\n\n## Acceptance Criteria\n1. Leader reads coordination file, applies tier priority (audit → implement → plan → intake)\n2. Leader checks LLM idle status (30s proxy poll + 4 min continuous idle threshold)\n3. When slot opens, leader dispatches highest-priority available item and removes it from coordination file\n4. Non-leader instances skip dispatch; they only update coordination entry on 30-min check-in\n5. Existing dispatched-marker exclusion and CAS claim mechanisms preserved\n\n## Minimal Implementation\n- Refactor `dispatchDowntimeWork` to check `isLeader()` before dispatching\n- Leader path: read coordination file → filter tier → check idle → dispatch → remove entry\n- Non-leader path: skip proxy polling, run on 30-min check-in timer\n- Preserve `downtime-dispatches.log` recording and CAS claim logic\n\n## Deliverables\n- `packages/herdr/src/downtime-worker.ts` — Refactored: leader-only dispatch logic\n- `packages/herdr/src/downtime-log.ts` — Updated to log coordination-file operations\n- `packages/herdr/tests/downtime-worker.test.ts` — Integration tests for dispatch flow\n\n## Dependencies\n- Leader Election Module (WL-0MSXH3UF5000HJUS)\n- Coordination File Module\n","effort":"","id":"WL-0MSXHA1B0005G3E5","issueType":"feature","needsProducerReview":false,"parentId":"WL-0MST3OJ8S0001ROL","priority":"high","risk":"","sortIndex":10500,"stage":"intake_complete","status":"blocked","tags":[],"title":"Implement leader-only dispatch logic","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-17T16:58:49.214Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Scheduler Refactoring\n\n## Summary\nUpdate `worklist.ts` and `scheduler.ts` to support leader vs. non-leader timer cadences.\n\n## Acceptance Criteria\n1. Non-leader instances use 30-minute check-in timer for coordination updates\n2. Leader instances use existing proxy-poll cadence (30s) for LLM idle checks\n3. `createDowntimeDeps` in `index.ts` wires coordination file path and leader election\n4. Graceful degradation when coordination file is missing\n\n## Minimal Implementation\n- Scheduler accepts leader status from `leader-election.ts`\n- Leader: 30s proxy poll + coordination updates; Non-leader: 30min check-in timer\n- `index.ts`: wire `coordinationFilePath`, `leaderElection`, `worklogDir` into deps\n\n## Deliverables\n- `packages/herdr/src/worklist.ts` — Updated for leader/non-leader timer paths\n- `packages/herdr/src/scheduler.ts` — Updated for check-in cadence\n- `packages/herdr/src/index.ts` — Wire coordination file path and leader election into `createDowntimeDeps`\n- `packages/herdr/tests/scheduler.test.ts` — Tests for scheduler timer behavior\n\n## Dependencies\n- Leader Election Module (WL-0MSXH3UF5000HJUS)\n- Leader-Only Dispatch Logic\n","effort":"","id":"WL-0MSXHA7LP000QNGP","issueType":"feature","needsProducerReview":false,"parentId":"WL-0MST3OJ8S0001ROL","priority":"high","risk":"","sortIndex":10600,"stage":"intake_complete","status":"blocked","tags":[],"title":"Implement scheduler refactoring","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-17T16:58:57.585Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Logging & Documentation Updates\n\n## Summary\nUpdate `downtime-log.ts` for coordination-file operations and ensure all documentation is current.\n\n## Acceptance Criteria\n1. `downtime-log.ts` records coordination-file operations (check-ins, re-elections, stale pruning)\n2. Code comments reflect leader-election and coordination file architecture\n3. `README.md` updated to describe new dispatcher architecture\n4. Any wiki/docs entries updated\n\n## Minimal Implementation\n- Add logging methods for coordination events (check-in, election, pruning, re-queue)\n- Update inline code comments in all changed files\n- Update `packages/herdr/README.md` with new architecture diagram/description\n- Update any wiki pages about downtime dispatcher\n\n## Deliverables\n- `packages/herdr/src/downtime-log.ts` — Updated with coordination-file logging\n- `packages/herdr/README.md` — Updated architecture documentation\n- Updated inline code comments across all modified files\n\n## Dependencies\n- Leader-Only Dispatch Logic\n- Scheduler Refactoring\n","effort":"","id":"WL-0MSXHAE290067VAL","issueType":"feature","needsProducerReview":false,"parentId":"WL-0MST3OJ8S0001ROL","priority":"medium","risk":"","sortIndex":11300,"stage":"intake_complete","status":"blocked","tags":[],"title":"Implement logging and documentation updates","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-17T16:59:05.778Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Full Integration Tests & Verification\n\n## Summary\nIntegration tests covering the full leader-election → coordination → dispatch flow, plus full test suite verification.\n\n## Acceptance Criteria\n1. Integration test: multiple instances, leader election, lease expiry, re-election\n2. Integration test: coordination file updates, entry pruning, re-queueing after dispatch\n3. Integration test: full dispatch flow (leader polls → idle detected → dispatch → remove → re-queue)\n4. All existing tests pass; new tests achieve coverage of changed code paths\n5. Full project test suite passes with new changes\n\n## Minimal Implementation\n- Integration test fixture with temp worklog directories\n- Test mocking for file locks, proxy polling, and LLM idle state\n- Full end-to-end test of the new dispatcher architecture\n- Regression tests for existing dispatch guard mechanisms\n\n## Deliverables\n- `packages/herdr/tests/integration-leader-dispatch.test.ts` — Full integration test suite\n- `packages/herdr/tests/downtime-worker.test.ts` — Updated with new scenarios\n- All tests passing in CI\n\n## Dependencies\n- Leader-Only Dispatch Logic\n- Scheduler Refactoring\n- Logging & Documentation Updates\n","effort":"","id":"WL-0MSXHAKDT005H6VQ","issueType":"feature","needsProducerReview":false,"parentId":"WL-0MST3OJ8S0001ROL","priority":"high","risk":"","sortIndex":10900,"stage":"intake_complete","status":"blocked","tags":[],"title":"Implement integration tests and verification","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-17T20:28:11.717Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"","effort":"","id":"WL-0MSXORGW4009072T","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSI28AP80002F5S","priority":"high","risk":"","sortIndex":3100,"stage":"idea","status":"open","tags":[],"title":"Fix formatDetailView pinned header+ToC slicing and body viewport math","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-17T20:29:21.969Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"","effort":"","id":"WL-0MSXOSZ3L000Q40V","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSI28AP80002F5S","priority":"high","risk":"","sortIndex":3200,"stage":"idea","status":"open","tags":[],"title":"Fix detailScrollDown to clamp against reduced body viewport","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-17T20:29:42.558Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"","effort":"","id":"WL-0MSXOTEZH008V3FC","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSI28AP80002F5S","priority":"high","risk":"","sortIndex":3300,"stage":"idea","status":"open","tags":[],"title":"Add/update unit tests for detail view ToC overflow fix","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-18T00:35:31.029Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"Currently the downtime dispatcher uses the same priorities for work items regardless of the time of day. Some models are cheaper at different times. Local hosting may be optimized for different models at different times. Make it possible to set different priorities for dispatches based on time slots. For example, since deepseek-v4-flash is cheaper at certain times we should prioritize implementation work at those times. While in times where Deepseek is more expensive we might focus on planning using local models.","effort":"","id":"WL-0MSXXLIZ9005QQP4","issueType":"","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":5600,"stage":"idea","status":"open","tags":[],"title":"Allow downtime dispatch priorities to change over time","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-19T00:35:44.732Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — This is a library module in packages/herdr/shared/ (not a standalone script); the executable bit should be removed rather than adding a shebang.. candidate false positive — producer decision required.","effort":"","id":"WL-0MSZD1O7W009UIOQ","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":5700,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-19T06:02:24.170Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — This is a pure Python module (not a script), so the executable bit is likely accidental; the correct fix is removing execute permissions, not adding a shebang.. candidate false positive — producer decision required.","effort":"","id":"WL-0MSZOPR8Q0059DY9","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":5800,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-19T09:07:04.659Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"In the web UI we show prefill increments, but then switch to a 'Working' badge, before switching back to a prefill increment note ('Processed x of y...'). We should only show 'Working' when the prefill is complete, that is when the processed tokens (x) are within 20 of (y).","effort":"","id":"WL-0MSZVB902004AZZN","issueType":"","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":5900,"stage":"idea","status":"open","tags":[],"title":"Pre-fill vs working in Web UI","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"AI-Assistant","createdAt":"2026-08-19T16:17:43.791Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Herdr: re-enable terminal drag-select while keeping mouse support (WL-0MT0AP2LR000JFWN)\n\n## Problem\n\nWhen mouse tracking is enabled in the worklist TUI (packages/herdr), the terminal sends ALL mouse events to the app, including drag events. This prevents the terminal's native text selection (click-drag to select/copy content from the terminal). Users cannot select text to copy while the worklist TUI is active, even though the TUI needs mouse support for clicking/wheel navigation.\n\nThe root cause is that SGR mouse reporting captures all mouse events from the terminal, preventing the terminal's built-in text-selection mechanism from receiving drag events.\n\n## Root cause location\n\n- packages/herdr/src/worklist.ts line 3541: mouseEnable is emitted on raw-mode entry and stays enabled for the TUI lifetime.\n- packages/herdr/src/worklist.ts line 3560: mouseDisable is only emitted in cleanup() (TUI exit).\n- Once enabled, ALL mouse events are captured, the terminal has no way to selectively receive drag events for selection.\n\n## Solution\n\nAdd a toggle shortcut (Alt+m) that allows the user to temporarily disable mouse tracking, use the terminal's native text selection to copy content, then re-enable mouse tracking for worklist interaction.\n\n### Acceptance Criteria\n\n1. Toggle shortcut Alt+m toggles mouse tracking on/off. Verified by unit test on the toggle key handling.\n2. Toggle state tracked as boolean (mouseTrackingEnabled), initialized to true (mouse tracking enabled by default). Verified by unit test.\n3. Toggle works in all modes: Alt+m is handled in the onData handler BEFORE mode-specific dispatch, so it works in list mode, detail mode, and filter mode.\n4. Toggle shortcut shown in footer hints.\n5. No regression to existing keyboard shortcuts: Alt+m sequence is handled before falling through to keyToAction, so meta-down (m) is unaffected.\n6. All existing tests pass.\n7. Documentation updated.\n\n### Constraints\n\n- Alt+m (escape+m) must be consumed before reaching keyToAction so meta-down (m) is unaffected.\n- The toggle shortcut must be handled in onData before the modal-state guards so it always works.\n- Toggle must emit the correct ANSI sequence (enable/disable) to the terminal.\n- The mouseTrackingEnabled state must be initialized to true (mouse tracking on by default).\n\n### Key Files\n\n- packages/herdr/src/worklist.ts: toggle state, toggle handler in onData, footer hint update.\n- docs/dev/pause-when-hidden.md: add note about mouse toggle shortcut.\n- packages/herdr/dist/: rebuild via npm run build.\n","effort":"","id":"WL-0MT0AP2LR000JFWN","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSGHM5BQ0096BNJ","priority":"high","risk":"","sortIndex":3400,"stage":"plan_complete","status":"open","tags":[],"title":"Herdr: re-enable terminal drag-select while keeping mouse support","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-19T19:35:55.523Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — EXE002 targets scripts missing shebangs; this is a shared library module in packages/herdr/shared/ where the executable bit is incidental and should simply be removed, not a shebang added. candidate false positive — producer decision required.","effort":"","id":"WL-0MT0HRYBN00654UO","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":6000,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-19T23:10:12.047Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — This is a library module (grid.py) with docstring-style documentation, not a standalone script; library modules should not require shebangs and the executable bit should be removed instead of adding a shebang.. candidate false positive — producer decision required.","effort":"","id":"WL-0MT0PFIHA0088MOP","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":6100,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T05:56:20.899Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/.worklog/worktrees/wl-WL-0MSQ44MDX008U69J-herdr-selection-list-let-list-take-as-mu/packages/herdr/shared/grid.py:1 — This is a shared library module (not a script); the executable bit should be removed via chmod, not addressed by adding a shebang. candidate false positive — producer decision required.","effort":"","id":"WL-0MT13XTLU005UTRB","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":6200,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/.worklog/worktrees/wl-WL-0MSQ44MDX008U69J-herdr-selection-list-let-list-take-as-mu/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T09:41:51.219Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Display-rows model with heading type (T1)\n\nTest-first task for WL-0MSL5MPSZ003TG94 (Herdr selection list: collapsible group headings with item counts).\n\n## Summary\nIntroduce a first-class display-rows model in `WorkItemListState` that interleaves heading rows with item rows, plus an in-memory `collapsedGroups` set.\n\n## Acceptance Criteria\n- AC1: A `DisplayRow` union type is defined: heading rows `{ kind: 'heading', group, groupLabel, count, collapsed }` or item rows (WorkItem).\n- AC2: `WorkItemListState` gains `collapsedGroups: Set<number>` (in-memory, mirroring `expandedItems`).\n- AC3: A `getDisplayRows()` method produces rows over the flattened items: headings interleaved when the group label changes; children of expanded parents included; items of collapsed groups excluded.\n- AC4: Heading `count` = top-level items in that group in the current view (post stage-filter), unaffected by collapse state.\n- AC5: `collapsedGroups` survives list refresh (keyed by group number); tests cover it.\n- AC6: Test suite `worklist-heading-rows.test.ts` (or equivalent) covers heading generation, counts, and collapse-on-refresh survival.\n\n## Key Files\n- packages/herdr/src/worklist.ts\n- packages/herdr/src/worklist-heading-rows.test.ts (new)\n- packages/herdr/src/worklist-expand-refresh.test.ts\n\n## Dependencies\n- T2 and T3 depend on this task.\n\n## Deliverables\n- DisplayRow type, getDisplayRows(), collapsedGroups state, tests.","effort":"","id":"WL-0MT1BZTO2002RPHG","issueType":"","needsProducerReview":false,"parentId":"WL-0MSL5MPSZ003TG94","priority":"medium","risk":"","sortIndex":6300,"stage":"plan_complete","status":"open","tags":[],"title":"T1: Display-rows model with heading type","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T09:42:13.029Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Navigation over display rows (T2)\n\nTest-first task for WL-0MSL5MPSZ003TG94 (Herdr selection list: collapsible group headings with item counts).\n\n## Summary\nRework navigation (up/down, page up/down, first/last, wrap-around) to operate over display rows so the cursor lands on heading rows as well as items.\n\n## Acceptance Criteria\n- AC1: `moveUp`/`moveDown` move the selection onto heading rows as well as items; headings are no longer skipped.\n- AC2: Page up/down, goToFirst/goToLast, and wrap-around behave consistently with heading rows included.\n- AC3: `_clampSelection` and `_adjustScroll` operate over display rows (replacing `flatCount`-based indexing).\n- AC4: `getSelectedItem()` returns `null` for a heading selection; returns the item otherwise.\n- AC5: Previously skipped headings become selectable; navigation counters use display row count.\n- AC6: Tests cover navigation onto/over headings, wrap-around, page up/down, and heading vs item selection.\n\n## Key Files\n- packages/herdr/src/worklist.ts\n- packages/herdr/src/worklist.test.ts\n\n## Dependencies\n- BLOCKED BY T1 (WL-0MT1BZTO2002RPHG) — needs the display-rows model.\n\n## Deliverables\n- Navigation over display rows, getSelectedItem() heading-null handling, tests.","effort":"","id":"WL-0MT1C0AHW002ADFB","issueType":"","needsProducerReview":false,"parentId":"WL-0MSL5MPSZ003TG94","priority":"medium","risk":"","sortIndex":12000,"stage":"plan_complete","status":"blocked","tags":[],"title":"T2: Navigation over display rows","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T09:42:26.879Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Goal\nCopy all icon content (IconOptions interface, icon maps, functions) from src/icons.ts into src/theme.ts, appended after the existing chalk theme exports. Preserve all names, signatures, and behaviour exactly. The icon maps must remain pure data (no chalk dependency) so herdr can consume them later.\n\n## Acceptance Criteria\n- [ ] All icon maps, constants, and public functions from icons.ts are present in theme.ts\n- [ ] Icon functions export unchanged names/signatures/behaviour\n- [ ] Icon maps are pure data with no chalk dependency\n- [ ] theme.ts is the single source of truth for both colours and icons","effort":"","id":"WL-0MT1C0L6M001WHEE","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSJ4BT4Z009LIB4","priority":"medium","risk":"","sortIndex":6400,"stage":"plan_complete","status":"open","tags":[],"title":"Move icon maps/functions from icons.ts into theme.ts","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T09:43:50.901Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Goal\nPoint the primary icon unit tests at src/theme.ts and add a wrapper-parity test verifying icons.ts re-exports the full original public API.\n\n## Acceptance Criteria\n- [ ] tests/unit/icons.test.ts imports icon functions from '../../src/theme.js'\n- [ ] A parity test enumerates every original public API name and verifies it is exported (and equal) from both icons.js and theme.js\n- [ ] All existing icon test assertions (emoji values, noIcons fallbacks) pass unchanged","effort":"","id":"WL-0MT1C2E0K001CTJ7","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSJ4BT4Z009LIB4","priority":"medium","risk":"","sortIndex":12100,"stage":"plan_complete","status":"blocked","tags":[],"title":"Update tests: point at theme.ts, add wrapper parity test","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T09:44:44.981Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Goal\nUpdate docs/icons-design.md and any other docs/README entries so they reference src/theme.ts as the canonical home of icon definitions, and note that src/icons.ts is now a deprecated re-export wrapper.\n\n## Acceptance Criteria\n- [ ] docs/icons-design.md references src/theme.ts as the single source of truth for icon content\n- [ ] docs/icons-design.md notes src/icons.ts is a deprecated re-export wrapper\n- [ ] Any other docs entries referencing src/icons.ts as canonical icon location are updated (herdr copy and removed-TUI references are out of scope)","effort":"","id":"WL-0MT1C3JQT005UIJ2","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSJ4BT4Z009LIB4","priority":"medium","risk":"","sortIndex":12200,"stage":"plan_complete","status":"blocked","tags":[],"title":"Update documentation references","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T09:45:09.474Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"Run \n> worklog@1.0.11 build\n> npm run build:shared && tsc && node ./scripts/generate-version.cjs\n\n\n> worklog@1.0.11 build:shared\n> cd packages/shared && npm run build\n\n\n> @worklog/shared@1.0.0 build\n> tsc\n\n\n> worklog@1.0.11 postbuild\n> bash ./scripts/install-herdr.sh\n\n\nadded 40 packages, and audited 41 packages in 822ms\n\n9 packages are looking for funding\n  run `npm fund` for details\n\nfound 0 vulnerabilities\nInstalled herdr plugin dependencies (npm ci: /home/rgardler/projects/ContextHub/packages/herdr)\n{\"id\":\"cli:plugin\",\"result\":{\"plugin\":{\"actions\":[{\"command\":[\"bash\",\"scripts/open-pi-agent.sh\"],\"description\":\"Open a Pi AI coding agent pane docked on the right.\",\"id\":\"open-pi-agent\",\"title\":\"Open Pi Agent\"},{\"command\":[\"bash\",\"scripts/open-podcast-editor-tab.sh\"],\"description\":\"Open the Worklog work item selection pane in a new tab renamed to 'Podcast Editing'.\",\"id\":\"open-podcast-editor-tab\",\"title\":\"Open Podcast Editing tab\"},{\"command\":[\"bash\",\"scripts/open.sh\"],\"description\":\"Open the Worklog work item selection pane.\",\"id\":\"open-worklist\",\"title\":\"Open worklist\"},{\"command\":[\"bash\",\"scripts/toggle.sh\"],\"description\":\"Open the Worklog work item selection pane (focus it if open; close it if focused).\",\"id\":\"toggle-worklist\",\"title\":\"Toggle worklist\"}],\"build\":[{\"command\":[\"npm\",\"run\",\"build\"]}],\"description\":\"Browse, filter, and select Worklog work items from a Herdr pane. Keyboard-navigable list with stage filtering and detail view.\",\"enabled\":true,\"manifest_path\":\"/home/rgardler/projects/ContextHub/packages/herdr/herdr-plugin.toml\",\"min_herdr_version\":\"0.7.0\",\"name\":\"Worklog Selection List\",\"panes\":[{\"command\":[\"npx\",\"tsx\",\"src/index.ts\"],\"id\":\"worklist\",\"placement\":\"tab\",\"title\":\"Work Items\"}],\"platforms\":[\"linux\",\"macos\",\"windows\"],\"plugin_id\":\"worklog-selection-list\",\"plugin_root\":\"/home/rgardler/projects/ContextHub/packages/herdr\",\"source\":{\"kind\":\"local\"},\"version\":\"0.1.0\"},\"type\":\"plugin_linked\"}}\nLinked herdr plugin: worklog-selection-list (/home/rgardler/projects/ContextHub/packages/herdr/herdr-plugin.toml)\nherdr keybinding already present: /home/rgardler/.config/herdr/config.toml to verify TypeScript compilation succeeds. Run the full test suite and ensure all tests pass. If any tests fail, triage and fix them.","effort":"","id":"WL-0MT1C42N6003QSCJ","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSJ4BT4Z009LIB4","priority":"medium","risk":"","sortIndex":12300,"stage":"plan_complete","status":"blocked","tags":[],"title":"Build, lint, and run full test suite","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T10:13:50.676Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Goal\nReplace the body of src/icons.ts with a module-level @deprecated JSDoc block and re-exports from './theme.js'.\n\n## Acceptance Criteria\n- [ ] icons.ts contains only re-export statements from './theme.js' plus a @deprecated JSDoc marker instructing consumers to import from './theme.js'\n- [ ] Every original public API name is re-exported: IconOptions (type), iconsEnabled, priorityIcon/statusIcon/epicIcon/riskIcon/effortIcon/stageIcon/auditIcon/auditStaleIcon/needsProducerReviewIcon, priorityLabel/statusLabel/epicLabel/riskLabel/effortLabel/stageLabel/auditLabel/auditStaleLabel/needsProducerReviewLabel, priorityFallback/statusFallback/epicFallback/riskFallback/effortFallback/stageFallback/auditFallback/auditStaleFallback/needsProducerReviewFallback\n- [ ] Existing consumers (src/commands/helpers.ts, tests/unit/icons.test.ts) import unchanged and continue to work\n- [ ] src/icons.ts is NOT deleted (follow-up item WL-0MSJ4BT4Z002HH9B removes it)","effort":"","id":"WL-0MT1D4YQC002GWZM","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSJ4BT4Z009LIB4","priority":"medium","risk":"","sortIndex":12400,"stage":"plan_complete","status":"blocked","tags":[],"title":"Rewrite src/icons.ts as deprecated re-export wrapper","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T10:48:56.743Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Tests: mode-switch worker core\n\nTest-first unit tests for the mode-switch worker core (parent WL-0MSN3FWV5008KQE9): idle-window evaluation, last-command tracking, cheap/fast switch triggers, restart reset, and fail-closed paths.\n\n## Goal\n`packages/herdr/src/mode-switch-worker.test.ts` covering the pure logic of the new worker so implementation can proceed test-first.\n\n## Acceptance Criteria\n- [ ] **Idle-window evaluation:** test that `lastOperatorCommandAt` null (never set this process) is treated as idle after the threshold; a timestamp older than `modeSwitchIdleThresholdMs` triggers the cheap path; a fresh timestamp (within threshold) does not.\n- [ ] **Last-command tracking:** test that an agent-route command updates `lastOperatorCommandAt` to now.\n- [ ] **Cheap switch trigger:** test that idle-window met AND proxy idle (reusing `evaluateIdle` semantics, `requiredFreeSlots = 0`/all slots free) yields a `POST /admin/set-mode {\"mode\":\"cheap\"}`; busy proxy (active local query) delays the switch.\n- [ ] **Fast switch trigger:** test that a new operator agent-route command yields `POST /admin/set-mode {\"mode\":\"fast\"}` fire-and-forget.\n- [ ] **No redundant switching:** test that when last-known mode already matches the target, no POST is issued (refresh via `GET /admin/mode` on poll).\n- [ ] **Restart reset:** test that worker construction sets `lastOperatorCommandAt = Date.now()` (active now; fail-safe fast).\n- [ ] **Fail-closed paths:** test that endpoint failure, timeout, network error, ambiguous response, and `409` (mode-switch restart in progress) yield NO switch and never throw; `409` is a no-op retried later.\n- [ ] **Settings clamps:** defaults/floors asserted via the settings contract (ties to the settings test child).\n- [ ] All tests pass in the project test suite.\n\n## Key Files\n- `packages/herdr/src/mode-switch-worker.test.ts` (new)\n- `packages/herdr/src/mode-switch-worker.ts` (under test)\n\n## Dependencies\nNone beyond the settings contract (first test child WL-0MT1EDYS0002CMUK is independent; this child focuses on worker logic).","effort":"","id":"WL-0MT1EE3S7005DJ1F","issueType":"","needsProducerReview":false,"parentId":"WL-0MSN3FWV5008KQE9","priority":"medium","risk":"","sortIndex":6500,"stage":"plan_complete","status":"open","tags":[],"title":"Tests: mode-switch worker core (idle-window, triggers, fail-closed)","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T10:49:03.173Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Tests: integration (agent-route hook + scheduler wiring)\n\nTest-first coverage for the wiring layers (parent WL-0MSN3FWV5008KQE9): `index.ts` agent-route hook (last-command timestamp + fire-and-forget fast switch) and `worklist.ts` scheduler task registration.\n\n## Goal\n`packages/herdr/src/index.test.ts` and `packages/herdr/src/worklist.test.ts` additions verifying the hook and scheduler integration, so the implementation children land test-first.\n\n## Acceptance Criteria\n- [ ] Test that an **agent-route** command (`/skill:*`, `/intake`, `/plan`, `/prompt:`) routed via `onCommand`/`routeCommand` records `lastOperatorCommandAt` and fires the fast switch (`POST /admin/set-mode {\"mode\":\"fast\"}`, fire-and-forget, fail-open — never blocks or delays the command dispatch).\n- [ ] Test that shell shortcuts (`!!`/`!`) and plain commands do NOT record operator activity (no fast switch, no timestamp update).\n- [ ] Test that the scheduler registers a mode-switch task on `modeSwitchPollIntervalMs` when enabled (pattern-matched on the existing `downtime` task: single-flight, `runTimeoutMs`, visibility-independent).\n- [ ] Test that a failed fast switch does not block the pane/command dispatch (fail-open).\n- [ ] Test that a disabled `modeSwitchEnabled` registers no mode-switch task.\n- [ ] All tests pass in the project test suite.\n\n## Key Files\n- `packages/herdr/src/index.test.ts`\n- `packages/herdr/src/worklist.test.ts`\n- `packages/herdr/src/index.ts` (under test)\n- `packages/herdr/src/worklist.ts` (under test)\n\n## Dependencies\nRequires the mode-switch worker core implementation and its tests (the hook and scheduler call into the worker).","effort":"","id":"WL-0MT1EE8QT0017P0H","issueType":"","needsProducerReview":false,"parentId":"WL-0MSN3FWV5008KQE9","priority":"medium","risk":"","sortIndex":12500,"stage":"plan_complete","status":"blocked","tags":[],"title":"Tests: integration (agent-route hook + scheduler wiring)","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T10:49:14.827Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Implement: mode-switch worker core\n\nNew `packages/herdr/src/mode-switch-worker.ts` implementing the activity-gated mode switching decision logic (parent WL-0MSN3FWV5008KQE9), satisfying the worker-core test child.\n\n## Goal\nA worker that tracks `lastOperatorCommandAt`, evaluates the idle window against `modeSwitchIdleThresholdMs`, reuses the existing `/llama/local/status` idle evaluation (`evaluateIdle`, all slots free), tracks last-known mode (refreshed via `GET /admin/mode`), and POSTs `/admin/set-mode` — cheap on idle (gated, fail-closed), fast on command (fire-and-forget, fail-open).\n\n## Acceptance Criteria\n- [ ] Construction sets `lastOperatorCommandAt = Date.now()` (restart resets to active now; fail-safe fast).\n- [ ] An agent-route command records `lastOperatorCommandAt` and fires `POST /admin/set-mode {\"mode\":\"fast\"}` fire-and-forget (never blocks or delays dispatch; a failed switch is logged, command proceeds).\n- [ ] Cheap switch tick: when `lastOperatorCommandAt` is null/older than `modeSwitchIdleThresholdMs` AND proxy idle (reuse `evaluateIdle` semantics: llama-server running, no active local query, no model switch, no local lease, all required slots free), POST `{\"mode\":\"cheap\"}`. A busy proxy delays the switch (never kills in-flight work).\n- [ ] No redundant switching: skip the POST when last-known mode already matches the target; refresh last-known mode via `GET /admin/mode` on poll. At most one switch per state change.\n- [ ] `409` (mode-switch restart in progress) is a no-op, retried on a later tick.\n- [ ] Fail-closed everywhere: endpoint failures, timeouts, network errors, and ambiguous responses yield no switch and never crash or block the plugin (log a warning).\n- [ ] Per-process single-flight: at most one switch in flight at a time.\n- [ ] All worker-core tests pass (child WL-0MT1EE3S7005DJ1F).\n\n## Key Files\n- `packages/herdr/src/mode-switch-worker.ts` (new)\n- `packages/herdr/src/mode-switch-worker.test.ts` (new)\n\n## Dependencies\nDepends on settings implementation (WL-0MT1EECPU0024Z7J) and the worker-core test child (WL-0MT1EE3S7005DJ1F, test-first). Reuses `evaluateIdle`/`isIdleStatus`/poller from `packages/herdr/src/downtime-worker.ts` (WL-0MSF49FMW009M06K infra).","effort":"","id":"WL-0MT1EEHQI002MOVT","issueType":"","needsProducerReview":false,"parentId":"WL-0MSN3FWV5008KQE9","priority":"medium","risk":"","sortIndex":12600,"stage":"plan_complete","status":"blocked","tags":[],"title":"Implement: mode-switch worker core","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T10:49:19.309Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Implement: agent-route hook (index.ts)\n\nWire the mode-switch worker into the plugin's command routing (parent WL-0MSN3FWV5008KQE9), satisfying the integration test child's hook requirements.\n\n## Goal\nIn `packages/herdr/src/index.ts`, the agent-route branch of `routeCommand`/`onCommand` records `lastOperatorCommandAt` and fires the fast switch (`POST /admin/set-mode {\"mode\":\"fast\"}`, fire-and-forget, fail-open) via the mode-switch worker. Shell shortcuts (`!!`/`!`) and plain commands do NOT count as operator activity.\n\n## Acceptance Criteria\n- [ ] Agent-route commands (`/skill:*`, `/intake`, `/plan`, `/prompt:` — routes that spawn a pi agent pane) record the timestamp and fire the fast switch.\n- [ ] Shell shortcuts and plain commands do not record operator activity and do not fire a fast switch.\n- [ ] A failed fast switch never blocks or delays the command dispatch (fail-open: the pane opens regardless).\n- [ ] All integration tests pass (child WL-0MT1EE8QT0017P0H).\n\n## Key Files\n- `packages/herdr/src/index.ts`\n- `packages/herdr/src/index.test.ts`\n\n## Dependencies\nDepends on the worker core implementation (WL-0MT1EEHQI002MOVT) and the integration test child (WL-0MT1EE8QT0017P0H, test-first).","effort":"","id":"WL-0MT1EEL700052IMQ","issueType":"","needsProducerReview":false,"parentId":"WL-0MSN3FWV5008KQE9","priority":"medium","risk":"","sortIndex":12700,"stage":"plan_complete","status":"blocked","tags":[],"title":"Implement: agent-route hook (index.ts)","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T10:49:24.555Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Implement: mode-switch scheduler task wiring\n\nRegister the mode-switch worker's poll task in the herdr consolidated scheduler (parent WL-0MSN3FWV5008KQE9), satisfying the integration test child's scheduler requirements.\n\n## Goal\nIn `packages/herdr/src/worklist.ts`, add a scheduler task (pattern-matched on the existing `downtime` task: single-flight, `runTimeoutMs`, visibility-independent, `getIntervalMs` jitter) that ticks on `modeSwitchPollIntervalMs` when `modeSwitchEnabled` — calling the mode-switch worker's tick (idle-window + idle + mode-dedup → cheap switch). Wire TUI options plumbing for the worker and poll interval.\n\n## Acceptance Criteria\n- [ ] A mode-switch task is registered on `modeSwitchPollIntervalMs` when `modeSwitchEnabled` is true (and not when disabled).\n- [ ] The task is single-flight with `runTimeoutMs` (a hung tick can never wedge the mode-switch task).\n- [ ] The task is visibility-independent (like the downtime task) — it runs while the worklist pane is open.\n- [ ] TUI options plumbing exposes the mode-switch worker and poll interval to the scheduler (mirroring `downtimeWorker`/`downtimePollIntervalMs`).\n- [ ] Settings re-read every tick so changes apply without a plugin restart (existing behavior).\n- [ ] All integration tests pass (child WL-0MT1EE8QT0017P0H).\n\n## Key Files\n- `packages/herdr/src/worklist.ts`\n- `packages/herdr/src/worklist.test.ts`\n\n## Dependencies\nDepends on the worker core implementation (WL-0MT1EEHQI002MOVT) and the integration test child (WL-0MT1EE8QT0017P0H, test-first).","effort":"","id":"WL-0MT1EEP8R000Q5ST","issueType":"","needsProducerReview":false,"parentId":"WL-0MSN3FWV5008KQE9","priority":"medium","risk":"","sortIndex":12800,"stage":"plan_complete","status":"blocked","tags":[],"title":"Implement: mode-switch scheduler task wiring","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T10:49:29.567Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Implement: documentation (README + code comments)\n\nUpdate herdr documentation for the new mode-switch worker (parent WL-0MSN3FWV5008KQE9).\n\n## Goal\n`packages/herdr/README.md` gains the new settings and behavior documentation; code comments in the new worker and wiring describe the decision logic (cheap on idle, fast on command) and the coexistence semantics with the proxy's built-in time schedule.\n\n## Acceptance Criteria\n- [ ] README documents the three new settings (`modeSwitchEnabled`, `modeSwitchIdleThresholdMs`, `modeSwitchPollIntervalMs`), their defaults, and that the proxy URL reuses `downtimeProxyUrl`.\n- [ ] README documents the decision logic: fast switch on agent-route command (fire-and-forget, fail-open), cheap switch on idle window + proxy idle (fail-closed, never kills in-flight work), restart resets to active now.\n- [ ] README documents coexistence with the proxy's built-in schedule: plugin switches are manual overrides (LP-0MSMF25V9002AY1J semantics); the proxy's time schedule reclaims control at its next boundary — documented behavior, not a conflict.\n- [ ] Code comments in `mode-switch-worker.ts` and the wiring describe the decision logic and fail-closed/fail-open boundaries.\n- [ ] Any relevant wiki/docs site entries updated.\n\n## Key Files\n- `packages/herdr/README.md`\n- `packages/herdr/src/mode-switch-worker.ts` (comments)\n\n## Dependencies\nDepends on the worker core implementation (WL-0MT1EEHQI002MOVT) — documents the implemented behavior.","effort":"","id":"WL-0MT1EET3Z007EBL9","issueType":"","needsProducerReview":false,"parentId":"WL-0MSN3FWV5008KQE9","priority":"low","risk":"","sortIndex":14300,"stage":"plan_complete","status":"blocked","tags":[],"title":"Implement: documentation (README + code comments)","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T13:11:01.527Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Tab toggle for group collapse (T3)\n\nTest-first task for WL-0MSL5MPSZ003TG94 (Herdr selection list: collapsible group headings with item counts).\n\n## Summary\nRoute Tab so a heading selection toggles that group's collapse state; item selections keep the existing expand/collapse-children behaviour.\n\n## Acceptance Criteria\n- AC1: Pressing Tab while a heading row is selected toggles that group's `collapsedGroups` entry: collapsed hides the group's items from render and navigation; expanded shows them.\n- AC2: Tab on an item row keeps its existing expand/collapse-children behaviour unchanged (including on-demand children fetch in the TUI loop).\n- AC3: Enter on a heading is a no-op (only Tab toggles).\n- AC4: The heading-row toggle path does not disturb `expandedItems`/navigation stack handling for items.\n- AC5: Tests cover Tab-on-heading toggle and Tab-on-item unchanged behaviour.\n\n## Key Files\n- packages/herdr/src/worklist.ts (keyToAction, handleKeypress, TUI loop dispatch)\n- packages/herdr/src/worklist.test.ts\n- packages/herdr/src/worklist-expand-refresh.test.ts\n\n## Dependencies\n- BLOCKED BY T1 (WL-0MT1BZTO2002RPHG) — needs display-rows model; consumes T2's selection model.\n\n## Deliverables\n- Tab routing (heading → group toggle; item → existing path), tests.","effort":"","id":"WL-0MT1JGTJR002RTCB","issueType":"","needsProducerReview":false,"parentId":"WL-0MSL5MPSZ003TG94","priority":"medium","risk":"","sortIndex":12900,"stage":"plan_complete","status":"blocked","tags":[],"title":"T3: Tab toggle for group collapse","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T13:11:07.011Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Renderer heading rows with counts (T4)\n\nTest-first task for WL-0MSL5MPSZ003TG94 (Herdr selection list: collapsible group headings with item counts).\n\n## Summary\nRework `createListRenderer` and the row-layout/click-mapping helpers to render heading rows directly from the display model (label + count + collapse arrow), replacing the group-transition separator derivation, while preserving the `rows - 1` line-count invariant.\n\n## Acceptance Criteria\n- AC1: Each heading row renders the label plus the number of items in that group (e.g. `── Group 1 (4) ──`), shown whether collapsed or expanded.\n- AC2: A collapse arrow/indicator is rendered on heading rows (e.g. `▼`/collapsed state), distinct from item rows.\n- AC3: `createListRenderer` stops deriving separators from group transitions; headings come from the display model passed by the caller.\n- AC4: The visible-window budget logic (`countSeparators`, `computeListRowLayout`, `mapListRowToIndex`) is reworked for heading rows; the `rows - 1` pane line-count invariant is preserved (code-freeze banner, fold indicators, metadata-panel sizing all keep working).\n- AC5: Collapsed groups' items do not render; heading rows still render with their count.\n- AC6: Tests cover heading rendering (counts, collapse arrow), budget/line-count invariant under heading configurations, and click-row mapping.\n\n## Key Files\n- packages/herdr/src/worklist.ts (createListRenderer, computeListRowLayout, mapListRowToIndex, countSeparators)\n- packages/herdr/src/worklist.test.ts\n- packages/herdr/src/worklist-mouse.test.ts\n\n## Dependencies\n- BLOCKED BY T1 (WL-0MT1BZTO2002RPHG) and T3 (WL-0MT1JGTJR002RTCB) — needs display model + collapse state.\n\n## Deliverables\n- Heading-row rendering with counts and collapse arrows, budget rework, tests.","effort":"","id":"WL-0MT1JGXS3006ZFY1","issueType":"","needsProducerReview":false,"parentId":"WL-0MSL5MPSZ003TG94","priority":"medium","risk":"","sortIndex":13000,"stage":"plan_complete","status":"blocked","tags":[],"title":"T4: Renderer heading rows with counts","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T13:11:11.819Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Metadata panel group info on heading selection (T5)\n\nTest-first task for WL-0MSL5MPSZ003TG94 (Herdr selection list: collapsible group headings with item counts).\n\n## Summary\nWhen a heading row is selected, the metadata panel shows the group info (label + count) rather than stale item metadata; Enter on a heading is a no-op.\n\n## Acceptance Criteria\n- AC1: Selecting a heading row renders group info (label + count) in the metadata panel instead of item metadata.\n- AC2: `getSelectedItem()` returns `null` for a heading selection and the panel caller renders group info (no stale item metadata shown).\n- AC3: Enter on a heading is a no-op (does not open a detail view).\n- AC4: Selecting an item row renders its normal metadata panel as before.\n- AC5: Tests cover the metadata panel on heading selection vs item selection.\n\n## Key Files\n- packages/herdr/src/worklist.ts (formatMetadataPanel, metadata panel call site in createListRenderer, detail-view dispatch)\n- packages/herdr/src/worklist.test.ts\n\n## Dependencies\n- BLOCKED BY T2 (WL-0MT1C0AHW002ADFB) and T4 (WL-0MT1JGXS3006ZFY1) — needs heading selection model + renderer.\n\n## Deliverables\n- Metadata panel group-info rendering, Enter-on-heading no-op, tests.","effort":"","id":"WL-0MT1JH1HN004JP1F","issueType":"","needsProducerReview":false,"parentId":"WL-0MSL5MPSZ003TG94","priority":"medium","risk":"","sortIndex":13100,"stage":"plan_complete","status":"blocked","tags":[],"title":"T5: Metadata panel group info on heading","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T13:11:17.332Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Integration tests and regression sweep (T6)\n\nTest-first task for WL-0MSL5MPSZ003TG94 (Herdr selection list: collapsible group headings with item counts).\n\n## Summary\nEnd-to-end integration tests tying together display rows, navigation, Tab toggle, rendering, and the metadata panel, plus a full-suite regression sweep to keep the `rows - 1` invariant and existing expand/refresh tests green.\n\n## Acceptance Criteria\n- AC1: An end-to-end scenario test: navigate to a heading → Tab → group collapses → rows shrink in render and navigation → Tab again → group expands.\n- AC2: Counts shown on headings match the top-level items in the group in the current view (post stage-filter), before/after collapse.\n- AC3: The `rows - 1` line-count invariant holds under all heading/group/collapse configurations (code-freeze banner, fold indicators, metadata-panel sizing, stage-filter views).\n- AC4: Existing expand/refresh tests (worklist-expand-refresh.test.ts) stay green; Tab-on-item children fetch unaffected.\n- AC5: Full Herdr test suite (vitest for packages/herdr) passes.\n- AC6: All related documentation and code comments updated to reflect the changes (AC7 of parent).\n\n## Key Files\n- packages/herdr/src/worklist.ts\n- packages/herdr/src/worklist.test.ts\n- packages/herdr/src/worklist-expand-refresh.test.ts\n- packages/herdr/src/worklist-mouse.test.ts\n- packages/herdr/README.md (docs updates)\n\n## Dependencies\n- BLOCKED BY T1 (WL-0MT1BZTO2002RPHG), T2 (WL-0MT1C0AHW002ADFB), T3 (WL-0MT1JGTJR002RTCB), T4 (WL-0MT1JGXS3006ZFY1), T5 (WL-0MT1JH1HN004JP1F).\n\n## Deliverables\n- Integration tests, full regression sweep, docs updates.","effort":"","id":"WL-0MT1JH5QS007UWQP","issueType":"","needsProducerReview":false,"parentId":"WL-0MSL5MPSZ003TG94","priority":"medium","risk":"","sortIndex":13200,"stage":"plan_complete","status":"blocked","tags":[],"title":"T6: Integration tests and regression sweep","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T13:41:13.212Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Problem statement\n\nDuring the 2026-08-20 system hang, \\\"Under memory pressure, flushing caches\\\" appeared 10+ times in the journal between 13:27 and 13:40 with no alert and no early warning. The system ran for 21 days with accumulating memory pressure and no monitoring to catch it before it became unusable.\n\n## Goal\n\nInstall lightweight memory-pressure monitoring so a repeat of the 2026-08-20 memory exhaustion is detected and alerted on well before the system becomes unresponsive.\n\n## Acceptance criteria\n\n- [ ] A monitoring mechanism is installed (e.g. cron/systemd timer script checking /proc/meminfo MemAvailable, or earlyoom/PSI-based monitoring via /proc/pressure/memory)\n- [ ] An alert fires when MemAvailable drops below a threshold (e.g. 2 GB) - alert method documented (logger/journald entry, desktop notification, or both)\n- [ ] Historical trend is captured so memory exhaustion can be diagnosed after the fact (e.g. collectd/sysstat already captures sar data - verify and document retention)\n- [ ] The alert mechanism is tested end-to-end once (verified alert emission)\n- [ ] Setup and thresholds documented in a comment on this work item or docs\n\n## Suggested approach\n\n```bash\n# /etc/cron.d/memwatch (example)\n* * * * * root [ $(awk '/MemAvailable/ {print int($2/1024)}' /proc/meminfo) -lt 2048 ] && logger -p user.warning \"LOW MEMORY: $(awk '/MemAvailable/ {printf \\\"%.0fMB\n\\\", $2/1024}' /proc/meminfo)\"\n```\n\nConsider also monitoring /proc/pressure/memory (PSI) for sustained pressure events, which is what journald's \\\"Under memory pressure\\\" messages derive from.\n\n## Key files\n\n- /proc/meminfo, /proc/pressure/memory, /var/log/sysstat (sar data if installed)\n- Discovered from: RCA of system hang 2026-08-20 (session analysis; no parent work item)","effort":"","id":"WL-0MT1KJNGA006EEHU","issueType":"task","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":6600,"stage":"idea","status":"open","tags":["rca","monitoring","memory"],"title":"Set up memory pressure monitoring and alerting","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-20T13:41:13.252Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Problem statement\n\nDuring the 2026-08-20 system hang, snapd was stuck and required force-kill by systemd:\n\n```\nAug 20 13:37:51 systemd[1]: snapd.service: Watchdog timeout (limit 5min)!\nAug 20 13:37:51 systemd[1]: snapd.service: Killing process 1734095 (snapd) with signal SIGABRT.\nAug 20 13:37:59 systemd[1]: snapd.service: Failed with result 'watchdog'.\nAug 20 13:38:12 systemd[1]: Starting snapd.service - Snap Daemon...\nAug 20 13:39:52 systemd[1]: snapd.service: start operation timed out. Terminating.\n```\n\nThe snapd SIGABRT stack dump shows the smoking gun: a udev monitor goroutine stuck in an infinite select that never receives events and never times out:\n\n```\ngoroutine 226 ... [select, 7740 minutes]:\n  github.com/snapcore/snapd/overlord/ifacestate/udevmonitor.(*Monitor).Run.func1()\n    /build/snapd/.../udevmonitor/udevmon.go:147\n```\n\nMultiple goroutines were stuck for hundreds of minutes (IO wait 388 minutes, sync.Cond.Wait 388 minutes). snapd held state locks that other services depended on, contributing to the freeze. The restart also timed out, forcing the reboot.\n\n## Acceptance criteria\n\n- [ ] Confirm whether the snapd udev monitor hang reproduces after a fresh boot (observe over several days; check for stuck goroutines via SIGQUIT dump if it recurs)\n- [ ] Research the upstream snapd issue: udevmonitor goroutine never waking from select (snapd bug tracker / launchpad); record the upstream bug reference in a comment\n- [ ] Apply mitigation: either update snapd to a fixed version, or if no fix exists, adjust snapd.service WatchdogSec (e.g. disable watchdog via `systemctl set-property snapd.service WatchdogSec=0` or increase it) and document the trade-off\n- [ ] Verify snapd stays responsive after mitigation: no watchdog timeouts over a 7-day observation window\n- [ ] Record the commit/bug references in comments on this work item\n\n## Key files / commands\n\n- `snap version` (currently snapd 2.76.2 on ubuntu/26.04)\n- `journalctl -b -1 -u snapd` for the stack dump evidence\n- Discovered from: RCA of system hang 2026-08-20 (session analysis; no parent work item)\n## Related work (automated report)\n\n### Related work items\n- **WL-0MT38GKEP0057T4I** – Document concurrency profile and memory footprint of chrome-headless instances (completed)\n- **WL-0MN7ZP9GX001XJE4** – Add unit test: show --json includes structured audit (present & absent) (completed)\n- **WL-0MSBVYH3W0066KJS** – wl process health check watchdog (scripts/wl-process-healthcheck.sh + CI regression gate) (completed)\n\n### Repository file matches\n- `packages/herdr/README.md` — matched: acceptance, also, analysis, apply, bug (+76 more)\n- `scripts/audit_runner.py` — matched: acceptance, adjust, also, analysis, apply (+76 more)\n- `CLI.md` — matched: adjust, also, apply, boot, bug (+67 more)\n","effort":"Extra Small","id":"WL-0MT1KJNHE0048HW3","issueType":"bug","needsProducerReview":true,"parentId":null,"priority":"high","risk":"Low","sortIndex":1500,"stage":"plan_complete","status":"in-progress","tags":["rca","snapd","systemd"],"title":"snapd udev monitor goroutine hang - watchdog kills and restart loops","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T13:41:13.448Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Problem statement\n\nThe system has recurring crashes that are not being reported or reviewed:\n\n- /var/crash/_usr_bin_node.1000.crash - a 616 MB crash dump from Aug 17 (node process)\n- 10+ chrome-headless int3 traps in the kernel log across Aug 18-20\n- /var/crash/kdump_lock and kexec_cmd exist but no kdump crash dumps are captured (kernel crash handling not generating reports)\n\nCrash dumps are accumulating (616 MB is significant) without review or reporting, meaning recurring crash patterns go unnoticed until they cause a system outage (as happened on 2026-08-20).\n\n## Acceptance criteria\n\n- [ ] Verify whoopsie/apport crash reporting is functional (systemctl status whoopsie, apport status) and report generation is enabled\n- [ ] Review the existing /var/crash/_usr_bin_node.1000.crash dump and record what it reveals (what crashed, why) in a comment on this work item; decide whether to keep or purge it (616 MB)\n- [ ] Confirm whether kernel crash reporting (kdump/kexec) is configured to capture the next kernel panic; if not, either configure it or document why it is intentionally disabled\n- [ ] Ensure crash dumps are rotated/aged so they cannot fill the disk (e.g. whoopsie retention settings or a cleanup job)\n- [ ] Document the crash-reporting setup and retention policy in a comment on this work item or docs\n\n## Key files / commands\n\n- /var/crash/, whoopsie/apport config (/etc/default/apport, /etc/whoopsie)\n- `apport-cli` / `whoopsie` status, `du -sh /var/crash`\n- Discovered from: RCA of system hang 2026-08-20 (session analysis; no parent work item)","effort":"","id":"WL-0MT1KJNMV0018SWP","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":6700,"stage":"idea","status":"open","tags":["rca","crash-reporting","whoopsie"],"title":"Enable automatic crash reporting and review existing crash dumps","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T13:58:56.300Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — This is a library module (grid.py), not a standalone script; modules are not expected to have shebangs, so the executable permission is likely a file-level permissions issue rather than a code defect.. candidate false positive — producer decision required.","effort":"","id":"WL-0MT1L6FQJ0040GB9","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":6800,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T15:44:47.062Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — grid.py is a Python module always invoked via 'python3 grid.py' (confirmed in open-pi-agent.sh and send-to-pi.sh line 125/172); the executable bit is incidental and a shebang is never needed, so EXE002 misfires.. candidate false positive — producer decision required.","effort":"","id":"WL-0MT1OYK0M0002U07","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":6900,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T17:35:03.729Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — This is a Python library module (starts with module docstring), not a standalone script; executable permissions on a library module are likely set incorrectly and a shebang is not expected for importable modules. candidate false positive — producer decision required.","effort":"","id":"WL-0MT1SWDGW0005F2X","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":7000,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T17:55:13.568Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — grid.py is never executed directly via shebang — shell scripts (open-pi-agent.sh, send-to-pi.sh) and tests always invoke it as `python3 grid.py` — so the missing shebang is intentional and the executable bit is vestigial; not a real defect.. candidate false positive — producer decision required.","effort":"","id":"WL-0MT1TMAZJ002LJET","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":7100,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T18:10:23.823Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — grid.py is always invoked via `python3 grid.py` (see open-pi-agent.sh:125) and imported by tests, so it is not meant to be executed directly; the executable bit is an accidental permission artifact and the missing shebang is not a code defect.. candidate false positive — producer decision required.","effort":"","id":"WL-0MT1U5TCF002HRBA","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":7200,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T19:25:01.192Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — This is a library module in packages/herdr/shared/, not a script; EXE002 targets scripts missing shebangs, whereas the real issue here is incorrect file permissions (executable bit should be removed, not a shebang added). candidate false positive — producer decision required.","effort":"","id":"WL-0MT1WTS3S009MMGY","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":7300,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T23:40:11.586Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"navigating within the Herdr plugin is sluggish, moving up and down in the selection list is slow, typing into an input screen, (e.g. P-p) is slow and letters are often missed. Perform a full RCA and look for opportunities to impove the speed","effort":"","id":"WL-0MT25XXOI000DWP5","issueType":"","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":7400,"stage":"idea","status":"open","tags":[],"title":"Herdr plugin is sluggish","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-20T23:40:34.586Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — This is a shared library module (grid.py in shared/), not a standalone script; the executable bit is likely set erroneously and the correct fix is to remove it, not add a shebang.. candidate false positive — producer decision required.","effort":"","id":"WL-0MT25YFFE000U4F6","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":7500,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T00:01:15.618Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"The intention of the herdr selection list is that it will show all items that are in_review, plus all critical items that are not yet in_review or done. Plus, if there is space, up to 20 other items. At the time of writing this work item there are 28 items shown by the f-s-r (in_review) filter while the f-s-s (sprint) filter shows only 19 in_review items.","effort":"","id":"WL-0MT26P10H00465AN","issueType":"","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":7600,"stage":"idea","status":"open","tags":[],"title":"The Herdr workitem selection list should show all in_review items","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T00:04:39.327Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"As I write this work item the herdr selection list for this project is claiming to show 24 items (top 24 of 100). However, the group totals show 19 in_review, 1 critical group 1 and 1 critical group 2. The heading counts seem accurate.","effort":"","id":"WL-0MT26TE72002FLKX","issueType":"","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":7700,"stage":"idea","status":"open","tags":[],"title":"counts in Herdr selection list are incorrect","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T04:30:18.576Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Create @worklog/shared/icons module\n\n## Goal\nExtract dependency-free icon and colour data from  into a new  module so the herdr package (which cannot import -dependent ) can consume a single shared source of truth.\n\n## Scope\n- Create  with all icon maps and functions from  (the icon/colour data portion — pure data, no chalk imports)\n- Add  and  functions (needed by herdr's worklist)\n- Add  function (needed by herdr's downtime-worker)\n- Add  function (needed by herdr's tests and worklist prefix)\n- Export  interface\n- Add export to  ( sub-path)\n- No chalk dependency — the icon data in theme.ts is already pure data (emoji maps, text fallbacks)\n\n## Acceptance Criteria\n1.  exists and exports all icon/colour data types and functions\n2.  includes the  export sub-path\n3.  can be imported by herdr without pulling in chalk\n4. The shared icons module is built successfully by \n> worklog@1.0.11 build\n> npm run build:shared && tsc && node ./scripts/generate-version.cjs\n\n\n> worklog@1.0.11 build:shared\n> cd packages/shared && npm run build\n\n\n> @worklog/shared@1.0.0 build\n> tsc\n\n\n> worklog@1.0.11 postbuild\n> bash ./scripts/install-herdr.sh\n\n\nadded 40 packages, and audited 41 packages in 640ms\n\n9 packages are looking for funding\n  run `npm fund` for details\n\nfound 0 vulnerabilities\nInstalled herdr plugin dependencies (npm ci: /home/rgardler/projects/ContextHub/packages/herdr)\n{\"id\":\"cli:plugin\",\"result\":{\"plugin\":{\"actions\":[{\"command\":[\"bash\",\"scripts/open-pi-agent.sh\"],\"description\":\"Open a Pi AI coding agent pane docked on the right.\",\"id\":\"open-pi-agent\",\"title\":\"Open Pi Agent\"},{\"command\":[\"bash\",\"scripts/open-podcast-editor-tab.sh\"],\"description\":\"Open the Worklog work item selection pane in a new tab renamed to 'Podcast Editing'.\",\"id\":\"open-podcast-editor-tab\",\"title\":\"Open Podcast Editing tab\"},{\"command\":[\"bash\",\"scripts/open.sh\"],\"description\":\"Open the Worklog work item selection pane.\",\"id\":\"open-worklist\",\"title\":\"Open worklist\"},{\"command\":[\"bash\",\"scripts/toggle.sh\"],\"description\":\"Open the Worklog work item selection pane (focus it if open; close it if focused).\",\"id\":\"toggle-worklist\",\"title\":\"Toggle worklist\"}],\"build\":[{\"command\":[\"npm\",\"run\",\"build\"]}],\"description\":\"Browse, filter, and select Worklog work items from a Herdr pane. Keyboard-navigable list with stage/priority filtering and detail view.\",\"enabled\":true,\"manifest_path\":\"/home/rgardler/projects/ContextHub/packages/herdr/herdr-plugin.toml\",\"min_herdr_version\":\"0.7.0\",\"name\":\"Worklog Selection List\",\"panes\":[{\"command\":[\"npx\",\"tsx\",\"src/index.ts\"],\"id\":\"worklist\",\"placement\":\"tab\",\"title\":\"Work Items\"}],\"platforms\":[\"linux\",\"macos\",\"windows\"],\"plugin_id\":\"worklog-selection-list\",\"plugin_root\":\"/home/rgardler/projects/ContextHub/packages/herdr\",\"source\":{\"kind\":\"local\"},\"version\":\"0.1.0\"},\"type\":\"plugin_linked\"}}\nLinked herdr plugin: worklog-selection-list (/home/rgardler/projects/ContextHub/packages/herdr/herdr-plugin.toml)\nherdr keybinding already present: /home/rgardler/.config/herdr/config.toml in packages/shared\n5. Icon function signatures match the current  exports (no API changes)\n\n## Deliverables\n-  — new file\n-  — updated exports\n-  +  — generated by build\n- All existing tests in packages/shared still pass\n","effort":"","id":"WL-0MT2GB100005NKT0","issueType":"","needsProducerReview":false,"parentId":"WL-0MSJ4BT4Z002HH9B","priority":"high","risk":"","sortIndex":2400,"stage":"plan_complete","status":"open","tags":[],"title":"Create @worklog/shared/icons module with dependency-free icon/colour data","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T04:35:39.038Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"@file:/tmp/desc2.txt","effort":"","id":"WL-0MT2GHW9Q003T19L","issueType":"","needsProducerReview":false,"parentId":"WL-0MSJ4BT4Z002HH9B","priority":"high","risk":"","sortIndex":10700,"stage":"plan_complete","status":"blocked","tags":[],"title":"Remove src/icons.ts and rewire CLI consumers","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T04:36:21.921Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Remove packages/herdr/src/icons.ts and rewire herdr consumers\n\n## Goal\nDelete `packages/herdr/src/icons.ts` (the standalone herdr icon copy) and rewire all herdr consumers to import from `@worklog/shared/icons`.\n\n## Scope\n- Delete `packages/herdr/src/icons.ts`\n- Update `packages/herdr/src/worklist.ts` to import from `@worklog/shared/icons`\n- Update `packages/herdr/src/downtime-worker.ts` to import from `@worklog/shared/icons`\n- Update `packages/herdr/src/worklist-agent-state.test.ts` to import from `@worklog/shared/icons`\n- Update `packages/herdr/src/icons.test.ts` to import from `@worklog/shared/icons`\n- Update `tests/herdr/icons.test.ts` to import from `@worklog/shared/icons`\n- Update `packages/herdr/README.md` directory tree listing to remove icons.ts reference\n- Verify herdr build succeeds and all tests pass\n\n## Acceptance Criteria\n1. `packages/herdr/src/icons.ts` is deleted\n2. All herdr consumers import from `@worklog/shared/icons` instead of `./icons.js`\n3. `packages/herdr` builds successfully (npm run build)\n4. Herdr test suite passes\n5. `grep -r \"from.*icons\" packages/herdr/src/ | grep -v shared/icons` finds no local icons imports\n6. No behaviour change — icons/colours render identically\n\n## Deliverables\n- Deleted: packages/herdr/src/icons.ts\n- Modified: packages/herdr/src/worklist.ts, packages/herdr/src/downtime-worker.ts, packages/herdr/src/worklist-agent-state.test.ts, packages/herdr/src/icons.test.ts, tests/herdr/icons.test.ts, packages/herdr/README.md\n\n## Dependencies\n- Depends on: WL-0MSJ4BT4Z005NKT0 (shared/icons module must exist first)","effort":"","id":"WL-0MT2GITCX009WUEW","issueType":"","needsProducerReview":false,"parentId":"WL-0MSJ4BT4Z002HH9B","priority":"high","risk":"","sortIndex":10800,"stage":"plan_complete","status":"blocked","tags":[],"title":"Remove packages/herdr/src/icons.ts and rewire herdr consumers","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T04:39:28.476Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Add repo-wide grep guard and finalize documentation\n\n## Goal\nAdd a repo-wide grep-based guard (CI or test suite) asserting no stale `icons.js` imports remain, and update all related documentation.\n\n## Scope\n- Add a grep-based test in the test suite that scans all .ts/.tsx/.js/.jsx files for imports from `icons.js` or `icons.ts` paths (excluding node_modules, dist, .worklog)\n- Update `docs/icons-design.md` to reflect the removal of both icons.ts files\n- Update `tests/herdr/icons.test.ts` to test from the shared source\n- Update README.md to reflect new mock patterns (if the icons.js mock is no longer needed)\n\n## Acceptance Criteria\n1. A repo-wide grep test exists and passes\n2. `grep -r \"from.*icons\" --include=\"*.ts\" .` (excluding node_modules, dist, .worklog) finds no imports from removed icons paths\n3. docs/icons-design.md is updated — no references to deprecated wrapper or herdr copy\n4. README.md is updated if icons.js mock reference is obsolete\n5. Full project test suite passes\n\n## Deliverables\n- New or modified test file with grep guard\n- Updated docs/icons-design.md\n- Updated README.md (if needed)\n\n## Dependencies\n- Depends on: WL-0MSJ4BT4Z005NKT0, WL-0MSJ4BT4Z009LIB4 (both removals must be complete first)","effort":"","id":"WL-0MT2GMTAZ003VKVL","issueType":"","needsProducerReview":false,"parentId":"WL-0MSJ4BT4Z002HH9B","priority":"high","risk":"","sortIndex":11000,"stage":"plan_complete","status":"blocked","tags":[],"title":"Add repo-wide grep guard and finalize documentation","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T09:09:00.684Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Implement auto-downgrade of critical priority on terminal transitions\n\n## Goal\nWhen a work item with `priority=critical` transitions to a terminal state (status=completed or stage=in_review), automatically downgrade its priority to `high`.\n\n## Implementation Details\n\n### 1. Downgrade Logic\n\nAdd a helper function to detect and perform the auto-downgrade:\n- Location: `packages/shared/src/database.ts` alongside the existing `cascadePriorityDowngrade`\n- Function: `downgradeIfTerminal(item)` or inline check\n- Logic: if item's new state has `status=completed` or `stage=in_review` AND `priority=critical`, set `priority=high`\n\n### 2. Call Sites\n\nInvoke from terminal-transition paths:\n- `src/commands/update.ts` — after status/stage update, before response\n- `src/commands/close.ts` — after close logic, before response\n\nPrefer explicit call sites to avoid interfering with internal DB `update()` cascade calls.\n\n### 3. Reporting\n\nReport the downgrade in both output formats, mirroring the existing `downgradedChildren` pattern:\n- JSON output: add `downgradedItem` field (or similar) with `{id, priority}` \n- Human-readable: add a summary line like `Downgraded priority from critical to high`\n\n### 4. Constraints\n\n- Only affect `critical` → `high` downgrade\n- No change to other priorities (high/medium/low remain untouched)\n- Only trigger on terminal transitions: `--status completed`, `--stage in_review`, or `wl close`\n- Non-terminal transitions (e.g., `idea` → `plan_complete`) must NOT change priority\n\n### Key Files\n- `src/commands/update.ts` — terminal transition handling\n- `src/commands/close.ts` — close path\n- `packages/shared/src/database.ts` — helper function location\n- `src/validators/priority.ts` — priority constants","effort":"","id":"WL-0MT2Q9FUZ002MZU2","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSJM4EIV001A0V9","priority":"high","risk":"","sortIndex":11100,"stage":"intake_complete","status":"blocked","tags":[],"title":"Implement auto-downgrade of critical priority on terminal transitions","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T09:10:45.022Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Write unit tests for auto-downgrade of critical priority\n\n## Goal\nAdd comprehensive unit tests covering the auto-downgrade behavior for critical priority items transitioning to terminal states.\n\n## Test Coverage Required\n\nPer Acceptance Criteria 4 of parent (WL-0MSJM4EIV001A0V9):\n\n1. **`wl update --status completed`** — verify critical priority downgraded to high\n2. **`wl update --stage in_review`** — verify critical priority downgraded to high\n3. **`wl close`** — verify critical priority downgraded to high\n4. **No-op: non-critical priorities** — verify high/medium/low unchanged on terminal transition\n5. **No-op: non-terminal transitions** — verify critical priority unchanged when moving between non-terminal stages (e.g., `idea` → `plan_complete`)\n\n## Implementation Notes\n\n- Tests should use the existing test infrastructure (see `tests/database.test.ts` and `tests/cli/`)\n- Follow the existing test patterns for `cascadePriorityDowngrade` tests\n- Tests should verify both the priority change AND the reporting output (AC3 of parent)\n- Consider both JSON and human-readable output paths\n\n### Key Files\n- `tests/database.test.ts` — database layer tests\n- `tests/cli/` — CLI command tests (update, close)","effort":"","id":"WL-0MT2QBOD9003JZ5T","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSJM4EIV001A0V9","priority":"high","risk":"","sortIndex":2500,"stage":"intake_complete","status":"open","tags":[],"title":"Write unit tests for auto-downgrade of critical priority","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T10:00:27.466Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"In the herdr selection list double clicking a header should expand or collapse that section","effort":"","id":"WL-0MT2S3LMW0016DN3","issueType":"","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":7800,"stage":"idea","status":"open","tags":[],"title":"double clicking a heading should expand/collapse it","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T11:14:34.107Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Goal (user story)\n\nAs an operator, when the local LLM is idle, I want the herdr downtime\nworker to avoid starting **non-critical** implementations while the\ncompleted/in_review review queue is already deep enough to fill the\nselection list, so the producer-review bottleneck is not overloaded.\nAuditing, intake, and planning can still be dispatched — only the\nimplement tier is gated.\n\n## Background\n\n- Dispatch tier order (dispatchDowntimeWork): scheduled prompts → audit →\n  implement → plan → intake. The code-freeze gate (WL-0MSQ0RPQP00636JY)\n  already skips audit+implement during a release; this is a second,\n  orthogonal implement-tier-only throttle on the producer-review queue.\n- The herdr selection list displays up to `browseItemCount` items\n  (default 20, clamp 1–50, packages/herdr/src/settings.ts).\n- Review-queue count = completed/in_review **root** items\n  (`wl list --status completed --stage in_review --root-only --json`) —\n  same query family as the audit tier (WL-0MSI8H3HP000K0RG) and the\n  selection list's mandatory review subset (WL-0MS8W5LTW006YZ4B).\n  Operator decision (2026-08): count is ROOT-ONLY, not all items.\n- Implement candidates carry `priority` (critical/high/medium/low) via\n  parseImplementCandidatesOutput.\n\n## Design notes\n\n- Gate: when `reviewQueueCount >= browseItemCount`, the implement tier\n  selects only candidates with `priority === 'critical'`; non-critical\n  candidates are excluded and dispatch falls through to plan/intake.\n- Critical priority BYPASSES the gate — a critical implement always stays\n  eligible regardless of queue depth.\n- Fail-closed: a wl error counting the review queue → gate active (only\n  critical implements), consistent with the code-freeze \"ambiguous ⇒\n  frozen\" convention.\n- `browseItemCount` is re-read live from settings on each dispatch (no\n  plugin restart needed), matching the existing config() re-read pattern.\n- The gate skip must NOT look like an empty backlog: when implement\n  candidates exist but are gated AND plan/intake are empty, the outcome\n  must be a neutral reason (never 'no-candidate'), so the worker's\n  no-candidate cooldown is not triggered — polling continues and a\n  critical implement / new plan / intake item dispatches immediately\n  (mirrors the code-freeze 'code-freeze' reason convention).\n\n## Acceptance criteria\n\n1. A critical-priority implement candidate is always eligible regardless\n   of review-queue depth (critical bypasses the gate).\n2. When reviewQueueCount < browseItemCount, implement-tier selection is\n   UNCHANGED: all priorities eligible; risk/effort/dispatched-marker\n   filters unchanged.\n3. When reviewQueueCount >= browseItemCount, non-critical implement\n   candidates are excluded; if only non-critical candidates remain,\n   getNextImplementCandidate resolves null and dispatch falls through to\n   plan/intake (no-candidate only when plan AND intake are genuinely\n   empty; a gate skip with an empty plan/intake backlog is a neutral\n   reason, never 'no-candidate').\n4. The gate applies ONLY to the implement tier — audit, plan, intake,\n   and scheduled-prompt dispatch are unaffected.\n5. The review-queue count query is bounded by DOWNTIME_WL_TIMEOUT_MS and\n   fail-closed (error ⇒ gate active).\n6. Tests cover: gate boundary (count == listSize − 1 vs count == listSize),\n   critical bypass, root-only counting, fall-through outcome, and the\n   neutral (no-cooldown) reason on a gate skip with empty plan/intake.\n\n## Related work\n\n- WL-0MSMAYPQP001FLR6 — Implement tier end-to-end in herdr downtime worker\n- WL-0MSI8H3HP000K0RG — Audit tier (completed/in_review without valid audit)\n- WL-0MSQ0RPQP00636JY — Code-freeze gate\n- WL-0MS8W5LTW006YZ4B — Selection list must always show all\n  completed/in_review and critical items\n","effort":"S","id":"WL-0MT2UQWOR007CYY9","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"Low","sortIndex":7900,"stage":"idea","status":"open","tags":[],"title":"Downtime dispatch: gate non-critical implement tier on review-queue depth","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T11:29:08.383Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"When I run a command within the herdr plugin it automatically tracks the progress of that pane and reflects it in the selection list. However, when a work item is dispatched in downtime that does not happen. We need the behaviour to be the same regardless of how a pane is launched.","effort":"","id":"WL-0MT2V9NA70035F0K","issueType":"","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":8000,"stage":"idea","status":"open","tags":[],"title":"Not tracking dispatched panes","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T14:49:21.662Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"As an operator, I do not want the herdr downtime worker to inhibit dispatch based on the GLOBAL active_query signal when the proxy serves the local-only signal, so that remote/cloud streams never block local downtime dispatch. The backward-compatibility fallback to the global `active_query` for pre-fix proxies must be removed.\n\n## Background\n\n- `isIdleStatus`/`globalIdleChecks` in packages/herdr/src/downtime-worker.ts: `const queryActive = status.local_active_query !== undefined ? status.local_active_query : status.active_query;` — the fallback to `active_query` exists for proxies that predate the `local_active_queries` counter (LP-0MSL2ZLLS009RVKR, delivered in WL-0MSL2ZQIF006QB4Q).\n- The global `active_query` is true whenever ANY request is in flight — local AND remote provider streams. Remote streams keep it true while the local model is idle with free slots, so the fallback can block legitimate downtime dispatch (RCA WL-0MSK9TUCA00206M7).\n- The local-only signal (`local_active_query`) is the correct busy signal for the local model.\n\n## Design notes\n\n- Remove the `local_active_query !== undefined ? ... : active_query` fallback; use only `local_active_query`.\n- Fail-closed question to resolve: is a MISSING `local_active_query` now treated as busy (no dispatch) or idle (dispatch)? Decision: treat a missing signal as busy (fail-closed, consistent with the rest of the worker) — a pre-fix proxy must not cause dispatch on an unverifiable signal.\n- parseLlamaStatus currently accepts an absent `local_active_query` (`undefined`); a work item must decide whether absence becomes null (busy) or a documented explicit non-dispatch.\n- Update parseLlamaStatus doc + isIdleStatus/globalIdleChecks comments (LP-0MSL2ZLLS009RVKR reference).\n\n## Acceptance criteria\n\n1. `globalIdleChecks` uses ONLY `local_active_query` once present in the payload; the `active_query` fallback is removed.\n2. A payload WITHOUT `local_active_query` fails closed: the worker treats the status as busy (no dispatch) and the behaviour is documented — no silent degraded dispatch on pre-fix proxies.\n3. Remote/cloud streams (global active_query true, local false) are never a dispatch inhibitor.\n4. Test suite covers: local_active_query present (both values), local_active_query absent (busy), and the removal of the fallback branch.\n","effort":"","id":"WL-0MT32F4SD003KC2V","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"low","risk":"","sortIndex":10400,"stage":"idea","status":"open","tags":[],"title":"Downtime idle detection: remove fallback to global active_query when local_active_query absent (fail-closed busy)","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T14:49:27.129Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"As an operator, I do not want a transient failure in the downtime dispatch pipeline to strand a work item in `in_progress` with no agent working it and no automatic recovery.\n\n## Background\n\n- dispatchClaimedTier (packages/herdr/src/downtime-worker.ts) pipeline: CAS claim → marker write → spawn.\n- Gap 1 from the dispatch decision-point evaluation: if the marker write fails (D16) AFTER the CAS claim succeeds, the outcome is reason 'marker-write-failed' and the item stays `in_progress` — but the claim is never rolled back. The item is then invisible to every later tier: plan/implement tiers select `status === 'open'`, the audit tier selects `completed`. Only manual intervention recovers it.\n- Same class of issue on 'spawn-failed' (D17): the marker stands (kind `implement`/`plan`/`audit` at the dispatched-at stage), so even after a human reverts the item to `open`, the dispatched-marker exclusion keeps it out of selection until the stage changes — a transient send-to-pi.sh failure (e.g. ENOENT) permanently removes a never-worked item from the idle-slot queue.\n- Existing design intent (RCA WL-0MSRBFFLN005W3VT design point 2): \"an unmarked item is never dispatched\" — this is sound; the gap is the missing RECOVERY path, not the fail-closed abort.\n\n## Design notes\n\n- Option A (rollback): on 'marker-write-failed', restore the item to its pre-claim state (CAS reverse: `wl update <id> --status open --if-status in_progress [--if-stage ...]`) so the next idle period can select it again.\n- Option B (requeue for spawn-failed): on 'spawn-failed', write a distinct marker variant (e.g. `outcome: 'spawn-failed'` entries are already appended) and have the marker-exclusion readers treat a spawn-failed marker as NON-excluding (item is re-eligible) while still logging the failure — the rolling log already distinguishes attempted vs opened (WL-0MSLWJ3I70031Z8U AC2).\n- Both options preserve the fail-closed invariant (a marker-less item is never dispatched twice concurrently); the CAS claim already serializes re-selection.\n- Decide and document whether the re-dispatch window should be immediate (next idle) or after a short backoff to avoid spawn-failure hot loops.\n\n## Acceptance criteria\n\n1. 'marker-write-failed' leaves the item in a state selectable by a future tier (rolled back to open+original stage, or equivalently recovered) — never stranded in_progress with no agent.\n2. 'spawn-failed' does not permanently exclude the item from selection; the failure trace remains in the rolling log and the outcome is still not success.\n3. No regression: an item with a STANDING success marker is still excluded (never double-dispatched); CAS claim still serializes concurrent panes.\n4. Tests cover marker-write-failed and spawn-failed recovery paths end-to-end (claim → failure → re-selection on the next idle period).\n","effort":"","id":"WL-0MT32F908002YFFA","issueType":"bug","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":8100,"stage":"idea","status":"open","tags":[],"title":"Downtime dispatch: recover items stranded by marker-write-failed or spawn-failed (no permanent->in_progress stranding)","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T14:49:27.152Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Downtime Dispatch: Spare-Capacity Dispatch (Default N Instead of All-Slots-Free)\n\n## Problem Statement\n\nThe herdr downtime worker currently requires ALL llama-proxy slots to be free before dispatching downtime work. With a 3-slot proxy and 1 slot occupied by an active operator session, the worker does nothing — wasting 2 available slots of compute capacity.\n\n## Users\n\n- **Local AI operator** (primary): Wants to work on a task via the TUI while the llama-proxy runs downtime audits/dispatches on spare slots simultaneously.\n- **Producer/reviewer**: Benefits from faster audit throughput when spare compute is utilized during operator work sessions.\n\n## Acceptance Criteria\n\n1. When 2 slots (of 3) have been continuously free for the full idle threshold, downtime dispatch fires — even if 1 slot is occupied by an active (non-processing) session.\n2. A mid-query (processing) session on a busy slot does NOT block dispatch into free slots when per-slot identity mode is active.\n3. An audit dispatch is only selected when ≥2 slots are free; implement/plan/intake panes require ≥1 free slot.\n4. Pre-fix proxies (no per-slot identity) retain the existing all-slots-free fail-closed behaviour; ambiguous payloads are treated as busy.\n5. Tests cover: 3 slots/1 busy (dispatch into 2 free), N free of total with a mid-query busy slot, audit 2-slot minimum, old-proxy degradation unchanged.\n6. All related documentation is updated to reflect the changes, including code comments, README, and any relevant wiki or docs site entries.\n7. Full project test suite must pass with the new changes.\n\n## Constraints\n\n- Preserve fail-closed conventions: ambiguous slot payloads must be treated as busy.\n- Per-slot identity detection (proxy serves `slots: [{slot_id, is_processing}]`) must remain opt-in; default behavior for old proxies unchanged.\n- CAS claim still serializes concurrent pane dispatches.\n- Parallelism accounting: audit phases need `AUDIT_PHASE2_PARALLELISM=1` → 2 slots minimum.\n- The N > total rule must be preserved (never idle).\n- **Default N = 2 of 3 slots** (confirmed decision).\n\n## Existing State\n\n- `DEFAULT_DOWNTIME_REQUIRED_FREE_SLOTS = 0` means all slots must be free (`available_slots >= total_slots`).\n- `globalIdleChecks` rejects when ANY local query is in flight or a lease is active — blocks spare-capacity regardless of free slots.\n- Per-slot identity + same-slot idle tracking already exists (completed WL-0MSG7P9N8009PCKG, WL-0MSP28LSY007NDYX) but the default and global-busy gate prevent its use for this scenario.\n\n## Desired Change\n\n- Relax the idle gate in per-slot mode: evaluate idle from free slots only, not all slots.\n- A query/lease on a busy slot should not block dispatch into 2 free slots (the busy slot that is actively querying is precisely the \"active work item\" the operator wants to work alongside).\n- Set `DEFAULT_DOWNTIME_REQUIRED_FREE_SLOTS` to 2 (of 3), so spare-capacity dispatch works out-of-the-box.\n- Implement per-slot idle duration tracking in `evaluateIdle` when per-slot data is present and `0 < N < total`.\n\n## Key Files (predicted)\n\n- `packages/herdr/src/downtime-worker.ts` — Core idle evaluation and dispatch logic\n- `packages/herdr/src/downtime-worker.ts` (constants) — `DEFAULT_DOWNTIME_REQUIRED_FREE_SLOTS`, `AUDIT_PHASE2_PARALLELISM`\n- `packages/herdr/src/downtime-worker.ts` (`evaluateIdle`) — Per-slot idle tracking logic\n- `packages/herdr/src/downtime-worker.ts` (`globalIdleChecks`) — Global busy checks to relax in per-slot mode\n- `packages/herdr/src/herdr-proxy.ts` (or similar) — Proxy slot data parsing\n- `packages/herdr/tests/` — Test fixtures and assertions\n\n## Related Work\n\n- **WL-0MSG7P9N8009PCKG** — Same-slot idle tracking for herdr downtime worker (per slot_id) [COMPLETED]\n- **WL-0MSP28LSY007NDYX** — Per-slot identity mechanism [COMPLETED]\n- **WL-0MSZ4NSOE007AQEF** — Shortcut key to toggle downtime dispatch per herdr instance [IN PROGRESS]\n- **WL-0MSGPI4AR000YOK8** — Log herdr downtime dispatches [COMPLETED]\n- **WL-0MSZS1OHV0067JD1** — Documentation: README update for `d` shortcut and `[Downtime Off]` [COMPLETED]\n\n## Appendix: Clarifying Questions\n\n- Q: \"Does the llama-proxy (192.168.0.199:8000, `GET /slots` broadcast) support concurrent independent requests on different slots?\" — Answer: Yes, the router allows a new local dispatch while another session holds a lease / is mid-query. Source: interactive reply (2026-08-21).\n- Q: \"What should DEFAULT_DOWNTIME_REQUIRED_FREE_SLOTS default to for out-of-the-box spare-capacity dispatch?\" — Answer: 2 (of 3 slots), so at least 1 slot is reserved for operator sessions. Source: interactive reply (2026-08-21).\n- Q: \"Should the setting be a fixed default (e.g. 2 of 3) or a configurable 'reserve N slots for operator' setting?\" — Answer: Fixed default of 2 of 3. Source: interactive reply (2026-08-21).\n","effort":"Small","id":"WL-0MT32F90V008UAD2","issueType":"feature","needsProducerReview":true,"parentId":null,"priority":"high","risk":"Medium","sortIndex":3500,"stage":"plan_complete","status":"open","tags":[],"title":"Downtime dispatch: use available slots while some slots busy (spare-capacity dispatch, default N instead of all-slots-free)","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T19:49:28.817Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# F1: Spare-capacity dispatch test suite\n\nTest-first spine for WL-0MT32F90V008UAD2 (spare-capacity dispatch, default N instead of all-slots-free). Pin the new behavior end-to-end before implementation lands.\n\n## Goal\n\nWrite/extend red-first tests covering: dispatch into free slots while a busy slot holds the operator session (per-slot identity mode), tier-specific free-slot minimums, and unchanged fail-closed behavior for pre-fix proxies.\n\n## Acceptance Criteria\n\n1. 3 slots / 1 busy: when 2 slots have been continuously free for the full idle threshold, dispatch fires into the 2 free slots even though 1 slot is occupied by an active non-processing session (parent AC1/AC5a).\n2. N free of total with a mid-query (processing) busy slot: idle is evaluated from the free slots only in per-slot mode (parent AC2/AC5b).\n3. Audit tier is only selected when ≥2 slots are free; implement/plan/intake tiers when ≥1 slot is free (parent AC3/AC5c).\n4. Pre-fix proxy (no per-slot identity) with a configured N (0<N<total) degrades to all-slots-free — behavior unchanged (parent AC4/AC5d).\n5. Ambiguous payloads (missing/non-finite fields, total_slots 0, malformed slots) are treated as busy; N > total is never idle.\n6. The full herdr test suite passes with the new tests (parent AC7 baseline).\n\n## Key Files (predicted)\n\n- `packages/herdr/src/downtime-worker.test.ts` — primary test file (per-slot evaluateIdle, dispatch tier minimums, proxy degradation)\n- `packages/herdr/src/downtime-worker.fixtures.ts` — per-slot fixtures (3 slots/1 busy, mid-query busy slot, pre-fix proxy payloads)\n- `packages/herdr/src/downtime-worker.ts` — implementation under test (read-only reference)\n\n## Implementation Notes\n\n- Prefer table-driven `it.each` for slot-count permutations.\n- Reuse existing fixtures (`perSlotAllFree`, `perSlotTwoFree`, `perSlotOneProcessing`) and extend where the fails are ambiguous.\n- Red-phase tests land first (expected to fail until F2/F3 land), matching the plan's test-first ordering.","effort":"S","id":"WL-0MT3D53740051JVO","issueType":"task","needsProducerReview":false,"parentId":"WL-0MT32F90V008UAD2","priority":"high","risk":"Medium","sortIndex":2600,"stage":"plan_complete","status":"open","tags":[],"title":"F1: Spare-capacity dispatch test suite","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-21T19:49:35.218Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# F3: Per-tier dispatch free-slot minimums\n\nDispatch-selection slice for WL-0MT32F90V008UAD2: enforce tier-specific minimum free slots at selection time — audit ≥2, implement/plan/intake panes ≥1.\n\n## Goal\n\nAn audit dispatch (which needs a second slot for Phase 2 parallelism at `AUDIT_PHASE2_PARALLELISM=1`) must only be selected when ≥2 slots are free; the pane tiers (implement/plan/intake) need ≥1 free slot. Minimums are evaluated against the latest polled status at selection time, independent of the idle-duration gate.\n\n## Acceptance Criteria\n\n1. Audit tier dispatches only when ≥2 slots are free (parent AC3).\n2. Implement/plan/intake tiers dispatch only when ≥1 slot is free (parent AC3).\n3. Scheduled-prompt tier follows the pane minimum (≥1 free slot).\n4. Minimums evaluated against the latest status at selection time; CAS claim still serializes concurrent pane dispatches (constraints preserved).\n5. The idle-duration gate (configured N, default 2) is unchanged — tier minimums are an additional selection-time check.\n6. All F1 red tests for tier minimums turn green; full suite passes (parent AC7).\n\n## Key Files (predicted)\n\n- `packages/herdr/src/downtime-worker.ts` — `dispatchDowntimeWork` tier selection (pass latest free-slot count into tier checks; constants for audit min 2 and pane min 1)\n- `packages/herdr/src/downtime-worker.test.ts` — dispatch-level tier-minimum tests\n\n## Implementation Notes\n\n- Plumb the latest polled free-slot count (or the parsed status) into `dispatchDowntimeWork` so each tier can apply its minimum before the claim.\n- Reuse the same fail-closed counting rule: an entry without an explicit boolean `is_processing` is busy, never free.\n- Scheduled-prompts and pane tiers share the `PANE_MIN_FREE = 1` constant; audit uses `AUDIT_MIN_FREE = 2`.","effort":"S","id":"WL-0MT3D584X000UD9K","issueType":"feature","needsProducerReview":false,"parentId":"WL-0MT32F90V008UAD2","priority":"high","risk":"Low","sortIndex":11200,"stage":"plan_complete","status":"blocked","tags":[],"title":"F3: Per-tier dispatch free-slot minimums","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-22T01:35:21.570Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Ensure only single audit active during downtime dispatch\n\n## Problem statement\n\nDuring downtime dispatching through the leader, only a single audit should ever be active at any one time. The dispatch logic must check for existing active audits (dispatched but not yet completed) and skip dispatching another audit if one is already in progress, ensuring audits are strictly sequential rather than allowing fan-out.\n\n## Users\n\n- **Operator running multiple herdr instances** — wants audit work to progress deterministically, one at a time, without competing audits consuming LLM capacity.\n- **Operator relying on audit results for release decisions** — needs audit outcomes to be reliable and complete; concurrent audits increase the risk of missed findings.\n\n## Acceptance Criteria\n\n1. **Audit sequential enforcement** — Before dispatching an audit-tier item, the dispatcher checks whether any audit-tier item is currently `in_progress` (dispatched but not yet completed/reviewed). If one exists, the audit tier is skipped and the dispatcher falls through to the next tier.\n2. **Leader-only check** — Only the elected leader performs the active-audit check; non-leader instances do not dispatch audits at all (unchanged behavior).\n3. **Check mechanism** — The dispatcher queries the worklog for any audit-tier item (completed/in_review candidates) that has a corresponding dispatch entry in `downtime-dispatches.log` with kind `audit`. If the dispatched audit entry is older than a stale threshold (2 hours), it is treated as stale and ignored (audit pane may have crashed without updating the work item).\n4. **Active audit blocks new dispatch** — If an active (non-stale) audit is found, the audit tier is skipped and dispatch falls through to the implement tier. The skip reason `audit-in-flight` is logged for observability.\n5. **Existing guards preserved** — The single-flight guard (`dispatchInFlight`), CAS claim, dispatched-marker exclusion, and code-freeze gate continue to work unchanged.\n6. **All related documentation is updated** to reflect the changes, including code comments, README, and any relevant wiki or docs site entries.\n7. **Full project test suite must pass** with the new changes.\n\n## Constraints\n\n- **Audit tier only** — this constraint applies only to the audit dispatch tier; implement/plan/intake tiers are unaffected.\n- **Existing single-flight guard** — the in-process `dispatchInFlight` guard remains in place; this adds a cross-idle-period guard (prevents re-dispatch on the next idle tick while a previous audit is still running).\n- **Coordination file aware** — in the leader-election refactor context, the check must consider audits dispatched by any instance (not just the current instance), using the coordination file or dispatch log as the source of truth.\n- **Fail-safe** — if the active-audit check cannot complete (e.g., worklog query fails), the dispatcher falls through to the next tier rather than blocking all dispatch.\n\n## Existing state\n\n- `dispatchDowntimeWork` dispatches audit-tier items via `dispatchClaimedTier(deps, 'audit', ...)`.\n- The `dispatchInFlight` guard prevents concurrent dispatch calls within the same idle period.\n- CAS claim moves dispatched items to `in_progress`, removing them from `wl next`/`wl list` selection.\n- Dispatched markers in `downtime-dispatches.log` track dispatched items with kind and timestamp.\n- The audit tier runs first, before implement/plan/intake tiers.\n- Under the leader-election refactor (WL-0MST3OJ8S0001ROL), only the leader dispatches, but multiple idle periods could span across a running audit.\n\n## Desired change\n\n1. **Active audit check** — Before selecting an audit candidate, query whether any audit-tier item is currently active (dispatched and `in_progress`). This can be done by: (a) scanning the dispatched-marker log for recent audit dispatches where the item is still `in_progress`, or (b) querying the worklog for `in_progress` items that match audit-tier criteria.\n2. **Audit tier skip** — If an active audit is found, skip the audit tier and proceed to the implement tier (unchanged fallback behavior).\n3. **Logging** — Log the skip reason (`audit-in-flight`) for observability.\n\n## Key Files (predicted)\n\n- `packages/herdr/src/downtime-worker.ts` — `dispatchDowntimeWork`: add active-audit check before `getNextAuditCandidate`; skip audit tier if active audit found; log skip reason.\n- `packages/herdr/src/index.ts` — possibly a new dep method `getActiveAudits(cwd)` or reuse existing dispatched-marker logic.\n- `packages/herdr/src/downtime-worker.test.ts` — tests: audit skipped when one active, audit dispatched when none active, active-audit check failure falls through gracefully.\n\n## Related work\n\n- **WL-0MST3OJ8S0001ROL** — Refactor Downtime Dispatcher: leader election with shared coordination file (open, plan_complete) — the primary context; this constraint applies to the leader-only dispatch model.\n- **WL-0MSI8H3HP000K0RG** — Allow /skill:audit on complete/in_review items without a valid audit (completed) — establishes the audit tier.\n- **WL-0MSLIY8ZR004QUSY** — Downtime dispatcher triggers multiple identical sessions (completed) — parent for dispatched-marker exclusion.\n\n### Related work (automated report)\n\n- **WL-0MT32F908002YFFA** — Downtime dispatch: recover items stranded by marker-write-failed or spawn-failed (open) — directly relevant to stale-audit timeout design; a crashed audit pane that fails to update the work item state is the scenario the timeout protects against.\n- **WL-0MSN3FWV5008KQE9** — Herdr plugin: activity-gated proxy mode switching (completed) — proxy idle/busy semantics relevant to dispatch timing.\n- **WL-0ML7QRBQR183KXPB** — Auto-unblock on dependency changes (completed) — dependency resolution patterns may inform how active-audit detection handles blocked items.\n\n## Risks & assumptions\n\n- **Assumption:** An audit is considered \"active\" from the moment it is dispatched (marked in `downtime-dispatches.log`) until the item transitions from `in_progress` to a terminal state (`completed`/`in_review` with a fresh audit, or reverted to `completed/in_review` for re-audit).\n- **Assumption:** The dispatch log or worklog query is fast enough to not impact dispatch latency (should be sub-second for typical worklog sizes).\n\n- **Risk:** The active-audit check adds complexity to the dispatch loop. Mitigation: keep the check simple (one worklog query or log scan) and fail-open (skip audit tier on query failure).\n- **Scope-creep guard:** Extending this constraint to implement/plan/intake tiers is out of scope. Only audit-tier sequentialization is required.\n- **Risk (leader-election context):** Multiple instances could have stale active-audit entries from crashed instances. Mitigation: the check should use the coordination file or dispatch log timestamps to detect stale entries.\n\n## Appendix: Clarifying questions & answers\n\n- Q1: \"Should the single-audit constraint apply only to audits, or to all dispatch tiers?\" — Answer (user): \"Audit only. Implement/plan/intake are not affected.\" Source: interactive reply.\n- Q2: \"How should we detect an 'active' audit? By worklog status or dispatch log?\" — Answer (user): \"Check for audit-tier items that are dispatched but not yet completed — use the dispatched markers or worklog query. The check must work in the leader-election context.\" Source: interactive reply.\n- Q3: \"What happens if the active-audit check fails?\" — Answer (user): \"Fail-open: skip the audit tier and fall through to implement. Do not block all dispatch.\" Source: interactive reply.\n- Q4: \"Should there be a timeout on active audits? (e.g., if an audit pane crashes, the item stays in_progress forever)\" — Answer (user): \"Yes — add a stale-audit timeout. If an audit dispatch entry is older than a reasonable threshold (e.g., 2 hours), treat it as stale and allow re-dispatch.\" Source: interactive reply.\n","effort":"Small","id":"WL-0MT3PHW4I002SNOV","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"high","risk":"Medium","sortIndex":1600,"stage":"plan_complete","status":"in-progress","tags":[],"title":"Ensure only single audit active during downtime dispatch","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-22T09:17:06.878Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# [test-failure] resolveDependencyFrontier / dispatch critical-first tier\n\n## Failure Signature\n\n- 18 failing tests in `packages/herdr/src/downtime-worker.test.ts`:\n  - `resolveDependencyFrontier` (7 tests)\n  - `dispatch critical-first tier` (11 tests)\n- Discovered during WL-0MT3D585I0070FF0 (F2 spare-capacity) full-suite run.\n\n## Root cause (NOT a regression from WL-0MT32F90V008UAD2 work)\n\nThese are the RED-PHASE test-first tests of the in-progress critical-first\nfeature **WL-0MT3FM8VA005XBHE** (\"Downtime dispatcher: critical items always\nprogress first\"). Its test matrix (WL-0MT3I9QJU002S34W) is committed on dev\nand the `getNextCriticalCandidate`/`selectCriticalCandidate` slice\n(WL-0MT3I9UVU004722X) is landed, but the remaining implementation slices\n(resolveDependencyFrontier — WL-0MT3I9YNZ007IC5V and tier wiring —\nWL-0MT3IA1UB005TLVJ) have NOT landed yet, so the tests stay red by design.\n\n## Resolution\n\n- NO action needed from WL-0MT32F90V008UAD2 (spare-capacity dispatch): the\n  failures are unrelated to its changes and verified pre-existing on dev\n  (main-checkout dev HEAD 9d728fbf shows the same 18 failures).\n- Resolved automatically when WL-0MT3FM8VA005XBHE's remaining implementation\n  slices land (dependency/descendant of that feature).\n- marked as discovered-from WL-0MT3D585I0070FF0 (this run's parent).","effort":"","id":"WL-0MT45ZPOD001G584","issueType":"bug","needsProducerReview":false,"parentId":"WL-0MT3D585I0070FF0","priority":"critical","risk":"","sortIndex":2200,"stage":"plan_complete","status":"open","tags":["test-failure"],"title":"[test-failure] resolveDependencyFrontier / dispatch critical-first tier (pre-existing red-phase tests of in-progress WL-0MT3FM8VA005XBHE) — failing test","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-22T09:54:27.170Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Document single-audit dispatch guarantee\n\n## Summary\n\nUpdate all related documentation to reflect the single-active-audit guarantee in downtime dispatch: the active-audit check mechanism, the 2-hour stale window, and the `audit-in-flight` skip reason.\n\n## Acceptance Criteria\n\n1. **All related documentation is updated** — Documentation reflects the changes: code comments, README, and any relevant wiki or docs site entries describe the single-active-audit guarantee.\n2. **Code comments** — The tier-order doc comment in `downtime-worker.ts` and the `getActiveAudit` JSDoc describe the active-audit check, its 2-hour stale threshold, and its fail-open behavior.\n3. **README** — The Downtime worker section of `packages/herdr/README.md` documents the single-active-audit guarantee, the check mechanism (dispatch markers + stale window), and the `audit-in-flight` skip reason.\n4. **No stale references** — No remaining documentation implies concurrent audit fan-out is possible.\n\n## Minimal Implementation\n\n- Update the dispatch tier-order comment and dep JSDoc in `packages/herdr/src/downtime-worker.ts` / `index.ts`.\n- Update the Downtime worker section in `packages/herdr/README.md`.\n- Grep for audit-tier documentation in docs/ and docs-site entries; update only where the single-audit behavior is described.\n\n## Key Files\n\n- `packages/herdr/README.md`\n- `packages/herdr/src/downtime-worker.ts`\n- `packages/herdr/src/index.ts`\n\n## Dependencies\n\n- F2 (Active-audit detection & audit-tier skip) — documents the implemented behavior.\n\n## Deliverables\n\nDocumentation changes only.\n","effort":"","id":"WL-0MT47BQAP004G3GY","issueType":"docs","needsProducerReview":false,"parentId":"WL-0MT3PHW4I002SNOV","priority":"medium","risk":"","sortIndex":13300,"stage":"intake_complete","status":"blocked","tags":[],"title":"Document single-audit dispatch guarantee","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-22T09:54:27.174Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Active-audit detection & audit-tier skip\n\n## Summary\n\nAdd an active-audit detection dependency (`getActiveAudit(cwd)`) that reads `kind=audit` dispatch markers from `downtime-dispatches.log` within a 2-hour stale window and confirms the item is still `in_progress` via a single worklog query; gate the audit tier in `dispatchDowntimeWork` and log the skip reason `audit-in-flight`.\n\n## Acceptance Criteria\n\n1. **Audit sequential enforcement** — Before dispatching an audit-tier item, the dispatcher checks whether any audit-tier item is currently `in_progress` (dispatched but not yet completed/reviewed). If one exists, the audit tier is skipped and the dispatcher falls through to the next tier.\n2. **Check mechanism** — The dispatcher queries the worklog for audit-tier items that have a corresponding dispatch entry in `downtime-dispatches.log` with `kind` `audit`; a dispatched audit entry older than a stale threshold (2 hours) is treated as stale and ignored (audit pane may have crashed without updating the work item).\n3. **Stale window constant** — A named constant `DOWNTIME_AUDIT_STALE_WINDOW_MS` (= 2 hours) defines the stale threshold; entries older than it are ignored.\n4. **Active audit blocks new dispatch** — If an active (non-stale) audit is found, the audit tier is skipped and dispatch falls through to the implement tier; the skip reason `audit-in-flight` is logged for observability, and when the remaining backlog is empty the outcome is `audit-in-flight` (never `no-candidate`), so the no-candidate cooldown is not entered (mirrors `code-freeze`).\n5. **Fail-open on check failure** — If the active-audit check cannot complete (e.g., worklog query fails), the audit tier is skipped and dispatch falls through to the implement tier; dispatch is never blocked.\n6. **Existing guards preserved** — The single-flight guard (`dispatchInFlight`), CAS claim, dispatched-marker exclusion, and code-freeze gate continue to work unchanged.\n7. **Leader-only check scope** — Only the elected leader performs the active-audit check; the check is placed in the audit-tier dispatch path only (leader-only under WL-0MST3OJ8S0001ROL), with no new instance-local state, so non-leader instances do not dispatch audits (unchanged behavior).\n8. **Full project test suite passes** — The complete test suite passes with the new changes.\n\n## Minimal Implementation\n\n- Extend `DowntimeWorkerDeps` with `getActiveAudit(cwd)` (returns `{ ok, active }`-shaped result).\n- Implement in `createDowntimeDeps` (`packages/herdr/src/index.ts`) using `readDowntimeLogEntries` + one `wl list --status in_progress --json` query intersected with non-stale audit dispatched ids; fail-open (never throws).\n- Wire the gate into `dispatchDowntimeWork` (`packages/herdr/src/downtime-worker.ts`) immediately before `getNextAuditCandidate`, within the audit tier branch; add the `audit-in-flight` outcome reason + stderr/log observability.\n- Add `DOWNTIME_AUDIT_STALE_WINDOW_MS` constant (2h).\n- Verify against F1 test battery; run full suite.\n\n## Key Files\n\n- `packages/herdr/src/downtime-worker.ts`\n- `packages/herdr/src/index.ts`\n- `packages/herdr/src/downtime-log.ts`\n\n## Dependencies\n\n- F1 (Active-audit single-flight tests) — test-first prerequisite.\n\n## Deliverables\n\nProduction code changes + passing full test suite.\n","effort":"","id":"WL-0MT47BQAT00375VB","issueType":"feature","needsProducerReview":false,"parentId":"WL-0MT3PHW4I002SNOV","priority":"high","risk":"","sortIndex":1700,"stage":"in_progress","status":"in-progress","tags":[],"title":"Active-audit detection & audit-tier skip","updatedAt":"2026-08-23T12:35:08.476Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-22T12:49:04.161Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — File is a shared library module (packages/herdr/shared/grid.py) with a docstring, not a standalone script — the executable bit is likely set accidentally and no shebang is expected for importable modules.. candidate false positive — producer decision required.","effort":"","id":"WL-0MT4DKAE8003UYT1","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":8200,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-22T13:22:13.330Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — grid.py is only ever invoked via `python3 grid.py` (docstring, send-to-pi.sh:172, open-pi-agent.sh:125, all tests use sys.executable), never executed directly, so the missing shebang is not a real defect and the executable bit is spurious metadata.. candidate false positive — producer decision required.","effort":"","id":"WL-0MT4EQX8Y001NPUM","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":8300,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-22T14:14:56.440Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py:1 — grid.py is never executed directly anywhere in the repo — all call sites (open-pi-agent.sh:125, send-to-pi.sh:172, tests via sys.executable) invoke it as `python3 grid.py` and its docs state that usage, so the executable bit is incidental and EXE002's premise (file meant to run directly) does not hold; remediation would be chmod -x/ruff config, not a code defect.. candidate false positive — producer decision required.","effort":"","id":"WL-0MT4GMPX3008HX4K","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":8400,"stage":"idea","status":"open","tags":[],"title":"ruff EXE002 in /home/rgardler/projects/ContextHub/packages/herdr/shared/grid.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-22T19:26:03.494Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# F3-fix: implement tier missing panesEligible free-slot guard\n\nBug found by re-audit of WL-0MT32F90V008UAD2 (2026-08-22, F3 AC2 partial): the implement tier in dispatchDowntimeWork has a 'Pane minimum' comment (downtime-worker.ts:1336) but NO panesEligible guard (downtime-worker.ts:1328-1341) — a direct dispatchDowntimeWork({freeSlots:0}) still dispatches implement. Only unreachable via the worker because the idle gate (N>=1, default 2) guarantees ≥1 free slot first.\n\n## Goal\n\nMake the implement tier honor the ≥1 free-slot pane minimum at selection time, matching the critical/audit/plan/intake tiers — so a direct dispatchDowntimeWork({freeSlots:0}) never dispatches implement.\n\n## Acceptance Criteria\n\n1. The implement tier dispatches only when ≥1 free slot is available at selection time (dispatchDowntimeWork({freeSlots:0}) never dispatches implement).\n2. With freeSlots >= 1 the implement tier behaves exactly as before (no regression to normal worker dispatch).\n3. With freeSlots undefined (direct legacy callers) the implement tier is not gated (fail-open preserved, matching all other tiers).\n4. The 0-free-slot defensive path returns no-candidate (matching the plan tier's defensive path) rather than dispatching.\n5. Ensure F1/F2/F3 existing tests still pass; add regression test(s) cover AC1/AC3/AC4.\n6. Full project test suite passes with the new changes.","effort":"","id":"WL-0MT4RQTID000GT69","issueType":"bug","needsProducerReview":false,"parentId":"WL-0MT32F90V008UAD2","priority":"high","risk":"","sortIndex":3600,"stage":"plan_complete","status":"open","tags":[],"title":"F3-fix: implement tier missing panesEligible free-slot guard","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-22T23:25:06.139Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# F1 — Acceptance tests: FTS freshness & error surfacing (tests-first)\n\n## Target outcome\n\nLock in parent AC1–AC5 with automated tests: index freshness on every write path (create, comment, sync/import, delta upsert, reconcile, delete) and punctuated-query behavior (auto-quote + warning, hard error when auto-quote fails).\n\n## Acceptance Criteria\n\n1. New item title is found by `db.search()` immediately after `db.create()` — no `--rebuild-index`.\n2. New comment text is found immediately after `db.createComment()` — no rebuild.\n3. After `db.import()` (full sync path), all imported items and their comments are searchable; items absent from the import are not.\n4. After `db.upsertItems()` (delta sync path), new/updated items are searchable.\n5. A dependency-reconcile status change (block → unblock) is reflected in status-filtered search (`search(q, { status: 'open' })` finds the item).\n6. Deleting an item or comment removes it from subsequent results; the `--deleted`-flag edge case is asserted/documented.\n7. Punctuated query `v0.1.11` (unquoted): auto-quote returns matches with a non-silent warning; a query that fails even when auto-quoted surfaces an error.\n8. `--rebuild-index` backstop still indexes everything and searches correctly; the full test suite passes.\n\n## Minimal Implementation\n\n- Extend `tests/fts-search.test.ts` (or add `tests/fts-freshness.test.ts`) using the `WorklogDatabase` API and temp-dir fixtures from `tests/test-utils.ts`.\n- Assert failure (red) on current code for the import/upsertItems/reconcile/syntax cases; assert green for the already-working create/comment/delete cases.\n- No production-code changes in this item.\n\n## Constraints\n\n- Tests must run in the existing vitest setup (`npx vitest run tests/…`).\n- Use `--allow-duplicate`/dedup window only if title collisions occur; keep titles distinct.\n\n## Dependencies\n\n- None (created first; F2/F3 depend on it).\n\n## Deliverables\n\n- Test suite additions; red/green matrix noted in the completion comment.\n\n## Key Files\n\n- `tests/fts-search.test.ts` (or new `tests/fts-freshness.test.ts`)\n- `tests/test-utils.ts` (existing fixtures)","effort":"","id":"WL-0MT50A8D6007VYFK","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSLW8UCP001771K","priority":"medium","risk":"","sortIndex":8500,"stage":"intake_complete","status":"open","tags":[],"title":"Acceptance tests: FTS freshness & error surfacing","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-22T23:25:11.189Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# F2 — Keep FTS index fresh on import/delta/reconcile paths\n\n## Target outcome\n\nClose the verified freshness gaps: after a full sync (`import()`), a delta sync (`upsertItems()`), or a dependency-reconcile status change, the FTS index reflects the current data without requiring a manual `--rebuild-index`.\n\n## Acceptance Criteria\n\n1. After `wl sync` / `db.import()`, every imported item (and its comments) is found by `wl search` without `--rebuild-index`.\n2. After `db.upsertItems()` (delta sync), new/updated items are found; unchanged items are skipped (no spurious upserts).\n3. Block/unblock reconciles keep `status`-filtered search correct (FTS `status` column refreshed).\n4. Repeated imports are idempotent — no duplicate FTS rows (upsert semantics; verified by row count).\n5. `wl search --rebuild-index` remains functional as the manual backstop (parent AC4).\n\n## Minimal Implementation\n\n- `import()` (packages/shared/src/database.ts ~line 2899): after the transaction, upsert each saved item into FTS via `upsertFtsEntry` (already concatenates comments).\n- `upsertItems()` (~line 2958): call `upsertFtsEntry` for each saved item.\n- `reconcileOpenStatus` / `reconcileDependentStatus` (~lines 3105/3128/3145): call `upsertFtsEntry` after the direct `store.saveWorkItem`.\n- Wiring lives at the `WorklogDatabase` layer, matching the existing 6 call sites (producer decision Q1=A). No store-layer changes. Rebuild `packages/shared/dist/` so the compiled product matches.\n\n## Constraints\n\n- FTS upserts must be idempotent (delete-then-insert semantics already in `upsertFtsEntry`).\n- Do not change `searchFts` behavior in this item (that is F3).\n\n## Dependencies\n\n- F1 (tests exist first; red before this work, green after).\n\n## Deliverables\n\n- Code changes; compiled dist refreshed; F1 tests green for import/upsertItems/reconcile cases.\n\n## Key Files\n\n- `packages/shared/src/database.ts`\n- `packages/shared/dist/database.js` (build output)","effort":"","id":"WL-0MT50AC9G003YNML","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSLW8UCP001771K","priority":"medium","risk":"","sortIndex":13400,"stage":"intake_complete","status":"blocked","tags":[],"title":"Keep FTS index fresh on import/delta/reconcile","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-22T23:25:12.879Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# F3 — Surface FTS5 syntax errors: auto-quote with warning\n\n## Target outcome\n\nStop silently swallowing FTS5 query syntax errors. Unquoted punctuated terms (versions, paths, ids — e.g. `v0.1.11`) are auto-quoted as a phrase and searched, with a **visible warning** in the output; when auto-quoting also fails, the error is surfaced with a non-zero exit (parent AC2, producer decision Q2=B).\n\n## Acceptance Criteria\n\n1. `wl search v0.1.11` (unquoted punctuated term) returns matches AND prints a visible warning that the term was interpreted as a phrase — never a silent empty result.\n2. A query that fails even when auto-quoted exits non-zero with a clear `invalid query — quote phrases`-style message (parent AC2 fallback).\n3. Genuinely zero-match queries still print \"No results found.\" and exit 0.\n4. JSON mode reports the change via structured fields (`success`, `error`, `warning`) without contaminating `workItems`.\n5. Non-syntax DB errors are no longer masked as empty results (logged with context; distinguished from `no matches`).\n\n## Minimal Implementation\n\n- `searchFts` (packages/shared/src/persistent-store.ts ~line 1623): classify the catch — FTS5 syntax errors (message matches `fts5: syntax error near …`) trigger an auto-quote retry (raw query wrapped as a quoted phrase); if the retry succeeds, return results with an \"auto-quoted\" signal; if it fails, throw a typed error. Other DB errors are rethrown with context.\n- `Db.search()` (packages/shared/src/database.ts ~line 778): propagate results + optional warning/error surrogates without breaking the existing `{ results, ftsUsed }` shape (extend, don't remove fields).\n- `src/commands/search.ts`: print the warning on auto-quote; on unhandleable syntax error call `output.error(...)` + `process.exit(1)`; include warning/error in the JSON payload.\n\n## Constraints\n\n- Preserve the existing search result shape for downstream consumers (`FtsSearchResult[]`); additive changes only.\n- Keep the fallback path (`searchFallback`) untouched.\n\n## Dependencies\n\n- F1 (error-surface tests exist first; red before this work, green after).\n\n## Deliverables\n\n- Code changes; compiled dist refreshed; F1 error tests green; `wl search v0.1.11` behavior verified in CLI integration.\n\n## Key Files\n\n- `packages/shared/src/persistent-store.ts`\n- `packages/shared/src/database.ts`\n- `src/commands/search.ts`","effort":"","id":"WL-0MT50ADKE001VKBN","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSLW8UCP001771K","priority":"medium","risk":"","sortIndex":13500,"stage":"intake_complete","status":"blocked","tags":[],"title":"Surface FTS5 syntax errors: auto-quote with warning","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-22T23:25:14.812Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# F4 — FTS search docs & quoting hygiene\n\n## Target outcome\n\nDocument the FTS freshness guarantee, the auto-quote-with-warning behavior for punctuated terms, the `--deleted` edge case, and the `--rebuild-index` backstop (parent AC5; producer decisions Q2=B, Q3=A).\n\n## Acceptance Criteria\n\n1. `CLI.md` and `docs/dev/fts.md` document FTS query syntax and that punctuated terms (versions/paths/ids) are auto-quoted with a visible warning.\n2. `wl search --help` text mentions quoting hygiene and the warning behavior.\n3. The `--deleted` edge case is documented: CLI-deleted items are removed from the index (parent AC3); the flag applies to items imported with `status='deleted'`.\n4. `--rebuild-index` is documented as the backstop/manual backfill tool for bulk migrations and recovery.\n\n## Minimal Implementation\n\n- Update `CLI.md` search sections and add a `docs/dev/fts.md` note on index freshness (every create/update/comment/import path) and error behavior.\n- Update the `search.ts` command help string.\n\n## Constraints\n\n- Doc-only; no production behavior changes (unless a doc review flags an inconsistency, which becomes a comment on the parent).\n\n## Dependencies\n\n- F2, F3 (docs reflect final behavior).\n\n## Deliverables\n\n- Updated docs (CLI.md, docs/dev/fts.md, help text).\n\n## Key Files\n\n- `CLI.md`\n- `docs/dev/fts.md`\n- `src/commands/search.ts` (help string)","effort":"","id":"WL-0MT50AF24006V6WH","issueType":"docs","needsProducerReview":false,"parentId":"WL-0MSLW8UCP001771K","priority":"medium","risk":"","sortIndex":13600,"stage":"intake_complete","status":"blocked","tags":[],"title":"FTS search docs & quoting hygiene","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T03:25:38.816Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Acceptance Criteria\n\n1. **New cycle: null → false → null.** After two consecutive  calls,  is  (follow settings), not .\n2. **First press disables.** After one  call from ,  is  and  is .\n3. **Second press returns to follow-settings.** After two  calls from ,  is  and  follows the global setting (no force-enable).\n4. **Tick short-circuits after disable.** After one , a subsequent  returns .\n5. **Second press doesn't re-enable.** After two  calls from , a subsequent  behaves normally (follows global setting).\n6. **Three presses cycle null→false→null→false.** After three  calls from ,  is  (same as after one press).\n\n**Test file:**  — new test in the existing per-instance toggle override describe block.\n\n**Key Files:**\n- \n-  (toggle implementation)\n","effort":"","id":"WL-0MT58VKOW0066XJ6","issueType":"task","needsProducerReview":false,"parentId":"WL-0MT4BWUHW008LIFE","priority":"medium","risk":"","sortIndex":8600,"stage":"intake_complete","status":"open","tags":[],"title":"Test: toggle() new cycle null→false→null (no force-enable)","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T03:26:18.802Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Acceptance Criteria\n\n1. **toggle() cycles null → false → null.** After two toggle() calls from null, override is null (not true).\n2. **Single disable works.** After one toggle() from null, override is false and enabled is false.\n3. **tick() short-circuits when disabled.** After toggle() sets override to false, tick() returns {polled:false, dispatched:false, idle:false}.\n4. **No force-enable on second press.** The cycle must NOT go through true — the second press returns to null (follow settings), never force-enables dispatch.\n5. **All existing tests pass.** The pre-existing toggle cycle test and related tests update to reflect the new null→false→null cycle.\n6. **Two independent workers toggle independently.** The per-instance isolation test still passes.\n\n**Implementation:**\n- Modify toggle() in packages/herdr/src/downtime-worker.ts: change `override = override === null ? false : override === false ? true : null;` to `override = override === null ? false : null;`\n- Update the JSDoc comment for toggle() to reflect the new cycle.\n- Update the test at line ~2679 in downtime-worker.test.ts to assert null→false→null instead of null→false→true→null.\n- Update all dependent tests that assert the old cycle pattern.\n\n**Key Files:**\n- packages/herdr/src/downtime-worker.ts\n- packages/herdr/src/downtime-worker.test.ts\n- packages/herdr/src/index.ts (if toggle is called from onDowntimeToggle)","effort":"","id":"WL-0MT58WFJM001REH5","issueType":"task","needsProducerReview":false,"parentId":"WL-0MT4BWUHW008LIFE","priority":"medium","risk":"","sortIndex":13700,"stage":"intake_complete","status":"blocked","tags":[],"title":"Implement: fix toggle cycle to null→false→null","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T03:26:41.565Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Acceptance Criteria\n\n1. **Marker file created on disable.** After toggle() sets override to false, a marker file `.herdr-downtime-disabled` is created in the worklog root (cwd) if it doesn't already exist.\n2. **Marker file removed on re-enable.** When override returns to null (second toggle), the marker file is deleted if it exists.\n3. **Worker restores from marker on init.** When createDowntimeWorker is called and the marker file exists in cwd, override starts as false (disabled).\n4. **No marker = no restore.** When no marker file exists, override starts as null (follow settings), matching current behavior.\n5. **Marker file is per-worklog-root.** The marker is in the worklog root directory (where worklog-data.jsonl lives), not in a global location.\n6. **Tests.** Unit tests for marker creation/deletion, and integration test for worker init from marker.\n\n**Test file:** New test block in downtime-worker.test.ts for marker persistence.\n\n**Key Files:**\n- packages/herdr/src/downtime-worker.test.ts\n- packages/herdr/src/downtime-worker.ts\n- packages/herdr/src/index.ts (worker initialization)","effort":"","id":"WL-0MT58WX3X0092PTK","issueType":"task","needsProducerReview":false,"parentId":"WL-0MT4BWUHW008LIFE","priority":"medium","risk":"","sortIndex":8700,"stage":"intake_complete","status":"open","tags":[],"title":"Test: persisted disable marker behavior","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T06:48:43.852Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). I001 in /home/rgardler/projects/ContextHub/scripts/audit_runner.py:81 — The import block is logically grouped (stdlib → third-party → sys.path manipulation → local imports) and imports within each group are alphabetically sorted; the sys.path.insert() in the middle is an intentional and necessary code pattern for this skill/runner, not a real sorting defect. candidate false positive — producer decision required.","effort":"","id":"WL-0MT5G4QQ400259XK","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":8800,"stage":"idea","status":"open","tags":[],"title":"ruff I001 in /home/rgardler/projects/ContextHub/scripts/audit_runner.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T07:01:46.716Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Code quality finding\n\n- **Severity**: medium\n- **File**: /home/rgardler/projects/ContextHub/scripts/audit_runner.py\n- **Line**: 81\n- **Message**: Import block is un-sorted or un-formatted\n- **Linter**: ruff\n- **Code**: I001\n\nDiscovered during automated code quality review.","effort":"","id":"WL-0MT5GLISB005TGS6","issueType":"task","needsProducerReview":false,"parentId":"WL-0MSS3380K0002ZVO","priority":"medium","risk":"","sortIndex":8900,"stage":"intake_complete","status":"open","tags":["Refactor"],"title":"[MEDIUM] /home/rgardler/projects/ContextHub/scripts/audit_runner.py:81 — Import block is un-sorted or un-formatted (I001)","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T08:10:04.085Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Goal\n\nDocument the SQLite WAL checkpoint behavior in Worklog and explain the root cause of the 16MB WAL observed during the 206-process spawn storm.\n\n## Investigation\n\n### Current Configuration (src/persistent-store.ts, ~line 130)\n\nThe database opens with:\n- `PRAGMA journal_mode = WAL` — enables Write-Ahead Logging for concurrency\n- `PRAGMA foreign_keys = ON` — enforces referential integrity\n- `PRAGMA busy_timeout` — 5000ms (non-TUI) or 250ms (TUI), configurable via `WL_SQLITE_BUSY_TIMEOUT_MS`\n\n**No explicit checkpoint logic exists.** No `PRAGMA wal_checkpoint` or `PRAGMA wal_autocheckpoint` is set anywhere in the codebase.\n\n### What SQLite Does by Default\n\n- WAL auto-checkpoint triggers after **1000 pages** of writes (SQLite default)\n- At default page size (4096 bytes), this is ~**4MB** of accumulated writes\n- Auto-checkpoint runs when a connection checks between transactions\n- On close, SQLite performs a checkpoint that merges the WAL back into the main database\n\n### Root Cause of 16MB WAL Under 206 Concurrent Writers\n\n1. Each of the 206 concurrent `wl` processes opens a SQLite connection\n2. All writes go into the shared WAL file (`worklog.db-wal`)\n3. Each connection checks for auto-checkpoint between transactions\n4. But writes accumulate faster than any single connection can checkpoint\n5. With 206 writers, WAL segments from concurrent writers stack up, exceeding the 1000-page threshold many times\n6. The WAL reaches ~16MB because concurrent writers push writes faster than auto-checkpoint can reclaim pages\n7. When all processes exit, checkpoint-on-close merges everything back (WAL files disappear)\n\n### Why This Matters\n\n- **Read cost**: SQLite reads must scan the WAL, so a large WAL slows every read query\n- **Cache invalidation**: mtime/size of `worklog.db` alone is unreliable while a large WAL holds live writes (read-cache.ts already handles this correctly)\n- **Crash/recovery**: a large WAL means longer recovery time on abnormal exit\n\n### Code References\n\n- `src/persistent-store.ts` — WAL mode + busy_timeout configuration (lines ~125-135)\n- `packages/shared/src/persistent-store.ts` — same configuration\n- `src/read-cache.ts` — WAL-aware cache invalidation (already correct)\n- `packages/shared/src/database.ts` — write patterns (importData transactions, individual saves)\n\n## Acceptance Criteria\n\n1. This work item describes the exact SQLite WAL configuration (pragmas set, defaults used).\n2. Root cause of the 16MB WAL is identified and explained (concurrent writers outpace auto-checkpoint).\n3. All ACs from the parent (WL-0MSG8EG7P002MX2I) related to checkpoint understanding are covered.\n","effort":"","id":"WL-0MT5J1CC4007QJFP","issueType":"","needsProducerReview":false,"parentId":"WL-0MSG8EG7P002MX2I","priority":"medium","risk":"","sortIndex":9000,"stage":"intake_complete","status":"open","tags":[],"title":"Document WAL checkpoint behavior and root cause of 16MB WAL","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T08:12:04.239Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Goal\n\nMeasure the impact of a large WAL on read latency and produce before/after numbers for the parent AC.\n\n## Approach\n\n1. Create a temporary SQLite database with WAL mode enabled (mimicking Worklog schema)\n2. Write N work items (e.g., 500-1000) to build up a large WAL (target > 4MB)\n3. Run read queries (getAllWorkItems, search, getWorkItem by ID) and measure latency\n4. Force a checkpoint (PRAGMA wal_checkpoint(PASSIVE))\n5. Run the same read queries again and measure latency\n6. Compare before/after numbers\n\n## Expected Findings\n\nBased on SQLite behavior, a large WAL adds overhead because:\n- Each read must scan the WAL to find the latest version of each page\n- The WAL is read sequentially, which is fast on SSD but slower than reading the main DB from page cache\n- Expected impact: 10-50% slower reads with a 16MB WAL vs a checkpointed database\n\n## Acceptance Criteria\n\n1. Benchmark script or code exists that reproduces a large WAL scenario.\n2. Read latency measured with small WAL vs large WAL is documented with numbers.\n3. Results are recorded as a comment on this work item and referenced by the parent AC #2.\n4. Benchmark is reproducible and documented (command to run, parameters, results).\n","effort":"","id":"WL-0MT5J3X1R009A6I6","issueType":"","needsProducerReview":false,"parentId":"WL-0MSG8EG7P002MX2I","priority":"medium","risk":"","sortIndex":9100,"stage":"intake_complete","status":"open","tags":[],"title":"Measure read latency impact of large WAL","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T08:12:41.842Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Goal\n\nProduce a concrete recommendation on WAL checkpointing strategy with rationale and risk notes.\n\n## Options to Evaluate\n\n### Option A: Leave as-is (rely on auto-checkpoint)\n**Pros**: Minimal code change, no risk of introducing bugs.\n**Cons**: WAL can grow under concurrent writer load; read latency impact unknown without benchmarking.\n**Risk**: Low. WAL mode is inherently safe; the worst case is degraded read performance.\n\n### Option B: Add explicit checkpoint after write-heavy operations\n**Pros**: Controls WAL growth proactively; keeps reads fast.\n**Cons**: Adds I/O overhead after operations like `wl sync` or bulk mutations.\n**Risk**: Low. `PRAGMA wal_checkpoint(PASSIVE)` is safe — it won't block readers.\n**Implementation**: Add `this.db.pragma('wal_checkpoint(PASSIVE)')` after `importData()` transactions or after `wl sync` completes.\n\n### Option C: Tune auto-checkpoint threshold\n**Pros**: No code changes to write paths; adjusts the SQLite default.\n**Cons**: Less precise control; still relies on per-connection checkpoint checks.\n**Risk**: Very low. `PRAGMA wal_autocheckpoint=N` changes the threshold.\n**Implementation**: Set a higher threshold (e.g., 10000 pages ≈ 40MB) in persistent-store.ts constructor.\n\n### Option D: Hybrid — tune threshold + checkpoint after sync\n**Pros**: Best of both worlds; threshold prevents runaway growth, explicit checkpoint after sync keeps WAL small.\n**Cons**: Slightly more code changes.\n**Risk**: Low.\n\n## Recommendation\n\n**Recommend Option C (tune auto-checkpoint) as the primary fix** with the following rationale:\n\n1. The root cause is that the default 1000-page threshold is too conservative for concurrent writer workloads.\n2. Raising it to ~10000 pages (40MB) would prevent the 16MB WAL from triggering frequent checkpoints while still capping growth.\n3. WAL files up to 40MB are not a correctness risk — they only impact read latency.\n4. For `wl sync` specifically (the main write-heavy operation), adding an explicit `PRAGMA wal_checkpoint(PASSIVE)` after the sync completes would ensure a clean state for the next operation.\n5. This approach requires minimal code changes and has negligible risk.\n\n## Implementation Sketch\n\n```typescript\n// In persistent-store.ts constructor, after journal_mode = WAL:\nthis.db.pragma('wal_autocheckpoint = 10000');  // 40MB threshold\n```\n\nAnd optionally after bulk write operations:\n```typescript\n// In importData() or after sync merge:\nthis.db.pragma('wal_checkpoint(PASSIVE)');\n```\n\n## Acceptance Criteria\n\n1. A recommendation is recorded on this work item: leave as-is, add explicit checkpointing, tune threshold, or hybrid — with rationale and risk notes.\n2. If a code change is recommended, the implementation scope is clearly defined.\n3. All ACs from the parent (WL-0MSG8EG7P002MX2I) are satisfied.\n4. Any code change is tested and does not regress the full test suite (covered by parent AC #4).\n","effort":"","id":"WL-0MT5J4Q290025O0L","issueType":"","needsProducerReview":false,"parentId":"WL-0MSG8EG7P002MX2I","priority":"medium","risk":"","sortIndex":9200,"stage":"intake_complete","status":"open","tags":[],"title":"Produce recommendation on WAL checkpointing strategy","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T08:17:44.282Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). I001 in /home/rgardler/projects/ContextHub/scripts/audit_runner.py:81 — The import block is correctly sorted: __future__ first, followed by stdlib import statements in alphabetical order (argparse→uuid), then from-imports in alphabetical order (collections.abc→pathlib); no actual sorting violation exists.. candidate false positive — producer decision required.","effort":"","id":"WL-0MT5JB7FD005CB7V","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":9300,"stage":"idea","status":"open","tags":[],"title":"ruff I001 in /home/rgardler/projects/ContextHub/scripts/audit_runner.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T09:07:35.483Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"Create a shared pickFields() projection helper that projects work items to only the requested fields, with validation against a known field whitelist and guaranteed inclusion of id.\n\nFile: packages/shared/src/fields.ts\nExport: pickFields(item, fields) - rejects unknown field names, always includes id, returns full item when fields undefined.\nValid field list: id, title, description, status, stage, priority, issueType, assignee, tags, createdAt, updatedAt, parentId, needsProducerReview, sortIndex\n\nAcceptance Criteria:\n1. pickFields(item, [\"id\", \"title\"]) returns object with only id and title\n2. pickFields(item, [\"nonexistent\"]) throws/returns error listing valid fields\n3. pickFields(item, []) returns object with only id\n4. pickFields(item, undefined) returns the full item (no projection when not requested)\n\nKey Files:\n- packages/shared/src/fields.ts (new)\n- packages/shared/src/types.ts (export)","effort":"","id":"WL-0MT5L3BGA001MKOP","issueType":"","needsProducerReview":false,"parentId":"WL-0MSLW8GHQ0092PJK","priority":"medium","risk":"","sortIndex":9400,"stage":"intake_complete","status":"open","tags":[],"title":"Create pickFields() projection helper module","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T09:08:36.405Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"Write unit tests for the pickFields() helper: field whitelist validation, unknown-field rejection, id always present, empty fields returns only id, undefined fields returns full item.\n\nAcceptance Criteria:\n1. Unit test: pickFields(item, [\"id\", \"title\"]) returns object with only id and title\n2. Unit test: pickFields(item, [\"nonexistent\"]) throws error listing valid fields\n3. Unit test: pickFields(item, []) returns object with only id\n4. Unit test: pickFields(item, undefined) returns the full item\n5. Unit test: pickFields preserves all requested fields with correct values\n\nKey Files:\n- packages/shared/src/fields.test.ts (new)\n- packages/shared/src/fields.ts","effort":"","id":"WL-0MT5L4MGL000Z43Y","issueType":"","needsProducerReview":false,"parentId":"WL-0MSLW8GHQ0092PJK","priority":"medium","risk":"","sortIndex":13800,"stage":"intake_complete","status":"blocked","tags":[],"title":"Write unit tests for pickFields projection helper","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T09:09:00.818Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"Add --fields <comma-separated> option to the wl list command.\n\nImplementation:\n1. Add fields?: string to ListOptions in cli-types.ts\n2. Add option in list.ts: .option(\"--fields <fields>\", \"Comma-separated list of fields to include in output (id always included)\")\n3. After enriching items (audit/childCount) and before JSON output, apply pickFields projection\n4. Validate fields list, reject unknown names with clear error listing valid fields\n5. Always include id in output\n\nAcceptance Criteria:\n1. wl list --fields id,title --json returns objects with only id and title\n2. wl list --fields id,title,status --stage in_review --json works with filters\n3. wl list --fields invalid_field exits non-zero with error listing valid fields\n4. Without --fields, output is unchanged (backward compatible)\n\nKey Files:\n- src/commands/list.ts\n- src/cli-types.ts\n- packages/shared/src/fields.ts","effort":"","id":"WL-0MT5L55AQ002WTGQ","issueType":"","needsProducerReview":false,"parentId":"WL-0MSLW8GHQ0092PJK","priority":"medium","risk":"","sortIndex":13900,"stage":"intake_complete","status":"blocked","tags":[],"title":"Add --fields option to wl list","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T09:10:01.641Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"Add --fields <comma-separated> option to the wl search command.\n\nImplementation:\n1. Add fields?: string to SearchOptions in cli-types.ts\n2. Add option in search.ts: .option(\"--fields <fields>\", \"Comma-separated list of fields to include in output (id always included)\")\n3. In the JSON output section, after building jsonResults, apply pickFields projection\n4. For search, --fields applies to the work item object portion (not the search score/snippet fields which are search-specific)\n5. Validate fields list, reject unknown names with clear error listing valid fields\n6. Always include id in output\n\nAcceptance Criteria:\n1. wl search <term> --fields id,title,status --json returns compact objects with only those fields\n2. wl search <term> --fields invalid_field exits non-zero with error listing valid fields\n3. Search results with --fields still include score, snippet, matchedField (search-specific fields)\n4. Without --fields, search output is unchanged (backward compatible)\n\nKey Files:\n- src/commands/search.ts\n- src/cli-types.ts\n- packages/shared/src/fields.ts","effort":"","id":"WL-0MT5L6G88006N4WD","issueType":"","needsProducerReview":false,"parentId":"WL-0MSLW8GHQ0092PJK","priority":"medium","risk":"","sortIndex":14000,"stage":"intake_complete","status":"blocked","tags":[],"title":"Add --fields option to wl search","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T09:10:18.974Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"Integration tests verifying the complete --fields workflow end-to-end for both wl list and wl search commands.\n\nTest scenarios:\n1. wl list --fields id,title --json: verify size reduction for large datasets\n2. wl list with --fields combined with --status, --priority, --stage filters\n3. wl search --fields id,title,status --json: verify field projection in search\n4. wl search --fields combined with --semantic flag\n5. Invalid field names: non-zero exit, clear error message listing valid fields\n6. Backward compatibility: all existing outputs without --fields unchanged\n\nAcceptance Criteria:\n1. wl list --stage in_review --fields id,title --json is significantly smaller than full output (target: >=90% reduction for large completed datasets)\n2. wl list and wl search produce consistent field vocabulary\n3. Invalid field names produce non-zero exit and clear error\n4. Full test suite passes\n\nKey Files:\n- src/commands/list.ts\n- src/commands/search.ts\n- packages/shared/src/fields.ts\n- packages/shared/src/fields.test.ts","effort":"","id":"WL-0MT5L6TLQ004NDLK","issueType":"","needsProducerReview":false,"parentId":"WL-0MSLW8GHQ0092PJK","priority":"medium","risk":"","sortIndex":14100,"stage":"intake_complete","status":"blocked","tags":[],"title":"Integration test: end-to-end --fields flag verification","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T09:48:25.290Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). I001 in /home/rgardler/projects/ContextHub/scripts/audit_runner.py:81 — The I001 finding is caused by intentional sys.path manipulation code between import blocks (required before importing local modules); imports within each block are properly sorted.. candidate false positive — producer decision required.","effort":"","id":"WL-0MT5MJTQI0058LN4","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":9500,"stage":"idea","status":"open","tags":[],"title":"ruff I001 in /home/rgardler/projects/ContextHub/scripts/audit_runner.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T10:36:13.206Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"False-positive candidate (medium/low severity). I001 in /home/rgardler/projects/ContextHub/scripts/audit_runner.py:81 — The 'un-sorted' flag is triggered by intentional sys.path manipulation between standard-library and project import blocks — a well-established Python pattern, not a real import-sorting defect.. candidate false positive — producer decision required.","effort":"","id":"WL-0MT5O9AMT006LNOI","issueType":"chore","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":9600,"stage":"idea","status":"open","tags":[],"title":"ruff I001 in /home/rgardler/projects/ContextHub/scripts/audit_runner.py — candidate false positive — producer decision required","updatedAt":"2026-08-23T12:09:34.403Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T12:33:10.557Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[{"from":"WL-0MT5SFP990001FNW","to":"WL-0MT58WFJM001REH5"},{"from":"WL-0MT5SFP990001FNW","to":"WL-0MT58WX3X0092PTK"}],"description":"## Acceptance Criteria\n\n1. **Marker file written on disable.** When toggle() transitions override to false, write a marker file `.herdr-downtime-disabled` in the worklog root (cwd). No write when the marker already exists.\n2. **Marker removed on re-enable.** When toggle() transitions override to null, delete the marker file if present. No error if absent.\n3. **Worker init reads the marker.** createDowntimeWorker checks for the marker in cwd at construction; if present, the initial override is false (opts.override wins if explicitly provided, marker is the fallback).\n4. **tick() gate unchanged.** tick() continues to short-circuit on `!(override ?? cfg.enabled)` — no behavior change to the dispatch gate itself.\n5. **Dispatch side channels unchanged.** No new dispatch mechanism introduced; only when the worker dispatches changes.\n6. **Existing downtime-worker tests pass.** No regressions in the toggle, tick, cooldown, or single-flight suites.\n\n**Implementation notes:**\n- Use the same fs pattern as existing marker files (downtime-log.ts writes dispatched-id markers in .worklog), e.g. a small `downtime-disable-marker.ts` helper or inline fs calls in createDowntimeWorker.\n- The marker path is derived from opts.config().cwd (the worklog root), e.g. `<cwd>/.herdr-downtime-disabled`. Confirm the cwd passed to the worker is the worklog root at implementation time.\n- Override priority on construction: explicit opts.override > marker file > null (follow settings).\n\n**Key Files:**\n- packages/herdr/src/downtime-worker.ts\n- packages/herdr/src/downtime-log.ts (existing marker pattern to mirror)\n- packages/herdr/src/index.ts (worker wiring, if cwd resolution needed)","effort":"","id":"WL-0MT5SFP990001FNW","issueType":"task","needsProducerReview":false,"parentId":"WL-0MT4BWUHW008LIFE","priority":"medium","risk":"","sortIndex":14200,"stage":"intake_complete","status":"blocked","tags":[],"title":"Implement: persisted disable marker (.herdr-downtime-disabled)","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T12:33:20.657Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Acceptance Criteria\n\n1. **Rendered state agrees with effective gate.** For each effective state (off/override=false, enabled+idle, enabled+paused, dispatching), renderDowntimeStatus output is asserted against the worker's effective enabled state. A pane whose dispatch gate is disabled (override false) NEVER renders the enabled-idle string `[⏳ downtime idle m:ss]`.\n2. **Disabled renders [Downtime Off].** worker.override === false renders `[Downtime Off]` regardless of global settings.enabled.\n3. **Enabled renders live status.** worker.override === null with settings enabled and idleSince set renders the idle string; dispatching renders the dispatching string; paused renders the paused string.\n4. **Restored-from-marker renders an explicit notice.** A worker restored from the persisted disable marker renders a notice (e.g. appended marker/notice suffix) so a pane previously shown [Downtime Off] that re-enables via settings change is never silent — the restored-disabled state must be visible from the header.\n5. **After re-enable, notice is gone.** When override returns to null AND the marker file is absent, no notice renders (back to normal live status).\n6. **Existing header tests pass.** No regression in worklist.test.ts header render tests.\n\n**Test file:** packages/herdr/src/worklist.test.ts (renderDowntimeStatus tests) + downtime-worker.test.ts (restored-state getter).\n\n**Key Files:**\n- packages/herdr/src/worklist.ts\n- packages/herdr/src/worklist.test.ts\n- packages/herdr/src/downtime-worker.test.ts","effort":"","id":"WL-0MT5SFX1S002YUYZ","issueType":"task","needsProducerReview":false,"parentId":"WL-0MT4BWUHW008LIFE","priority":"medium","risk":"","sortIndex":9700,"stage":"intake_complete","status":"open","tags":[],"title":"Test: header honesty + restored-disable notice","updatedAt":"2026-08-23T12:33:20.826Z"},"type":"workitem"}
{"data":{"assignee":"","createdAt":"2026-08-23T12:33:25.627Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Acceptance Criteria\n\n1. **renderDowntimeStatus never lies.** The rendered state always agrees with the worker's effective dispatch decision (override ?? settings): a disabled pane renders [Downtime Off], never the enabled-idle string.\n2. **Restored-disable notice.** When the worker was initialized/restored from the persisted disable marker (override false via marker), the header shows an explicit notice signaling \"disable persisted across restart\" (e.g. `[Downtime Off (restored)]` or similar suffix). A silent revert to enabled-idle in a pane previously shown off CANNOT occur.\n3. **No notice after explicit re-enable.** Once the operator re-enables (marker deleted, override null), the notice is not rendered.\n4. **Per-pane semantics preserved.** The notice and Off state are per-instance; sibling panes are unaffected.\n5. **Tests updated/passing.** Existing renderDowntimeStatus tests pass; new tests from the header-honesty test item pass (created test-first).\n\n**Implementation notes:**\n- Expose a worker getter, e.g. `restoredFromMarker`/`persistedDisabled`, reflecting that the current false override came from the marker file rather than a live toggle() press.\n- renderDowntimeStatus (worklist.ts ~2777) renders the notice when enabled === false AND restoredFromMarker === true.\n- Keep the notice text minimal and consistent with existing ANSI dim styling.\n\n**Key Files:**\n- packages/herdr/src/worklist.ts\n- packages/herdr/src/downtime-worker.ts\n- packages/herdr/src/index.ts\n- packages/herdr/src/worklist.test.ts","effort":"","id":"WL-0MT5SG0VU005ARUR","issueType":"task","needsProducerReview":false,"parentId":"WL-0MT4BWUHW008LIFE","priority":"medium","risk":"","sortIndex":9800,"stage":"intake_complete","status":"open","tags":[],"title":"Implement: header honesty + restored-disable notice","updatedAt":"2026-08-23T12:33:25.795Z"},"type":"workitem"}
{"data":{"author":"AI-Assistant","comment":"Marked ready to close: status completed / stage in_review, needsProducerReview set for producer sign-off. Audit (2026-08-19) confirmed 'Ready to close: Yes' — all 8 ACs met, all children terminal. Awaiting producer approval / dev→main release via ship skill.","createdAt":"2026-08-23T12:14:59.243Z","id":"WL-C0MT5RSB6Z005SMO4","references":[],"workItemId":"WL-0MSGHM5BQ0096BNJ"},"type":"comment"}
{"data":{"author":"effort_and_risk_skill","comment":"# Effort and Risk Report\n\n## Effort Estimate\n\n- **T-shirt size:** Small\n- **Three-point (PERT):** O=3.00h, M=2.00h, P=1.00h\n- **Expected (E=(O+4M+P)/6):** 2.00h\n- **Recommended (with overheads):** 4.50h\n- **Range:** [5.50h — 3.50h]\n- **Unit:** hours\n\n### Work Breakdown (suggested)\n\n_No detailed WBS items provided. Recommended breakdown based on estimate:_\n\n| Component | % of Effort | Estimated Hours |\n|-----------|-------------|-----------------|\n| Design & Planning | 15% | 0.67h |\n| Implementation — Core Logic | 30% | 1.35h |\n| Implementation — Edge Cases | 15% | 0.67h |\n| Testing & QA | 15% | 0.67h |\n| Documentation & Rollout | 10% | 0.45h |\n| Coordination & Review | 10% | 0.45h |\n| Risk Buffer | 5% | 0.23h |\n\n## Risk Assessment\n\n- **Risk Score:** 10/25 — **Medium**\n- **Probability:** 3.16/5 | **Impact:** 3.16/5\n\n## Confidence\n\n- **Confidence:** 74%\n- **Unknowns:** none\n- **Assumptions:** The four field pairs are fixed as specified in the AC; No changes to WorkItem type or data model needed\n\n\n```json\n{\n  \"effort\": {\n    \"unit\": \"hours\",\n    \"tshirt\": \"Small\",\n    \"o\": 3.0,\n    \"m\": 2.0,\n    \"p\": 1.0,\n    \"expected\": 2.0,\n    \"recommended\": 4.5,\n    \"range\": [\n      5.5,\n      3.5\n    ]\n  },\n  \"risk\": {\n    \"probability\": 3.16,\n    \"impact\": 3.16,\n    \"score\": 10,\n    \"level\": \"Medium\",\n    \"top_drivers\": [],\n    \"mitigations\": [\n      \"Add targeted tests and integration checks\",\n      \"Lock dependencies and add compatibility tests\",\n      \"Schedule extra review for risky components\"\n    ]\n  },\n  \"confidence_percent\": 74,\n  \"assumptions\": [\n    \"The four field pairs are fixed as specified in the AC\",\n    \"No changes to WorkItem type or data model needed\"\n  ],\n  \"unknowns\": []\n}\n```","createdAt":"2026-08-23T12:36:38.587Z","id":"WL-C0MT5SK5RV009RHGF","references":[],"workItemId":"WL-0MSNIX4V60012266"},"type":"comment"}
{"data":{"author":"Map","comment":"Producer decision recorded (2026-08-23, interactive): **goal = prevent ping-pong**. Contradiction resolved as Reading A: effort/risk stay EXCLUDED from the merge field lists (ping-pong prevention by construction — DB re-timestamp amplifier hasWorkItemChanged already tracks these fields, so merge participation would let one unguarded import become the newest record and propagate). Guard scope: merge-participating fields (status/stage/priority/assignee) get detect → refuse → report → override (--accept-regressions); effort/risk remote-default attempts are reported as protected conflicts (local kept, not imported). Adding effort/risk to the merge is a non-goal. AC1/AC5/Desired-Change/Appendix updated accordingly.","createdAt":"2026-08-23T12:31:13.163Z","id":"WL-C0MT5SD6OA001CSC8","references":[],"workItemId":"WL-0MSOYWZTH003JFVD"},"type":"comment"}
